- Continued the admin tenant workspace implementation with a safety pass instead of starting a new feature branch-off.
- Expanded Telegram/OpenClaw operator authority in-repo through `scripts/telegram_workspace_ops.py` with explicit actor-id allowlisting and action scoping instead of relying only on a memory note.
- Added elevated Telegram actions for repo-scoped testing and broader `workspace-command` execution so trusted operators can validate, refactor file structure, and run whole-project rollout commands from the BookedAI Telegram surface.
- Synced the new Telegram permission model across `.env` examples, OpenClaw compose env, `README.md`, `project.md`, env strategy, CI/CD runbook, sprint package, and implementation progress.
- Confirmed the current OpenClaw stack already has the official built-in browser Control UI on gateway port `18789`, then mapped the approved public host to `bot.bookedai.au` through Nginx, DNS automation defaults, and production cert/deploy scripts.
- Updated the live OpenClaw runtime env with the same Telegram allowlist/action values and recreated the OpenClaw containers so the bot can inherit the new permission scope on restart.
- Admin tenant catalog now has explicit publish/archive quick actions, client-side publish guardrails for booking-critical fields, and two-step delete confirmation in `frontend/src/features/admin/tenant-management-section.tsx`.
- Backend admin tenant delete is now safer in `backend/api/route_handlers.py`: published services return `422` and must be archived before deletion.
- `frontend/tests/admin-prompt5-preview.spec.ts` now covers tenant workspace navigation and a full tenant governance flow: profile save, member access update, publish guardrail, archive, and delete.
- Verification: `python3 -m py_compile backend/api/route_handlers.py backend/tests/test_admin_service_quality_routes.py` passed; Playwright targeted admin navigation test passed; Playwright tenant workspace flow test passed; repo-wide frontend `tsc` still times out at 30s in this environment; backend unit test execution is blocked locally because Python env is missing `fastapi` and `pytest`.
- Posted a Discord-only operator summary of the actual BookedAI CI/CD flow using `python3 scripts/telegram_workspace_ops.py sync-doc --skip-notion`, grounded in `docs/development/ci-cd-deployment-runbook.md` and the checked-in deploy/release scripts; archive saved under `docs/development/telegram-sync/2026-04-22/003259-bookedai-real-ci-cd-process.md`.
- Re-posted the same CI/CD summary directly to Discord channel `1492396018086576268` (`#team-bookedai-au-pinboard`) in guild `1277887185839718451` via the configured BookedAI bot after verifying channel access.
- Added a `Next phase` execution cadence to `docs/development/sprint-13-16-user-surface-delivery-package.md` with concrete April 24-27, 2026 milestones: scope freeze, AI-before-UI sequencing, daily Discord update, draft/video/final edit, and Monday polish.
- Synchronized the whole-program planning summary across `docs/architecture/current-phase-sprint-execution-plan.md`, `docs/development/implementation-progress.md`, `docs/development/roadmap-sprint-document-register.md`, and new detail note `docs/development/project-update-2026-04-22-program-plan-sync.md`, including the corrected calendar note that `2026-04-24` is Friday.
- Published that program-plan sync to Notion at `https://www.notion.so/BookedAI-program-plan-sync-2026-04-22-34ab21fdf8c781bdbeecc896808ca92c` and mirrored the concise summary to Discord channel `1492396018086576268`; archive saved under `docs/development/telegram-sync/2026-04-22/004205-bookedai-program-plan-sync-2026-04-22.md`.
- Added a real Prisma revenue-ops schema in `prisma/schema.prisma` for `tenants`, `users`, `roles`, `user_roles`, `customers`, `leads`, `services`, `bookings`, and `audit_logs`, with timestamps, soft delete, and `tenant_id` mapping on tenant-scoped tables.
- Added `prisma/seed.ts`, Prisma 7 config seed wiring, and Postgres adapter setup in `lib/db/prisma.ts`, including normalization from the repo's `postgresql+asyncpg://` env format to a Prisma/pg-compatible URL.
- Generated the initial migration artifact at `prisma/migrations/20260422011500_init_multi_tenant_revenue_ops/migration.sql`; `prisma migrate dev` and live seeding were still blocked from this workspace because the configured `supabase-db` host was not reachable here.
- Added `docs/architecture/admin-workspace-blueprint.md` as the detailed BookedAI admin source document, covering vision, principles, module map, role model, permission matrix, entity inventory, REST API baseline, UI page inventory, KPIs, business rules, sprint roadmap, repository structure, and Codex execution prompts.
- Synchronized that admin blueprint into `project.md`, `docs/architecture/admin-enterprise-workspace-requirements.md`, `docs/architecture/internal-admin-app-strategy.md`, `docs/architecture/implementation-phase-roadmap.md`, `docs/development/sprint-13-16-user-surface-delivery-package.md`, and `docs/development/implementation-progress.md`.
- Upgraded the root `Next.js` admin `Customers` module into a richer tenant-scoped workspace with KPI cards, lifecycle and source filters, sort controls, reusable create/update form components, row-level archive actions, and detail tabs for overview, bookings, payments, and notes plus audit.
- Extended `lib/db/admin-repository.ts` with richer customer mock data, payment history, and entity-specific audit records; tightened customer validation so email or phone is required; and verified the change with `node node_modules/typescript/bin/tsc --noEmit` plus `node node_modules/next/dist/bin/next build`.
- Added root `Next.js` admin API routes for Customers at `/api/admin/customers` and `/api/admin/customers/[customerId]`, including paginated list responses, detail responses with bookings/payments/audit logs, Zod validation for query/body/id input, RBAC checks, and permission-aware JSON error handling in `server/admin/api-responses.ts`.
- Extended the root `Next.js` admin `Leads` module into a full pipeline workspace with list and kanban views, owner assignment, follow-up date editing, convert-to-customer and convert-to-booking flows, plus lead create/update/archive server actions.
- Added root `Next.js` admin API routes for Leads at `/api/admin/leads`, `/api/admin/leads/[leadId]`, `/api/admin/leads/[leadId]/convert-to-customer`, and `/api/admin/leads/[leadId]/convert-to-booking`, with Zod validation, RBAC, soft delete, and build verification via `node node_modules/typescript/bin/tsc --noEmit` plus `node node_modules/next/dist/bin/next build`.
- Locked a new documentation rule that `Customers` and `Leads` in the admin workspace must preserve a `Zoho CRM` sync seam, including future retry/manual-review posture, instead of being treated as local-only CRUD modules.
- Simplified the root `Next.js` admin `Services` slice around `name`, `duration`, and `price`, with a simple list UI, shared create/edit form, soft-delete archive action, and API routes at `/api/admin/services` plus `/api/admin/services/[serviceId]`.
- Upgraded the root `Next.js` admin `Bookings` slice with list view, date-grouped calendar view, confirm/cancel/reschedule actions, explicit customer/service linkage, booking create form, time-range validation, and API routes under `/api/admin/bookings` plus dedicated action endpoints for `confirm`, `cancel`, and `reschedule`.
- Turned the root `Next.js` admin dashboard into a real revenue overview in `app/admin/page.tsx`, with KPI cards for revenue/pipeline/bookings/conversion, chart sections for revenue trend plus booking and lead distributions, and operational panels for overdue follow-ups, recent bookings, and audit highlights.
- Added a tenant-scoped `getDashboardSnapshot(...)` read model in `lib/db/admin-repository.ts` so dashboard data now aggregates bookings and leads into chart-friendly series instead of relying on a placeholder summary-only scaffold.
- Verification for the dashboard slice passed with `node node_modules/typescript/bin/tsc --noEmit` and `node node_modules/next/dist/bin/next build`.
- Re-reviewed the whole admin workspace plan against real code and locked a phase-by-phase execution order into project docs: runtime decision first, then production auth/RBAC/audit, then Prisma parity, then tenant/users/settings control plane, then revenue-core module hardening, then payments, then richer dashboard/reporting, and only after that campaigns/workflows/messaging/automation.
- Locked one explicit execution caution into the plan: the current root `Next.js` admin modules still depend partly on `getMockStore()` in `lib/db/admin-repository.ts`, so feature breadth should not outrun the migration to Prisma-backed data truth.
- Started the first root admin `Phase 1` trust-foundation slice: `lib/auth/session.ts` now verifies signed admin sessions with expiry, root admin auth route handlers now exist for `login/logout/me/switch-tenant`, tenant context is session-scoped, and RBAC now covers the broader enterprise lanes needed by later phases.
- Added root admin auth audit events for login, logout, and tenant switching through the repository baseline, and documented the new `ADMIN_BOOTSTRAP_PASSWORD` preference in `README.md` plus `docs/development/env-strategy.md`.
- Verification for this auth/RBAC foundation slice passed with `node node_modules/typescript/bin/tsc --noEmit` and `node node_modules/next/dist/bin/next build`.
- Started the first practical root admin `Phase 2` slice: Prisma schema now covers the richer customer, lead, and payment fields expected by the current UI, `prisma/seed.ts` was updated to match, and `lib/db/admin-repository.ts` now prefers Prisma-backed reads and writes for dashboard, customers, leads, services, bookings, payments, and audit logs when Prisma is enabled.
- Verification for this Prisma-parity slice passed with `npx prisma generate`, `node node_modules/typescript/bin/tsc --noEmit`, and `node node_modules/next/dist/bin/next build`.
- Extended that admin `Phase 2` slice further with Prisma models for permissions, role-permission joins, branches, tenant settings, subscriptions, and invoices; added preparatory repository seams for users, roles, settings, and paginated payments; and generated migration artifact `prisma/migrations/20260422034156_phase2_admin_prisma_parity/migration.sql`.
- Verification for the expanded parity pass again passed with `npx prisma generate`, `node node_modules/typescript/bin/tsc --noEmit`, and `node node_modules/next/dist/bin/next build`.
- Started the first practical root admin `Phase 3` control-plane slice: the admin shell now includes `Team`, `Payments`, and `Settings`, while page, action, repository, and API layers now support user access management, tenant branding/profile edits, and payment ledger writes.
- Verification for this Phase 3 control-plane slice passed with `node node_modules/typescript/bin/tsc --noEmit` and `node node_modules/next/dist/bin/next build`.
- Extended that same Phase 3 lane with `Roles` and `Audit` workspaces, including role-permission editing, paginated audit-log investigation, repository support for permission and audit reads, and matching `/api/admin/roles` plus `/api/admin/audit` contracts.
- Verification for the Phase 3 extension again passed with `node node_modules/typescript/bin/tsc --noEmit` and `node node_modules/next/dist/bin/next build`.
- Started the first practical Phase 4 slice by adding a dedicated lead detail workspace with follow-up timeline and conversion actions, plus shared customer timeline rendering across notes, tags, bookings, payments, and audit events.
- Verification for the Phase 4 opening slice passed with `node node_modules/typescript/bin/tsc --noEmit` and `node node_modules/next/dist/bin/next build`.
- Extended that same Phase 4 lane into bookings with a dedicated booking detail workspace, linked-payment visibility, derived payment status, and booking timeline payloads in the repository and API.
- Verification for the booking-coupling pass again passed with `node node_modules/typescript/bin/tsc --noEmit` and `node node_modules/next/dist/bin/next build`.
- Synced tenant Google auth wording across current source-of-truth surfaces so repo/docs no longer describe the flow as `Google register/login`; updated `frontend/src/features/tenant-auth/TenantAuthWorkspace.tsx`, `frontend/src/apps/tenant/TenantApp.tsx`, `frontend/src/components/landing/data.ts`, `README.md`, `docs/development/implementation-progress.md`, and `docs/architecture/implementation-phase-roadmap.md` toward `Continue with Google` plus gateway/workspace-creation wording.
- Verified the wording pass with `npm --prefix frontend run build`, which passed using the hardened fallback outDir path.
- Found and corrected tenant auth test coverage drift: `backend/tests/test_api_v1_tenant_routes.py` had been mounting `api.v1_routes.router` and patching `api.v1_routes.*`, while live runtime now mounts tenant auth through `api.v1_router` and `api.v1_tenant_handlers`; updated the harness to follow current router composition and retarget tenant auth patches accordingly.
- Verification for that test-harness correction passed with `python3 -m py_compile backend/tests/test_api_v1_tenant_routes.py backend/api/v1_tenant_handlers.py backend/api/v1_router.py`; full pytest execution remained unavailable in this runtime.
- Extended the backend runtime-verification cleanup across the remaining `backend/tests/test_api_v1_*` harnesses: `booking`, `communication`, `integration`, `portal`, `search`, and `search_location_guardrails` now mount `api.v1_router` instead of the legacy shim router from `api.v1_routes`, while keeping helper imports from `api.v1_routes` only where the tests actually need them.
- Verified that router-composition cleanup with `python3 -m py_compile` over the touched `backend/tests/test_api_v1_*.py` files plus `backend/api/v1_router.py`; full pytest execution is still unavailable in this runtime.
- Retargeted the remaining bounded-context test patch sites away from `api.v1_routes.*` to the live handler modules: booking tests now patch `api.v1_booking_handlers.*`, search tests patch `api.v1_search_handlers.*`, communication tests patch `api.v1_communication_handlers.*`, integration tests patch `api.v1_integration_handlers.*`, and portal tests patch `api.v1_tenant_handlers.*`.
- Verified the handler-target cleanup with `python3 -m py_compile` over the touched test files and live handler modules, and confirmed no `api.v1_routes.*` patch targets remain in those bounded-context `backend/tests/test_api_v1_*` files.
- Investigated the `AI Mentor 1-1` shortcut failure on `https://product.bookedai.au` and traced the hosted runtime into fallback mode instead of live intent.
- Confirmed two root causes: `frontend/src/shared/config/publicBookingAssistant.ts` compiled V1/live-read false by default when deploy flags were missing, and `frontend/src/apps/public/AIMentorProApp.tsx` identified the hosted product surface as `embedded_widget` + `plugin_integrated`, which steered backend behavior toward tenant `strict_catalog_only`.
- Patched `frontend/src/shared/config/publicBookingAssistant.ts` so public assistant V1/live-read default on unless explicitly disabled, and split `AIMentorProApp` runtime config so hosted product uses `channel: 'public_web'` with `deploymentMode: 'standalone_app'` while the actual embed widget keeps the plugin-integrated config.
- Validation: a fresh build to `/tmp/bookedai-frontend-dist` emitted an `AIMentorProApp` chunk containing both runtime configs, with hosted mode compiled as `public_web` + `standalone_app` and embed mode still `embedded_widget` + `plugin_integrated`; the currently deployed `product.bookedai.au` bundle still reflects the old embedded/plugin config, so production needs redeploy to pick up the fix.
- Added `docs/development/source-ownership-audit-2026-04-23.md` to convert the repo review into execution decisions: keep `frontend/` as the shipped truth, keep `backend/` as workflow truth, treat root Next.js plus Prisma as the constrained foundation lane, promote the booking-to-CRM-to-revenue golden flow, and reduce new breadth in root `app/` plus legacy `backend/api/v1_routes.py` until parity is clearer.
- Risk/TODO: after deploy, re-test the `AI Mentor 1-1` shortcut against `/booking-assistant/chat` to confirm the UI no longer falls into loading error and that hosted product now prefers live-intent/public-web results instead of tenant-only fallback.
- Traced the hosted AI Mentor shortcut deeper and confirmed the visible loading failure is a backend plain-text `500` from `/api/booking-assistant/chat` for `Book an AI Mentor 1-1 session for startup growth this week.`, while `/api/booking-assistant/chat/stream` is separately returning `500` even for simple probes like `hello` and only survives in the UI because the frontend falls back to the non-streaming endpoint.
- The query-specific `500` path is consistent with AI-event enrichment being triggered by `AI` + `this week`; `BookingAssistantService.chat(...)` previously let `AIEventSearchService.search(...)` exceptions bubble and kill the whole response instead of degrading back to normal service matching.
- Patched `backend/services.py` so AI-event search failures log `booking_assistant_event_search_failed` and degrade to `matched_events = []` rather than crashing the booking assistant flow.
- Added regression coverage in `backend/tests/test_booking_assistant_service.py` to assert `Book an AI Mentor 1-1 session for startup growth this week.` still returns `status == "ok"` with service matches even when `AIEventSearchService.search` raises.
- Patched `frontend/src/components/landing/assistant/BookingAssistantDialog.tsx` so `/booking-assistant/chat` no longer assumes error bodies are JSON; plain-text backend errors now surface as readable API messages instead of JSON parse noise.
- Verification: `python3 -m py_compile backend/services.py backend/tests/test_booking_assistant_service.py` passed. Remaining follow-up is deploy and then re-test live; the SSE `/chat/stream` 500 still needs a separate backend trace if we want streaming restored rather than just fallback-safe behavior.
- Continued the hosted AI Mentor/live-stream debugging and applied an additional likely-root-cause fix for the SSE-only `500`: `backend/api/route_handlers.py` had been serializing stream shortlist items with `s.model_dump()`, which is Pydantic v2-specific and can crash stream responses on runtimes still using Pydantic v1 even while non-stream chat keeps working.
- Added `_model_dump_compat(...)` in `backend/api/route_handlers.py` to prefer `model_dump()` when available and fall back to `.dict()` otherwise, then switched the stream `matched_services_payload` serialization to use that compatibility helper.
- Verification: `python3 -m py_compile backend/api/route_handlers.py` passed.
- Current deploy guidance remains: promote backend + frontend fixes together, then re-test `/booking-assistant/chat`, `/booking-assistant/chat/stream`, and the `AI Mentor 1-1` shortcut on `product.bookedai.au` before deciding whether any deeper stream-provider tracing is still needed.
- Extended that same Phase 4 lane with explicit lead quick actions for note capture and follow-up scheduling, backed by dedicated repository methods and `/api/admin/leads/[leadId]/add-note` plus `/api/admin/leads/[leadId]/schedule-follow-up`.
- Verification for the lead write-seam pass again passed with `node node_modules/typescript/bin/tsc --noEmit` and `node node_modules/next/dist/bin/next build`.
- Zoho CRM moved from documentation-only planning into backend runtime foundation: `backend/config.py` now loads `ZOHO_CRM_*`, `backend/integrations/zoho_crm/adapter.py` now resolves direct-token or refresh-token auth and can read module/field metadata, and `/api/v1/integrations/providers/zoho-crm/connection-test` now exposes a safe smoke-test route for tenant mapping validation.
- Verified the Zoho slice with `python3 -m py_compile backend/config.py backend/integrations/zoho_crm/adapter.py backend/api/v1_integration_handlers.py backend/api/v1_integration_routes.py backend/tests/test_api_v1_integration_routes.py`; full FastAPI route tests could not run in this runtime because `fastapi` is not installed locally.
- Repo-local `.env` still contains no `ZOHO_CRM_*` values, so the Zoho lane is ready for credentials but not yet connected to a live Zoho CRM org.
- Zoho CRM then moved into the first local-first write-back slice: `orchestrate_lead_capture(...)` now creates the sync ledger row, attempts a Zoho `Leads` upsert when credentials exist, and records explicit `pending/manual_review_required/failed/synced` outcomes plus `external_entity_id` on success.
- Verification for the write-back slice passed at compile level with `python3 -m py_compile backend/integrations/zoho_crm/adapter.py backend/service_layer/lifecycle_ops_service.py backend/api/v1_routes.py backend/api/v1_booking_handlers.py backend/tests/test_lifecycle_ops_service.py`; full runtime unit execution is still blocked here because local Python dependencies such as `pydantic` are missing.
- Zoho CRM then moved one step further into recovery and customer-sync foundation: the adapter now also supports `Contacts` upsert, `crm_sync_records` reads now include stored payloads, and `/api/v1/integrations/crm-sync/retry` can now attempt a real replay for stored `lead` or `contact` sync rows instead of only flipping status to `retrying`.
- Verification for that replay slice passed at compile level with `python3 -m py_compile backend/repositories/crm_repository.py backend/integrations/zoho_crm/adapter.py backend/service_layer/lifecycle_ops_service.py backend/service_layer/__init__.py backend/api/v1_integration_handlers.py backend/api/v1_routes.py backend/api/v1_booking_handlers.py backend/tests/test_lifecycle_ops_service.py backend/tests/test_api_v1_integration_routes.py`; full runtime unit execution is still blocked here because local Python dependencies such as `pydantic` are missing.
- Zoho CRM now also has the first root-admin customer bridge: backend exposes `POST /api/v1/integrations/crm-sync/contact`, and root admin customer create/update actions now call that backend contact-sync route after local mutation succeeds.
- Verification for the admin bridge passed at backend compile level with `python3 -m py_compile backend/api/v1_integration_handlers.py backend/api/v1_integration_routes.py backend/tests/test_api_v1_integration_routes.py`; project-wide `tsc --noEmit` did not complete within the 30-second execution window in this environment.
- Ran the approved host-level live deploy via `bash scripts/deploy_live_host.sh`; the deploy completed, reactivated the existing n8n workflow `stwMomWRq2qJoaTy`, and published the current workspace state to the live BookedAI surfaces.
- Post-deploy verification passed at the edge: `http://127.0.0.1:18789/healthz` returned `{"ok":true,"status":"live"}` and `https://bookedai.au` returned `HTTP 200`.
- Direct `docker compose ... ps` verification from the non-elevated shell was still blocked by Docker socket permissions after deploy, so runtime confirmation relied on the successful deploy log plus health and edge checks.
- Investigated the live booking `Failed to fetch` regression and traced the real production failure to backend boot-time import errors inside `backend/api/v1_integration_handlers.py`, not to frontend API config.
- Fixed two bad imports by moving `execute_crm_sync_retry` and `orchestrate_contact_sync` off `api.v1_routes` and onto `service_layer.lifecycle_ops_service`, then redeployed live through `bash scripts/deploy_live_host.sh`.
- After the second redeploy, internal backend and proxy checks both returned `200` for `/api/booking-assistant/catalog`, and public live GETs on `https://bookedai.au/api/booking-assistant/catalog` returned real JSON again; `HEAD` now returns `405 Allow: GET`, confirming the previous `502` edge failure is gone.
- Zoho CRM sync posture is now visible inside the root admin workspace: backend exposes `GET /api/v1/integrations/crm-sync/status`, customer detail shows direct Zoho sync and retry controls, and lead detail now shows CRM sync state with retry posture when a lead-side sync row exists.
- Zoho-connected admin visibility now also reaches list level through compact CRM badges on customer rows, lead rows, and lead kanban cards.
- The first practical Phase 5 revenue-truth slice is now in code: dashboard revenue summary and trend now prefer paid payment records instead of treating booking value as the only revenue signal.
- That same Phase 5 lane now also exposes outstanding-revenue posture: dashboard cards track outstanding revenue, paid bookings, and unpaid bookings, and recent bookings now show payment posture with paid versus outstanding breakdowns.
- Phase 5 now also includes collections operations: dashboard shows receivables aging buckets and a collection-priority queue ranked by overdue age and outstanding value.
- Phase 6 has started with a dedicated `/admin/reports` workspace for paid vs unpaid trend, collections aging, recovered revenue, and collection-priority reporting.
- Phase 6 reporting now also includes repeat revenue, repeat customer rate, retention segments, and source-to-revenue attribution cuts.
- Phase 6 reporting now also includes a source -> lead -> booking -> paid revenue funnel cut in `/admin/reports`.
- Phase 6 reporting now also supports drill-down filters for source, owner, and date range across the shared reports snapshot.
- Activated the live OpenClaw browser surface on `https://bot.bookedai.au`; public probe now returns `HTTP 200`, and the gateway logs show Control UI traffic reaching the service with token auth enforcement instead of origin rejection.
- Restored the live host-mounted OpenClaw config at `/home/dovanlong/.openclaw-bookedai-v3/openclaw.json` so it keeps both the proxy/trusted-origin fixes for `bot.bookedai.au` and the Telegram webhook settings for `https://api.bookedai.au/telegram-webhook`.
- Restarted `openclaw-bookedai-gateway` on the host and verified it returned to `healthy`; post-restart logs show `webhook local listener on http://0.0.0.0:8787/telegram-webhook` and `webhook advertised to telegram on https://api.bookedai.au/telegram-webhook`.
- Verified Telegram registration directly against Telegram Bot API `getWebhookInfo`: webhook URL is now `https://api.bookedai.au/telegram-webhook` with `pending_update_count: 0`.
- Public webhook probing now returns `404` instead of `502`, which confirms the Nginx route is reaching the live webhook service again rather than failing on an unavailable upstream.
- Added a safer OpenClaw host-ops lane through `python3 scripts/telegram_workspace_ops.py host-command --command "..."` instead of granting blanket whole-server shell access.
- The new host-command path uses `sudo -n` automatically, avoids `shell=True`, and only allows the checked-in host-maintenance program list: `apt`, `apt-get`, `docker`, `docker-compose`, `journalctl`, `service`, `systemctl`, `timedatectl`, and `ufw`.
- Synced the new `host_command` action across `.env` examples, OpenClaw compose defaults, `README.md`, `TOOLS.md`, `project.md`, env strategy, implementation progress, CI/CD runbook, and the active sprint package so docs match the safer operator model.
- Approved the live OpenClaw Control UI pairing requests for the currently connecting `bot.bookedai.au` devices: one `MacIntel` webchat client and two `iPhone` webchat clients.
- Verified via the runtime pairing store that those three devices now sit in the paired list with full operator scopes (`operator.admin`, `operator.read`, `operator.write`, `operator.approvals`, `operator.pairing`).
- The only remaining pending pairing request is an internal CLI repair request for the existing linux backend device, not a browser/mobile Control UI client.
- Added Anthropic-native Claude runtime support to the OpenClaw BookedAI stack by forwarding `ANTHROPIC_API_KEY` and `ANTHROPIC_BASE_URL` through `deploy/openclaw/docker-compose.yml` and the local host env file used by the live stack.
- Verified from inside the running gateway container that the configured Anthropic-compatible endpoint responds with `HTTP 200` on `/v1/models` and returns Claude model ids including `claude-sonnet-4-6`, `claude-opus-4-6`, and `claude-haiku-4-5`.
- Recreated the live OpenClaw gateway/CLI containers after the Anthropic env change; startup was slower than usual but recovered to `healthy`, `bot.bookedai.au` returned `HTTP 200` again, and Telegram webhook bootstrap resumed successfully.
- Operator note: repo-local `.env` still has no `ZOHO_CRM_*` credentials, so the Zoho CRM UI/control lane is code-ready but live sync remains disconnected until the Zoho org client, secret, and refresh/access token values are provisioned.
- Zoho CRM activation helper now exists in-repo at `scripts/zoho_crm_connect.py`, and backend config now auto-derives `ZOHO_ACCOUNTS_BASE_URL` from the Zoho API data-center host when the env var is left blank so AU refresh-token exchange no longer falls back to the US accounts domain.
- Repo-local `.env` now has a real `ZOHO_CRM_CLIENT_ID` plus `ZOHO_CRM_CLIENT_SECRET`; live Zoho CRM activation is now blocked only on the consent-code -> refresh-token step, not on missing client credentials.
- Zoho CRM is now live-connected for the AU tenant: repo-local `.env` holds real CRM access + refresh tokens, and `python3 scripts/zoho_crm_connect.py test-connection --module Leads` returned `status: connected` with visible `Leads`, `Contacts`, `Accounts`, and `Deals` module metadata.
- Live Zoho CRM contact sync is now verified end-to-end: after passing `ZOHO_CRM_*` through `docker-compose.prod.yml` and fixing `crm_repository.update_sync_record_status(...)` for asyncpg-safe `last_synced_at` writes, `POST /api/v1/integrations/crm-sync/contact` returned `sync_status: synced` with Zoho external id `120818000000569001`, and the follow-up `crm-sync/status` read returned the persisted synced record.
- Tightened the BookedAI workspace memory policy so dated memory notes must not keep routine shell history, raw command transcripts, git command sequences, or ordinary build/deploy logs; memory should keep decisions, outcomes, blockers, risks, and next steps unless a command reveals a durable failure mode or environment constraint.
- Tenant workspace security hardening now pins tenant-scoped plugin config to the signed tenant slug, restricts `Experience Studio` edits to `tenant_admin`/`operator`, and applies the billing role gate in the frontend save path as well.
- Tenant Vite workspace calls now prefer the signed session tenant slug for authenticated reads and writes, reducing stale URL-context risk after login or redirect.
- Verification for this tenant pass: `py_compile` passed for tenant handlers/service/tests, `frontend/tsc` passed, `npm --prefix frontend run build` passed; targeted pytest could not run because `pytest` is missing in the local Python runtime.
- Tenant workspace now exposes section-level activity summaries for experience, plugin, billing, team, and integrations so operators can see `last updated` and `last edited by` without admin-only audit tooling.
- Tenant billing and team forms now disable restricted fields directly, and integrations show an explicit read-only/access-denied notice instead of only passive select disablement.
- Root admin now has `/admin/tenants` for tenant auth, billing, and CRM retry investigation, plus a safer audited `read_only` support mode instead of silent impersonation.
- Root admin verification passed for this slice with `tsc --noEmit` and `next build`.
- `/admin/tenants` now also deep-links straight into tenant runtime `overview`, `billing`, `team`, and `integrations`, and the next execution plan is locked around unified investigation timeline + regression coverage.
- Zoho CRM role is now documented more explicitly across architecture and execution docs: Zoho is the commercial system of record, while BookedAI remains the AI/system-of-action plus booking, payment, retry, and dashboard truth.
- Booking-intent orchestration now extends the local-first CRM chain beyond `lead/contact`: `backend/api/v1_routes.py` and `backend/api/v1_booking_handlers.py` now also seed Zoho `deal` and follow-up `task` posture through `orchestrate_booking_followup_sync(...)`, and booking-intent responses now include a structured `crm_sync` summary.
- Repo demo coverage now includes `backend/migrations/sql/014_future_swim_crm_linked_flow_sample.sql`, which seeds one linked Future Swim sample across `contacts`, `leads`, `booking_intents`, `payment_intents`, and `crm_sync_records` for end-to-end CRM flow inspection.
- Verification for this pass: `python3 -m py_compile backend/integrations/zoho_crm/adapter.py backend/service_layer/lifecycle_ops_service.py backend/api/v1_routes.py backend/api/v1_booking_handlers.py backend/tests/test_lifecycle_ops_service.py` passed; local unittest execution still cannot import the test module because `pydantic` is missing in the default shell runtime.
- Added a dedicated execution map at `docs/architecture/bookedai-zoho-crm-integration-map.md` that locks the named CRM event order as `booking created`, `lead qualified`, `call scheduled`, `email sent`, then inbound `deal won/lost` dashboard feedback.
- Synced that event map into `project.md`, the Zoho blueprint, roadmap, implementation progress, and the sprint package so CRM execution now starts from one shared event-level backlog instead of ad-hoc discussion.
- Started the next event in that map: backend now exposes `POST /api/v1/integrations/crm-sync/lead-qualification`, admin lead update now best-effort syncs `qualified` leads into Zoho `lead/contact/deal` posture, and the runtime therefore now covers both `booking created` and `lead qualified` outbound CRM lanes.
- Verification for the qualification slice passed with `python3 -m py_compile backend/service_layer/lifecycle_ops_service.py backend/api/v1_integration_handlers.py backend/api/v1_integration_routes.py backend/tests/test_lifecycle_ops_service.py` and `node node_modules/typescript/bin/tsc --noEmit`; local unittest execution still fails to import because `pydantic` is missing in the default shell runtime.
- Started the next CRM event lane after that: backend now exposes `POST /api/v1/integrations/crm-sync/call-scheduled`, admin `scheduleLeadFollowUp` now best-effort syncs a Zoho task for scheduled calls, and the runtime therefore now covers `booking created`, `lead qualified`, and `call scheduled` from the CRM event map.
- Verification for the call-scheduled slice passed with `python3 -m py_compile backend/service_layer/lifecycle_ops_service.py backend/api/v1_integration_handlers.py backend/api/v1_integration_routes.py backend/tests/test_lifecycle_ops_service.py` and `node node_modules/typescript/bin/tsc --noEmit`; local unittest execution still fails to import because `pydantic` is missing in the default shell runtime.
- Started the next CRM event lane after that: lifecycle email sending now best-effort mirrors outreach into a Zoho task through `orchestrate_email_sent_sync(...)`, and the runtime therefore now covers `booking created`, `lead qualified`, `call scheduled`, and `email sent` from the CRM event map.
- Verification for the email-sent slice passed with `python3 -m py_compile backend/service_layer/lifecycle_ops_service.py backend/api/v1_routes.py backend/api/v1_communication_handlers.py backend/tests/test_lifecycle_ops_service.py backend/tests/test_api_v1_communication_routes.py` and `node node_modules/typescript/bin/tsc --noEmit`; local route-level unittest execution still fails to import because `fastapi` is missing in the default shell runtime.
- Started the first inbound CRM intelligence slice: backend now exposes `POST /api/v1/integrations/crm-feedback/deal-outcome` plus `GET /api/v1/integrations/crm-feedback/deal-outcome-summary`, and the root admin dashboard/reporting surfaces won/lost counts plus recent Zoho deal outcomes.
- Extended `backend/migrations/sql/014_future_swim_crm_linked_flow_sample.sql` so the Future Swim demo chain now includes one seeded `deal_feedback` row, making the BookedAI <-> Zoho loop demonstrable end-to-end from booking capture through commercial close feedback.
- Verification for the inbound deal-outcome slice passed with `python3 -m py_compile backend/repositories/crm_repository.py backend/api/v1_integration_handlers.py backend/api/v1_integration_routes.py backend/tests/test_api_v1_integration_routes.py` and `node node_modules/typescript/bin/tsc --noEmit`; local route-level unittest execution remains blocked because `fastapi` is missing in the default shell runtime.
- Hardened that inbound Zoho feedback slice further: the summary contract now includes `win_rate`, `owner_performance`, `lost_reasons`, `stage_breakdown`, and task-completion counts, and both root admin dashboard plus reports now surface those richer CRM intelligence cuts.
- Extended the Future Swim sample chain again so demo data now includes both `won` and `lost` `deal_feedback` rows, plus seeded `task_completed` and `stage_changed_at` signals for the inbound CRM read model.
- Added the first runtime Zoho polling bridge for inbound CRM intelligence: backend now exposes `POST /api/v1/integrations/crm-feedback/zoho-deals/poll`, which fetches known Zoho `Deals` by external id, ingests terminal `Closed Won` / `Closed Lost` posture into `deal_feedback`, and skips non-terminal stages for now.
- Added the first Zoho webhook trigger for inbound CRM intelligence: backend now exposes `POST /api/v1/integrations/crm-feedback/zoho-webhook`, supports Zoho Notifications-style payloads with `module`, `ids`, `operation`, `channel_id`, and `token`, logs raw events to `webhook_events`, and reuses the same terminal deal-feedback ingestion path.
- Added the Zoho Notifications registration helper too: backend now exposes `POST /api/v1/integrations/crm-feedback/zoho-webhook/register`, which subscribes a `Deals` watch channel in Zoho and returns the `channel_id/token` pair that should be persisted into env if strict webhook verification is desired.
- Added the rest of the notification lifecycle controls too: backend now supports `GET /api/v1/integrations/crm-feedback/zoho-webhook` plus `POST /renew` and `POST /disable`, so operators can inspect, extend, and unsubscribe Zoho notification channels without leaving the BookedAI runtime.
- Added a tracked auto-renew path too: backend now exposes `POST /api/v1/integrations/crm-feedback/zoho-webhook/auto-renew`, which checks current `channel_expiry`, renews the Zoho channel if it is inside the configured threshold, and records the maintenance run through `job_runs`.
- Added the low-overhead automation wrapper for that path: `scripts/run_zoho_crm_webhook_auto_renew.py`, `scripts/install_zoho_crm_webhook_auto_renew_cron.sh`, and a Telegram/OpenClaw `maintenance zoho-crm-webhook-auto-renew` entrypoint now let production renew the Zoho channel on a sparse 6-hour cadence without introducing a resident background loop.
- Root admin `/admin/tenants` now includes a unified investigation timeline that merges tenant auth, invite backlog, billing posture, CRM retry backlog, integration attention, and support-session audit events into one read-only support-case feed.
- Updated the current phase execution plan so the tenant-support lane now moves from timeline work into support-mode banner expansion, cross-surface return-link polish, and regression coverage.
- Root admin tenant-support lane now also has regression coverage: `node:test` assertions cover support-mode start/end contracts and investigation timeline rendering without needing a heavier new harness.
- Root admin support-mode context now follows operators into the main admin workspaces through shared inline banners with return-to-investigation links and tenant runtime section links; the next tenant-support step is now cross-surface return-link polish.
- Cross-surface tenant-support polish is now in place: admin links into tenant runtime carry support context, and tenant runtime surfaces return links back to both `/admin/tenants` and the originating admin workspace.
- Read-only tenant support is now enforced more strictly in the root admin lane: non-view permissions are blocked while support mode is active, and the main mutation forms now render disabled instead of looking writable.
- Phase 2 intelligent matching moved into a richer engine contract: `backend/core/contracts/matching.py` plus `backend/domain/matching/service.py` now shape `/api/v1/matching/search` with normalized `booking_fit`, `stage_counts`, and fuller shortlist reasoning instead of leaving response shaping mostly route-local.
- Frontend v1 matching contracts now normalize the same richer response in `frontend/src/shared/contracts/*` and `frontend/src/shared/api/v1.ts`, keeping backend/frontend search payloads aligned for later shortlist and diagnostics UI work.
- Verification: `.venv-backend/bin/python -m unittest backend.tests.test_matching_domain_service backend.tests.test_prompt9_matching_service backend.tests.test_api_v1_contract` passed; `py_compile` passed for the touched backend files; full `backend.tests.test_api_v1_search_routes` still has unrelated split-route drift in this repo state; full frontend `tsc` still timed out at 30s in this environment.
- Public booking assistant flow now surfaces a clearer enterprise journey: live `matching services` search state, explicit search-to-preview-to-booking workflow visibility, and a fuller confirmation path across email, calendar, payment, CRM, thank-you, and follow-up.
- Prompt 5 public assistant now preserves additive `crm_sync` detail from the v1 booking-intent response into the frontend confirmation state, so CRM linkage posture is visible without changing the backend contract.
- Booking confirmation now also generates reusable customer communication drafts for email, SMS, and WhatsApp when the operator or customer provides matching contact details.
- Public booking submit now also executes best-effort v1 post-booking automation for payment intent, lifecycle email, SMS, and WhatsApp, and the confirmation state shows those real automation results plus operator-attention warnings.
- The same public assistant confirmation state now shows a delivery timeline across booking, payment, email, SMS, WhatsApp, and CRM, and payment orchestration derives its option from trust or booking-path posture instead of one hardcoded payment mode.
- Homepage runtime now follows that same enterprise result posture more closely: `HomepageSearchExperience.tsx` preserves `crm_sync`, runs best-effort post-booking automation, and renders communication drafts plus a delivery timeline after booking submit instead of a thinner success state.
- Homepage Playwright coverage was repaired and revalidated after the homepage pass: desktop and mobile responsive smoke both pass, and the live-read homepage booking-submit assertion now passes after rebuilding with the updated runtime.
- Added `backend/migrations/sql/016_cross_industry_full_flow_test_pack.sql` plus `docs/development/bookedai-cross-industry-full-flow-test-pack.md` to seed 10 synthetic cross-industry bookings for QA, demo, and operator full-flow rehearsal.
- While applying the test pack for real, found and fixed a migration blocker in `backend/migrations/sql/012_demo_revenue_events_seed.sql`, which had drifted behind the current `payment_intents` schema.
- Applied migrations through `016` against the live repo database and verified the new pack with `scripts/verify_cross_industry_full_flow_test_pack.sh`: all 10 `testpack-*` scenarios now exist with complete payment, email, CRM, outbox, and audit trace coverage.
- Docs were synced in the same pass through `project.md`, `docs/development/implementation-progress.md`, and `docs/development/sprint-13-16-user-surface-delivery-package.md`.
- Verification remains partial in this workspace: frontend `tsc` and `npm --prefix frontend run build` were re-run with timeout guards, but both exceeded the local time window without emitting compile errors before timing out.
- Root admin `Phase 7` has now started with a real `/admin/campaigns` workspace instead of leaving growth modules as docs-only backlog.
- Campaigns now have Prisma schema, seed coverage, a migration artifact, repository CRUD, validation, API routes, sidebar navigation, and create/edit/archive UI, tied to `sourceKey` and UTM data so attribution can reuse the existing reporting lane.
- Targeted verification passed with `npx prisma generate`, `npx prisma validate`, and direct `tsx` module-import checks for the new Campaigns slice.
- Project-wide root `tsc --noEmit` and `next build` were re-run for this pass but timed out in this environment before returning either a clean success or a concrete compiler error.
- Tenant auth is now moving under an explicit email-first rule: email is the primary identifier, email-code sign-in/create/claim routes now exist, and Google remains on the same tenant login form as the fast continuation path.
- Added tenant verification-code persistence through `backend/migrations/sql/015_tenant_email_login_codes.sql` and the matching `TenantEmailLoginCode` runtime model so tenant email auth has a repo-backed state seam instead of a frontend-only UX promise.
- Replaced the main Vite tenant auth card with a new email-code-first component, removing username fields from the primary create/claim UX while keeping legacy password compatibility in the backend.
- Docs were synced in the same pass through `project.md`, `README.md`, `docs/architecture/auth-rbac-multi-tenant-security-strategy.md`, `docs/development/tenant-billing-auth-execution-package.md`, `docs/development/implementation-progress.md`, and `docs/development/sprint-13-16-user-surface-delivery-package.md`.
- Verification for this auth pass: `py_compile` passed for touched backend files, `npm --prefix frontend run build` passed, and `npm --prefix frontend run test:playwright -- --grep "tenant gateway"` passed; local Python unittest execution is still blocked because `fastapi` is missing in the default shell runtime.
- Raised OpenClaw host authority for this VPS by giving `openclaw-cli` root/privileged runtime access plus `/hostfs` and `/var/run/docker.sock`, while keeping `openclaw-gateway` on the standard runtime profile to avoid widening browser and webhook surface privileges.
- Recreated the live OpenClaw stack after the privilege split and confirmed the CLI container can see the host filesystem and Docker socket as `root`.
- Confirmed `ffmpeg` was already installed on the host from apt, so no package upgrade was needed for media tooling.
- Fixed a false `openclaw-bookedai-gateway` unhealthy state by extending the compose healthcheck startup grace to 12 minutes, matching the gateway's slow cold-start path while plugins and channels initialize.
- Hardened Zoho webhook live ops: `backend/db.py` now serializes ORM bootstrap with a PostgreSQL advisory lock, and `backend/integrations/zoho_crm/adapter.py` now prefers refresh-token exchange over stale direct access tokens.
- Host auto-renew maintenance is now wired for local Nginx instead of Cloudflare through `ZOHO_CRM_NOTIFICATION_AUTO_RENEW_API_URL=https://127.0.0.1`, `ZOHO_CRM_NOTIFICATION_AUTO_RENEW_HOST_HEADER=api.bookedai.au`, and `ZOHO_CRM_NOTIFICATION_AUTO_RENEW_SKIP_TLS_VERIFY=true`.
- Reinstalled `/etc/cron.d/bookedai-zoho-crm-webhook-renew`; the runner now reaches the live maintenance route on-host and fails only on the expected missing `ZOHO_CRM_NOTIFICATION_CHANNEL_ID` / `ZOHO_CRM_NOTIFICATION_TOKEN` precondition.
- Verified the remaining blocker is external OAuth scope, not runtime wiring: Zoho `actions/watch` registration currently returns `OAUTH_SCOPE_MISMATCH`, so the next refresh token must include `ZohoCRM.notifications.ALL`.
- Completed that Zoho re-consent with `ZohoCRM.notifications.ALL`; repo `.env` now has the new CRM refresh token and a live notification channel on `1000000068001` with token `deals.all.notif`.
- Fixed the final runtime gap by passing `ZOHO_CRM_NOTIFICATION_TOKEN` and `ZOHO_CRM_NOTIFICATION_CHANNEL_ID` through `docker-compose.prod.yml` into `backend` and `beta-backend`.
- Verified webhook operations end-to-end in production: channel inspect passes through `api.bookedai.au`, and auto-renew completed successfully, extending expiry from `2026-04-22T11:59:41+00:00` to `2026-04-29T10:01:54+00:00`.
- Performed a gateway-only fast recovery pass for a transient OpenClaw `disconnected (1006)` incident by recreating `openclaw-bookedai-gateway` with `ANTHROPIC_API_KEY` and `ANTHROPIC_BASE_URL` cleared at runtime, keeping the privileged `openclaw-cli` service untouched while rechecking local health and boot behavior.
- Root admin `Phase 2` parity moved one step further: `lib/db/admin-repository.ts` now exposes `listBranches(...)` and `getTenantBillingOverview(...)` over Prisma-backed `branches`, `subscriptions`, and `invoices`.
- `/admin/settings` now surfaces branch footprint plus subscription/invoice posture beside branding controls instead of stopping at profile-only state.
- Docs synced for this slice in `project.md`, `docs/architecture/admin-workspace-blueprint.md`, and `docs/development/implementation-progress.md`.
- Verification: root `tsc --noEmit` passed; follow-up `next build` rerun hit an already-running build process in this workspace instead of reporting a compile failure.
- Root admin `Phase 2` parity now also covers write seams for those tenant-control entities: branch create/update/archive/reactivate plus billing-baseline update all now run through `lib/db/admin-repository.ts` and `app/admin/settings/actions.ts`.
- `/admin/settings` now exposes real branch management controls and a billing-baseline edit form instead of stopping at read-only branch/subscription/invoice summary cards.
- Admin settings API now returns `branches` and `billing` alongside profile/settings so UI and API read paths stay aligned on the same Phase 2 parity data.
- Verification for this follow-up remains partial: no direct TS error surfaced, but root `tsc` and `next build` still behave inconsistently in this shell and did not produce a clean final success line before the processes disappeared or hung.
- Root admin `Phase 2` settings parity now also has explicit API mutations: `GET/POST /api/admin/settings/branches`, `GET/PATCH /api/admin/settings/branches/:branchId`, and `GET/PATCH /api/admin/settings/billing`.
- Import-level verification for the new API surface passed with `settings-api-phase2-import-ok`; repo-wide root `tsc` still remains inconclusive in this shell.
- Root admin `Phase 3` settings lane now also covers operational guides and billing-gateway controls through `tenant_workspace` and `billing_gateway` settings data.
- `/admin/settings` now lets admin edit workspace guides plus `merchantModeOverride`, `stripeCustomerId`, and `stripeCustomerEmail`, and root admin API now exposes `/api/admin/settings/guides` plus `/api/admin/settings/gateway`.
- Fixed a mock-settings merge bug so branding saves no longer overwrite unrelated settings keys when Prisma is not active.
- Import-level verification for this Phase 3 pass succeeded with `settings-phase3-import-ok`; repo-wide `tsc` remains inconclusive in the current shell.
- Root admin `Phase 3` settings lane now also covers plugin runtime posture through `partner_plugin_interface`, including embed/runtime URLs, widget ids, partner messaging, support contacts, and logo URL.
- Admin settings API now exposes `/api/admin/settings/plugin`, and the aggregate settings read model now includes `pluginRuntime`.
- Verification for this plugin slice passed with `settings-plugin-phase3-import-ok` and root `tsc --noEmit`.
- Root admin `Phase 3` settings lane now also mirrors integration operator posture from the backend runtime, including provider status, CRM retry backlog, attention triage, and reconciliation slices directly inside `/admin/settings`.
- Admin settings API now also exposes `/api/admin/settings/integrations`, and the aggregate settings read model now includes `integrations`; provider posture writes remain tenant-owned and link back to the tenant integrations workspace instead of bypassing tenant auth boundaries.
- Verification for this integration settings slice passed with root `node node_modules/typescript/bin/tsc --noEmit`; a standalone `tsx` import probe was not treated as signal because `server-only` is not resolvable outside Next runtime in this shell.
- That same integration settings slice now also surfaces Zoho CRM credential posture, outbox backlog counts, and recent integration runtime activity through the same aggregated admin snapshot instead of stopping at provider or CRM retry blocks only.
- The integration settings snapshot now also derives a provider credential-safe overview so admin can compare enabled posture and configured-field coverage across providers from one grid without exposing secrets.
- Re-synchronized the whole-program control docs so `project.md`, the master execution index, current phase plan, implementation roadmap, sprint register, Sprint 13-16 package, and implementation progress now all reflect the same `2026-04-22` baseline.
- Pulled the newest code-backed lanes into that planning baseline instead of leaving them only in point updates: tenant `email-first` auth, matching Phase 2 `booking_fit/stage_counts`, the cross-industry full-flow QA pack, and the root admin `Campaigns` foundation.
- Registered the new request-facing delivery docs in the active plan stack: chatbot landing implementation plan, sample video brief, storyboard or script materials, and investor pitch deck.
- Locked Sprint 15-16 planning around both product hardening and proof-pack delivery, with the same April 24-27, 2026 cadence now reflected across the synced roadmap and sprint docs.
- Homepage authoritative booking submit was hardened again so the success state appears as soon as the v1 booking write succeeds, while payment/email/SMS/WhatsApp automation continues asynchronously and updates the timeline after the booking reference is already visible.
- Homepage booking sidebar now exposes the fuller enterprise journey rail from search through aftercare, closing parity further with `BookingAssistantDialog` instead of leaving homepage at a thinner confirmation-only posture.
- Verification for the homepage parity follow-up passed with `PLAYWRIGHT_SKIP_BUILD=1 timeout 120s bash frontend/scripts/run_playwright_suite.sh legacy tests/public-homepage-responsive.spec.ts` and `PLAYWRIGHT_SKIP_BUILD=1 timeout 180s bash frontend/scripts/run_playwright_suite.sh live-read tests/public-booking-assistant-live-read.spec.ts --grep "booking submit uses v1 booking intent as the authoritative write when live-read is enabled"`; standalone `tsc --noEmit` still timed out in this workspace without emitting a concrete compiler error.
- Installed host `acl`, created Linux user `openclaw`, granted traverse ACL on `/home/dovanlong`, and granted recursive plus default write ACL on `/home/dovanlong/BookedAI`, which is the host bind source for `/workspace/bookedai.au`.
- Verified `openclaw` can now create, edit, rename, and delete files in the BookedAI repo without changing repo ownership; synced the runtime note into `README.md`, `deploy/openclaw/README.md`, and `docs/development/implementation-progress.md`.
- Re-applied the `openclaw` ACL recursively across the full BookedAI repo tree to catch previously restricted subdirectories and re-verified project-wide write access as `openclaw`.
- Explicitly verified write access on `frontend/src/components/landing/sections/HomepageExecutiveBoardSection.tsx` and `frontend/src/components/landing/sections/HomepageOverviewSection.tsx`.
- Identified the OpenClaw memory-flush `EACCES` root cause: the gateway container writes the bind-mounted repo as `uid 1000` (`node` in-container, `ubuntu` on this host), not as the host `openclaw` user.
- Granted the same recursive plus default ACL write access on `/home/dovanlong/BookedAI` to host `uid 1000`, then verified from inside `openclaw-bookedai-gateway` that `/workspace/bookedai.au/memory/2026-04-22.md` is writable again.
- Investigated the webchat live-deploy blocker and confirmed it was a tool-policy issue, not a build/deploy code failure: live OpenClaw state allowed elevated exec from `telegram` only, so provider `webchat` was denied by `tools.elevated.allowFrom`.
- Fixed the webchat elevated path by adding `webchat=["*"]` in `/home/dovanlong/.openclaw-bookedai-v3/openclaw.json`, correcting `deploy/openclaw/docker-compose.yml` so `openclaw-cli` runs `openclaw node run --host 127.0.0.1 --port 18789 --display-name bookedai-host-cli`, and recreating the OpenClaw stack; `openclaw-bookedai-cli` now stays up instead of exiting after help output.
