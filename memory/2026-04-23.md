- pinned the production Cloudflare DNS automation path to support the new origin IP `34.40.192.68` through `CLOUDFLARE_AUTO_DNS_IPV4` or a manual CLI arg in `scripts/update_cloudflare_dns_records.sh`
- fixed a real boot-sync blocker in `scripts/update_cloudflare_dns_records.sh` by checking `configure_cloudflare_dns.sh` with `-f` instead of `-x`, then ran the Cloudflare DNS sync successfully against the configured production record set at `34.40.192.68`
- switched Cloudflare DNS automation back to always auto-detect mode for ongoing operations by adding `deploy/systemd/bookedai-cloudflare-dns.timer` and updating the installer to enable both boot-time and periodic reconciliation
- widened the Cloudflare auto-sync host list to cover the remaining public BookedAI hosts and upgraded IP discovery to prefer cloud metadata endpoints before public echo services
- fixed the `supabase-kong` startup path by changing the compose entrypoint to `/bin/bash /home/kong/kong-entrypoint.sh`, removing the mounted-script execute-bit dependency that had been leaving `supabase.bookedai.au` behind a `502`
- recreated `supabase-kong`, confirmed `supabase.bookedai.au` returned `401` again, then rechecked the stack until `bookedai-cloudflare-dns.timer` was active, `scripts/healthcheck_stack.sh` passed, and `bot.bookedai.au` recovered to `200` after the OpenClaw gateway finished warming up
- updated `scripts/verify_gcp_ingress_for_bookedai.sh` default `EXPECTED_IP` to `34.40.192.68` so GCP ingress checks match the current production target
- annotated `deploy/nginx/bookedai.au.conf` with the current production origin IP and synced the operator guidance in `README.md` plus `docs/users/administrator-guide.md`
- fixed the live deploy blocker where `supabase-kong` could not start because `supabase/volumes/api/kong-entrypoint.sh` had no execute bit; the file now ships as executable
- reran `bash scripts/deploy_live_host.sh` successfully, reactivated the existing n8n workflow `stwMomWRq2qJoaTy`, and confirmed the main public surfaces and backend health endpoint were responding again
- hardened tenant workspace HTML handling by replacing the backend’s script-only filter with an allowlist sanitizer and by sanitizing both legacy admin preview and tenant onboarding preview before `dangerouslySetInnerHTML`
- reduced legacy admin session persistence risk by migrating browser-stored admin session data from `localStorage` into `sessionStorage`, while still clearing and reading legacy keys for compatibility
- removed admin or tenant session-signing fallback to `ADMIN_API_TOKEN` and `ADMIN_PASSWORD`, and tightened bootstrap admin auth so `ADMIN_BOOTSTRAP_PASSWORD` plus an eligible admin identity is now required for the root Next.js admin login lane
- replaced the root Next.js admin lane’s default shared-password login path with per-user email verification codes: `/admin-login` now requests and verifies one-time codes through new admin auth routes, while password bootstrap auth is now break-glass only behind `ADMIN_ENABLE_BOOTSTRAP_LOGIN=1`
- verified the new root admin flow end-to-end in local Next dev with `BOOKEDAI_ENABLE_PRISMA=0`: request-code returned a debug code in development, verify-code issued `bookedai_admin_session`, and `/api/admin/auth/me` accepted that cookie and returned the signed operator session
- attempted `npx prisma migrate deploy` for `admin_email_login_codes`, but the current shell cannot resolve the configured database host `supabase-db`; live migration still needs a runtime where that hostname is reachable
- confirmed the deeper rollout blocker is schema reality, not only hostname reachability: the live `bookedai` database already contains legacy `tenants`, `leads`, `subscriptions`, and `audit_logs` tables but does not carry the root Prisma `users` chain, so the checked-in Prisma admin migrations cannot be applied there directly
- added a root-admin compatibility path for that reality: when Prisma is not enabled, admin email-code login now resolves identities from `tenant_user_memberships`, stores codes in legacy `tenant_email_login_codes` with `auth_intent='admin-sign-in'`, and maps `legacy:<tenant_id>:<email>` sessions back to real accessible tenants
- verified that compatibility path end-to-end against the live Postgres container by requesting an admin code for `future-swim`, seeing the row persist in `tenant_email_login_codes`, verifying the code, receiving `bookedai_admin_session`, and confirming `/api/admin/auth/me` returns the real `Future Swim` tenant
- synced the security posture changes into `README.md`, `project.md`, `docs/architecture/auth-rbac-multi-tenant-security-strategy.md`, `docs/development/env-strategy.md`, and `docs/development/implementation-progress.md`
- widened the OpenClaw operator lane from allowlisted host maintenance to true full-server `host/elevated` execution by adding `host-shell` to `scripts/telegram_workspace_ops.py`
- updated OpenClaw permission defaults and docs so trusted actors can use `host_shell` or `full_project` for unrestricted server commands outside `/workspace/bookedai.au`
- verified the same `host_shell` lane also works through the Telegram actor path by running the wrapper in-container with `BOOKEDAI_TELEGRAM_USER_ID=8426853622`; permissions include `host_shell` and `host-shell --cwd / --command 'pwd'` returns `/`
- removed the live Telegram `pairing required` blocker by switching OpenClaw Telegram DM policy to `allowlist`, adding `channels.telegram.allowFrom=[\"8426853622\"]`, elevating the `bookedai-host-cli` device token to full operator scopes, and clearing the stuck pending approval request in OpenClaw runtime state
- restarted `openclaw-bookedai-gateway` and `openclaw-bookedai-cli`, then re-verified trusted Telegram host control with `host-shell` and a host-level `apt-get update` smoke run
- fixed an OpenClaw reliability gap by adding `restart: unless-stopped` to `openclaw-cli`, so the node-host process recovers automatically when it loses the initial gateway connect race during boot
- corrected the last host-control mismatch by making `scripts/telegram_workspace_ops.py` use `nsenter --target 1` when running inside the privileged CLI container; trusted `host-shell` and `host-command` now resolve `pip`, `venv`, and `apt` against the real host instead of the container filesystem
- widened the shared frontend admin shell into the first requirement-aligned enterprise IA package with `Overview`, `Tenants`, `Tenant Workspace`, `Catalog`, `Billing Support`, `Integrations`, `Reliability`, `Audit & Activity`, and `Platform Settings`
- kept admin hash routing backward-compatible by mapping older `#operations` links into the new `#overview` workspace while adding dedicated route homes for billing/support, integrations, audit chronology, and platform review
- split tenant selection from mutation in the shared admin runtime so operators review tenant scope in a directory lane before opening the mutable tenant workspace
- synced the admin IA change into `project.md`, `docs/architecture/admin-enterprise-workspace-requirements.md`, `docs/development/implementation-progress.md`, and `docs/development/sprint-13-16-user-surface-delivery-package.md`, then published the summary to Notion and Discord through `telegram_workspace_ops.py sync-doc`
- created the large-upgrade execution branch `codex/major-upgrade-plan-execution` from the current synced server baseline and committed checkpoint `7dc8a2b` (`feat: checkpoint admin auth hardening and enterprise ia`)
- pushed `codex/major-upgrade-plan-execution` to GitHub so the next implementation wave can proceed on a clean tracked branch without disturbing the earlier sync branch
- kept the pre-branch local backup in `stash@{0}` as `pre-merge-origin-main-2026-04-23-upgrade-plan`; only `.clawhub/` and `skills/` remain untracked local-only on the server working tree
- restored the missing repo files `frontend/src/shared/sanitizeTenantHtml.ts` and `lib/db/legacy-admin-auth.ts` from the pre-branch stash after build verification exposed them as dropped utilities
- committed and pushed the restored admin-auth and tenant-HTML files plus the root admin email-code login flow on `codex/major-upgrade-plan-execution` as `b5842a2` and `579f7dd`
- verified the new wave with Python compile checks, successful `node node_modules/next/dist/bin/next build`, and successful `npm --prefix frontend run build`; root `npm run build` still needs the `next` binary execute-bit workaround on this host
- redeployed the full live stack with `bash scripts/deploy_live_host.sh`, confirmed `bookedai.au`, `api.bookedai.au`, `tenant.bookedai.au`, `admin.bookedai.au`, and `bot.bookedai.au` were healthy again, and reactivated the existing n8n workflow `stwMomWRq2qJoaTy`
- re-synced the execution docs so `project.md`, the current phase plan, and the Sprint 13-16 package now all agree that `Campaigns` is already the opened growth-lane foundation and the next implementation slice is `Messaging`
- locked the active operator-facing admin productization lane to the shared `frontend/src` admin shell, while keeping the root `Next.js` admin tree as the parallel auth and repository foundation lane
- removed the remaining project-doc contradiction that still described `ADMIN_API_TOKEN` and `ADMIN_PASSWORD` as accepted session-signing fallback posture
- started the first real admin `Messaging` slice: backend now exposes unified `/api/admin/messaging` read and action routes over `email_messages`, `outbox_events`, and `crm_sync_records`, and the shared frontend admin shell now includes a first-class `Messaging` workspace with filters, detail, retry, and manual-follow-up posture
- re-synced `project.md`, the current phase plan, the sprint package, and implementation progress so repo truth now treats `Messaging` as implemented foundation and `Workflows` as the next growth-lane slice
- deepened the shared `frontend/src` admin enterprise IA by turning `Billing Support`, `Integrations`, and `Audit & Activity` from guidance-only route homes into read-model-backed workspaces using the existing admin event feed and support queue
- added `workspace-read-models.ts` plus new billing, integration, and audit summary sections so the broadened admin shell now gives operators actionable summaries before they open mutation-heavy panels
- verified the new admin workspace slice with `esbuild` on `AdminPage.tsx` and a targeted Playwright run `bash frontend/scripts/run_playwright_suite.sh legacy tests/admin-workspace-upgrade.spec.ts`
- converted the enterprise/startup repo review into an execution document at `docs/development/source-ownership-audit-2026-04-23.md`, recommending `frontend/` as the shipped truth, `backend/` as workflow truth, root Next.js plus Prisma as the constrained foundation lane, and the booking-to-CRM-to-revenue path as the next golden flow to harden
- added `docs/development/admin-surface-parity-tracker-2026-04-23.md`, mapping the shipped `frontend/` admin lane against root `app/admin/*` and concluding the project should keep the frontend shell for operator workflow framing while using root Next.js for auth, RBAC, repository truth, and module hardening until selective promotion is ready
- added `docs/development/admin-module-promotion-checklists-2026-04-23.md`, converting that parity tracker into concrete promotion gates for `Settings`, `Leads`, and `Customers`, with the recommended rollout order locked to `Settings` first, then `Leads`, then `Customers`
- added `docs/development/settings-promotion-execution-plan-2026-04-23.md`, locking the first real module-promotion plan to `Settings` via controlled handoff: keep the shipped `frontend/` shell as control tower, make root Next.js `Settings` the mutation truth, treat auth and tenant handoff as the first implementation task, and block release if fallback-only settings data paths remain in operator-target environments
- added `docs/development/settings-ownership-mapping-2026-04-23.md` and `docs/development/settings-auth-tenant-handoff-contract-2026-04-23.md`, freezing which settings concerns stay in the shipped shell versus root Settings and defining the required promotion guarantees around trusted session, tenant alignment, support mode, and return path
- added `docs/development/settings-promotion-technical-task-breakdown-2026-04-23.md`, turning the Settings promotion plan into implementation-ready engineering work across shipped shell summary and CTA changes, root-side auth and tenant handoff validation, route and param helpers, and smoke plus release verification; the next coding slice now starts with the handoff helpers and summary section
- implemented the first Settings promotion slice: shipped admin now has a `Workspace settings` summary plus `Open workspace settings` CTA, handoff URL building lives in `frontend/src/features/admin/settings-handoff.ts`, root-side handoff parsing and sanitization lives in `server/admin/settings-handoff.ts`, and `app/admin/settings/page.tsx` now shows handoff context, exposes a return path, and forces read-only posture on tenant mismatch
- pinned the production Cloudflare DNS automation path to support the new origin IP `34.40.192.68` through `CLOUDFLARE_AUTO_DNS_IPV4` or a manual CLI arg in `scripts/update_cloudflare_dns_records.sh`
- fixed a real boot-sync blocker in `scripts/update_cloudflare_dns_records.sh` by checking `configure_cloudflare_dns.sh` with `-f` instead of `-x`, then ran the Cloudflare DNS sync successfully against the configured production record set at `34.40.192.68`
- switched Cloudflare DNS automation back to always auto-detect mode for ongoing operations by adding `deploy/systemd/bookedai-cloudflare-dns.timer` and updating the installer to enable both boot-time and periodic reconciliation
- widened the Cloudflare auto-sync host list to cover the remaining public BookedAI hosts and upgraded IP discovery to prefer cloud metadata endpoints before public echo services
- fixed the `supabase-kong` startup path by changing the compose entrypoint to `/bin/bash /home/kong/kong-entrypoint.sh`, removing the mounted-script execute-bit dependency that had been leaving `supabase.bookedai.au` behind a `502`
- recreated `supabase-kong`, confirmed `supabase.bookedai.au` returned `401` again, then rechecked the stack until `bookedai-cloudflare-dns.timer` was active, `scripts/healthcheck_stack.sh` passed, and `bot.bookedai.au` recovered to `200` after the OpenClaw gateway finished warming up
- updated `scripts/verify_gcp_ingress_for_bookedai.sh` default `EXPECTED_IP` to `34.40.192.68` so GCP ingress checks match the current production target
- annotated `deploy/nginx/bookedai.au.conf` with the current production origin IP and synced the operator guidance in `README.md` plus `docs/users/administrator-guide.md`
- fixed the live deploy blocker where `supabase-kong` could not start because `supabase/volumes/api/kong-entrypoint.sh` had no execute bit; the file now ships as executable
- reran `bash scripts/deploy_live_host.sh` successfully, reactivated the existing n8n workflow `stwMomWRq2qJoaTy`, and confirmed the main public surfaces and backend health endpoint were responding again
- hardened tenant workspace HTML handling by replacing the backend’s script-only filter with an allowlist sanitizer and by sanitizing both legacy admin preview and tenant onboarding preview before `dangerouslySetInnerHTML`
- reduced legacy admin session persistence risk by migrating browser-stored admin session data from `localStorage` into `sessionStorage`, while still clearing and reading legacy keys for compatibility
- removed admin or tenant session-signing fallback to `ADMIN_API_TOKEN` and `ADMIN_PASSWORD`, and tightened bootstrap admin auth so `ADMIN_BOOTSTRAP_PASSWORD` plus an eligible admin identity is now required for the root Next.js admin login lane
- replaced the root Next.js admin lane’s default shared-password login path with per-user email verification codes: `/admin-login` now requests and verifies one-time codes through new admin auth routes, while password bootstrap auth is now break-glass only behind `ADMIN_ENABLE_BOOTSTRAP_LOGIN=1`
- verified the new root admin flow end-to-end in local Next dev with `BOOKEDAI_ENABLE_PRISMA=0`: request-code returned a debug code in development, verify-code issued `bookedai_admin_session`, and `/api/admin/auth/me` accepted that cookie and returned the signed operator session
- attempted `npx prisma migrate deploy` for `admin_email_login_codes`, but the current shell cannot resolve the configured database host `supabase-db`; live migration still needs a runtime where that hostname is reachable
- confirmed the deeper rollout blocker is schema reality, not only hostname reachability: the live `bookedai` database already contains legacy `tenants`, `leads`, `subscriptions`, and `audit_logs` tables but does not carry the root Prisma `users` chain, so the checked-in Prisma admin migrations cannot be applied there directly
- added a root-admin compatibility path for that reality: when Prisma is not enabled, admin email-code login now resolves identities from `tenant_user_memberships`, stores codes in legacy `tenant_email_login_codes` with `auth_intent='admin-sign-in'`, and maps `legacy:<tenant_id>:<email>` sessions back to real accessible tenants
- verified that compatibility path end-to-end against the live Postgres container by requesting an admin code for `future-swim`, seeing the row persist in `tenant_email_login_codes`, verifying the code, receiving `bookedai_admin_session`, and confirming `/api/admin/auth/me` returns the real `Future Swim` tenant
- synced the security posture changes into `README.md`, `project.md`, `docs/architecture/auth-rbac-multi-tenant-security-strategy.md`, `docs/development/env-strategy.md`, and `docs/development/implementation-progress.md`
- widened the OpenClaw operator lane from allowlisted host maintenance to true full-server `host/elevated` execution by adding `host-shell` to `scripts/telegram_workspace_ops.py`
- updated OpenClaw permission defaults and docs so trusted actors can use `host_shell` or `full_project` for unrestricted server commands outside `/workspace/bookedai.au`
- verified the same `host_shell` lane also works through the Telegram actor path by running the wrapper in-container with `BOOKEDAI_TELEGRAM_USER_ID=8426853622`; permissions include `host_shell` and `host-shell --cwd / --command 'pwd'` returns `/`
- removed the live Telegram `pairing required` blocker by switching OpenClaw Telegram DM policy to `allowlist`, adding `channels.telegram.allowFrom=[\"8426853622\"]`, elevating the `bookedai-host-cli` device token to full operator scopes, and clearing the stuck pending approval request in OpenClaw runtime state
- restarted `openclaw-bookedai-gateway` and `openclaw-bookedai-cli`, then re-verified trusted Telegram host control with `host-shell` and a host-level `apt-get update` smoke run
- fixed an OpenClaw reliability gap by adding `restart: unless-stopped` to `openclaw-cli`, so the node-host process recovers automatically when it loses the initial gateway connect race during boot
- corrected the last host-control mismatch by making `scripts/telegram_workspace_ops.py` use `nsenter --target 1` when running inside the privileged CLI container; trusted `host-shell` and `host-command` now resolve `pip`, `venv`, and `apt` against the real host instead of the container filesystem
- widened the shared frontend admin shell into the first requirement-aligned enterprise IA package with `Overview`, `Tenants`, `Tenant Workspace`, `Catalog`, `Billing Support`, `Integrations`, `Reliability`, `Audit & Activity`, and `Platform Settings`
- kept admin hash routing backward-compatible by mapping older `#operations` links into the new `#overview` workspace while adding dedicated route homes for billing/support, integrations, audit chronology, and platform review
- split tenant selection from mutation in the shared admin runtime so operators review tenant scope in a directory lane before opening the mutable tenant workspace
- synced the admin IA change into `project.md`, `docs/architecture/admin-enterprise-workspace-requirements.md`, `docs/development/implementation-progress.md`, and `docs/development/sprint-13-16-user-surface-delivery-package.md`, then published the summary to Notion and Discord through `telegram_workspace_ops.py sync-doc`
- created the large-upgrade execution branch `codex/major-upgrade-plan-execution` from the current synced server baseline and committed checkpoint `7dc8a2b` (`feat: checkpoint admin auth hardening and enterprise ia`)
- pushed `codex/major-upgrade-plan-execution` to GitHub so the next implementation wave can proceed on a clean tracked branch without disturbing the earlier sync branch
- kept the pre-branch local backup in `stash@{0}` as `pre-merge-origin-main-2026-04-23-upgrade-plan`; only `.clawhub/` and `skills/` remain untracked local-only on the server working tree
- restored the missing repo files `frontend/src/shared/sanitizeTenantHtml.ts` and `lib/db/legacy-admin-auth.ts` from the pre-branch stash after build verification exposed them as dropped utilities
- committed and pushed the restored admin-auth and tenant-HTML files plus the root admin email-code login flow on `codex/major-upgrade-plan-execution` as `b5842a2` and `579f7dd`
- verified the new wave with Python compile checks, successful `node node_modules/next/dist/bin/next build`, and successful `npm --prefix frontend run build`; root `npm run build` still needs the `next` binary execute-bit workaround on this host
- redeployed the full live stack with `bash scripts/deploy_live_host.sh`, confirmed `bookedai.au`, `api.bookedai.au`, `tenant.bookedai.au`, `admin.bookedai.au`, and `bot.bookedai.au` were healthy again, and reactivated the existing n8n workflow `stwMomWRq2qJoaTy`
- re-synced the execution docs so `project.md`, the current phase plan, and the Sprint 13-16 package now all agree that `Campaigns` is already the opened growth-lane foundation and the next implementation slice is `Messaging`
- locked the active operator-facing admin productization lane to the shared `frontend/src` admin shell, while keeping the root `Next.js` admin tree as the parallel auth and repository foundation lane
- removed the remaining project-doc contradiction that still described `ADMIN_API_TOKEN` and `ADMIN_PASSWORD` as accepted session-signing fallback posture
- deepened the shared `frontend/src` admin enterprise IA by turning `Billing Support`, `Integrations`, and `Audit & Activity` from guidance-only route homes into read-model-backed workspaces using the existing admin event feed and support queue
- added `workspace-read-models.ts` plus new billing, integration, and audit summary sections so the broadened admin shell now gives operators actionable summaries before they open mutation-heavy panels
- verified the new admin workspace slice with `esbuild` on `AdminPage.tsx` and a targeted Playwright run `bash frontend/scripts/run_playwright_suite.sh legacy tests/admin-workspace-upgrade.spec.ts`
- converted the enterprise/startup repo review into an execution document at `docs/development/source-ownership-audit-2026-04-23.md`, recommending `frontend/` as the shipped truth, `backend/` as workflow truth, root Next.js plus Prisma as the constrained foundation lane, and the booking-to-CRM-to-revenue path as the next golden flow to harden
- added `docs/development/admin-surface-parity-tracker-2026-04-23.md`, mapping the shipped `frontend/` admin lane against root `app/admin/*` and concluding the project should keep the frontend shell for operator workflow framing while using root Next.js for auth, RBAC, repository truth, and module hardening until selective promotion is ready
- added `docs/development/admin-module-promotion-checklists-2026-04-23.md`, converting that parity tracker into concrete promotion gates for `Settings`, `Leads`, and `Customers`, with the recommended rollout order locked to `Settings` first, then `Leads`, then `Customers`
- added `docs/development/settings-promotion-execution-plan-2026-04-23.md`, locking the first real module-promotion plan to `Settings` via controlled handoff: keep the shipped `frontend/` shell as control tower, make root Next.js `Settings` the mutation truth, treat auth and tenant handoff as the first implementation task, and block release if fallback-only settings data paths remain in operator-target environments
- added `docs/development/settings-ownership-mapping-2026-04-23.md` and `docs/development/settings-auth-tenant-handoff-contract-2026-04-23.md`, freezing which settings concerns stay in the shipped shell versus root Settings and defining the required promotion guarantees around trusted session, tenant alignment, support mode, and return path
- added `docs/development/settings-promotion-technical-task-breakdown-2026-04-23.md`, turning the Settings promotion plan into implementation-ready engineering work across shipped shell summary and CTA changes, root-side auth and tenant handoff validation, route and param helpers, and smoke plus release verification; the next coding slice now starts with the handoff helpers and summary section
- implemented the first Settings promotion slice: shipped admin now has a `Workspace settings` summary plus `Open workspace settings` CTA, handoff URL building lives in `frontend/src/features/admin/settings-handoff.ts`, root-side handoff parsing and sanitization lives in `server/admin/settings-handoff.ts`, and `app/admin/settings/page.tsx` now shows handoff context, exposes a return path, and forces read-only posture on tenant mismatch
- hardened that Settings handoff one step further: root `Settings` now distinguishes invalid return paths and missing tenant hints as explicit re-entry states, and the shipped admin smoke pack now includes a dedicated Settings handoff CTA case in `frontend/tests/admin-workspace-upgrade.spec.ts` plus `frontend/scripts/run_admin_smoke.sh`
- extracted the pure Settings handoff parser and mutation-boundary guard logic into `server/admin/settings-handoff-core.ts` so the promotion contract can be tested without the `server-only` runtime wrapper; root unit coverage now lives in `server/admin/settings-handoff.test.ts` and frontend helper coverage now lives in `frontend/src/features/admin/settings-handoff.test.ts`
- moved the Settings protection into the real mutation layer with `app/admin/settings/action-guard.ts`: all Settings mutation forms now submit expected tenant plus handoff metadata, and server actions now reject mutation on tenant mismatch, missing tenant hint, invalid handoff, or support-mode protection even if the UI is bypassed; unit coverage lives in `app/admin/settings/action-guard.test.ts`
- improved operator clarity around the new guard: `app/admin/settings/page.tsx` now explains why the page is read-only for support mode, handoff re-entry, or tenant mismatch, and `components/admin/settings/workspace-settings-form.tsx` now renders a dedicated read-only reason block inside the workspace form instead of leaving operators with disabled controls and no explanation
- tightened Settings release-gate coverage further: `app/admin/settings/action-guard.test.ts` now also rejects unsafe return paths, and `components/admin/settings/workspace-settings-form.test.tsx` now proves the root workspace form renders the hidden handoff metadata plus the inline read-only explanation that the mutation boundary depends on
- direct verification for the Settings promotion boundary now includes passing root and frontend TypeScript checks plus passing node-based tests for root handoff parsing, action-guard enforcement, root form metadata rendering, and shipped-shell handoff URL generation; browser-level Playwright coverage remains blocked in this shell because the required Chromium binary is missing (`Executable doesn't exist ... chrome-headless-shell`)
- opened the next promotion wave for `Leads` with `docs/development/leads-promotion-baseline-2026-04-23.md`; the baseline now locks the ownership model before code work: shipped admin keeps lead summary and operator framing, root `Leads` keeps workflow and conversion truth, and the next required docs are ownership mapping, conversion contract, and execution plan rather than immediate UI migration
- added `docs/development/leads-ownership-mapping-2026-04-23.md` and `docs/development/leads-conversion-contract-2026-04-23.md`, freezing the Leads handoff model and the meaning of conversion before promotion starts: shipped shell owns urgency and framing, root `Leads` owns triage and conversion actions, and both customer and booking conversions now require clear downstream discoverability for operators
- completed `docs/development/leads-promotion-execution-plan-2026-04-23.md`, which sequences the promotion work into guarded handoff first, conversion aftermath visibility second, shipped-shell summary plus CTA third, and release-gate verification last; this locks `Leads` as a controlled handoff module, not a full admin migration
- locked the public frontend direction to `responsive web app first` in new doc `docs/architecture/frontend-runtime-decision-record.md`, updated homepage CTA and messaging on `bookedai.au` toward `Open Web App`, and deferred native mobile positioning to a later phase instead of the active homepage story
- synchronized the active phase, MVP, and Sprint 13-16 delivery docs with that same runtime decision, and cleaned remaining homepage-adjacent public CTA wording so responsive web is now the consistent live-product story across homepage and pitch surfaces
- finished a second source-of-truth cleanup across README and active public UX strategy docs so the repo now consistently describes homepage as the acquisition surface for the current responsive web runtime, with native mobile deferred instead of implied as immediate scope
- prepared a dated operator summary for `2026-04-22` results plus the new locked plan, rollout phases, and follow-up path at `docs/development/telegram-sync/2026-04-23/2026-04-23-yesterday-summary-next-plan.md`
- used that note as the source payload for a Discord operator update; Discord send succeeded to channel `1485197822583377993` and the published archive was saved at `docs/development/telegram-sync/2026-04-23/045729-bookedai-summary-2026-04-22-results-and-next-plan.md`
- completed the Leads planning set after the baseline: `docs/development/leads-ownership-mapping-2026-04-23.md`, `docs/development/leads-conversion-contract-2026-04-23.md`, and `docs/development/leads-promotion-execution-plan-2026-04-23.md`
- locked the Leads promotion model to controlled handoff only: shipped `frontend/` keeps operator framing, urgency, summary, CTA, and return path, while root `app/admin/leads/*` keeps workflow, mutation, CRM, and conversion truth
- froze the downstream conversion contract for `convertLeadToCustomerAction` and `convertLeadToBookingAction`: tenant-scoped correctness, clear downstream record creation/linkage, and operator-visible aftermath are required before Leads can be treated as promotion-ready
- sequenced the next real Leads implementation slice to mirror the hardened Settings pattern: add handoff core/guard protections first, then post-conversion trace visibility, then shipped-shell summary/CTA framing, then release-gate verification
- current next coding step is no longer strategy docs; it is Leads implementation slice 1 across root handoff metadata, action guards, support-mode protection, tenant mismatch protection, and unsafe return-path rejection
- completed that first Leads implementation slice: added `server/admin/leads-handoff-core.ts` plus `app/admin/leads/action-guard.ts`, wired hidden handoff metadata into `components/admin/leads/lead-form.tsx`, `app/admin/leads/page.tsx`, and `app/admin/leads/[leadId]/page.tsx`, and enforced the guard across create, update, archive, conversion, note, follow-up, and CRM retry actions
- verification for the new Leads boundary passed with `node --import tsx --test app/admin/leads/action-guard.test.ts server/admin/leads-handoff.test.ts components/admin/leads/lead-form.test.tsx` plus a clean root `node node_modules/typescript/bin/tsc --noEmit`
- prepared a structured execution snapshot covering completed tasks, results, phase status, sprint status, blockers, and next steps at `docs/development/telegram-sync/2026-04-23/2026-04-23-execution-summary-phase-sprint-next-steps.md` for operator-facing sync; Discord publish succeeded to channel `1485197822583377993` and the archived post was saved at `docs/development/telegram-sync/2026-04-23/050556-bookedai-execution-summary-phase-and-sprint-status-and-next-steps.md`
- fetched `origin` and verified the server working branch `codex/major-upgrade-plan-execution` is already aligned with `origin/codex/major-upgrade-plan-execution` at commit `4581c4d`
- confirmed `origin/main` is an ancestor of the current server branch, so there were no newer GitHub commits to merge into the server source at this time
- ran `git pull --no-rebase --autostash origin codex/major-upgrade-plan-execution`; Git reported `Already up to date`, and the existing local uncommitted changes on the server were left intact
- compared the current server branch against the other tracked remote branches and confirmed there are no remote-only commits left to merge: `origin/codex/sprint-dependency-planning-sync` is fully contained in the current branch, and `origin/main` is also already superseded by the current branch history
- promoted the latest release branch to official `main` by fast-forwarding local `main` to commit `4581c4d` and pushing `origin/main` from `f17c503` to `4581c4d`
- switched the server workspace onto `main` so the active source now runs from the official branch instead of `codex/major-upgrade-plan-execution`
- the first live deploy attempt from `main` exposed missing admin workspace files in the working tree; restored the required frontend and server support files from `stash@{0}^3`, verified `npm --prefix frontend run build` passed, then reran `bash scripts/deploy_live_host.sh` successfully
- official live runtime is now deployed from `main`; deploy script completed successfully and reactivated the existing n8n workflow `stwMomWRq2qJoaTy`
- finalized the restored upgrade work as commit `638bcff` on `main` with message `feat: promote admin handoff guards and messaging workspace`, then pushed it to `origin/main`
- verification before commit passed for `npm --prefix frontend run build` and the targeted Node/TS handoff plus guard test pack (`24/24` passing); Python backend test `backend/tests/test_admin_messaging_routes.py` could not run in this shell because `pytest` is not installed
- cleaned up the completed release branch `codex/major-upgrade-plan-execution` from both local and remote after `main` was updated; intentionally left `codex/sprint-dependency-planning-sync` untouched
- later compared `main` against `codex/sprint-dependency-planning-sync` and confirmed the sprint branch had no commits missing from `main`; `main` was already ahead by seven commits
- removed `codex/sprint-dependency-planning-sync` from both local and remote after confirming it was fully contained in `main`
- locked the next execution wave to a web-first `Golden Tenant Activation + Revenue Proof Loop`, documented at `docs/development/golden-tenant-activation-revenue-proof-loop-2026-04-23.md`: `Future Swim` is the primary wedge, `children's chess classes` and `AI Mentor 1-1` are later adaptation templates, tenant activation plus billing plus lead-conversion aftermath plus tenant ROI visibility now outrank broader workflow automation, and React Native stays deferred until this loop is commercially credible
- started Slice 1 implementation in the shipped tenant runtime by adding a derived activation-state helper plus a new activation checklist card, then wiring that card into `frontend/src/apps/tenant/TenantApp.tsx` so tenants now get a concrete next-step CTA into profile, catalog, or billing instead of only a generic onboarding percentage
- continued into Slice 2 by upgrading `frontend/src/features/tenant-billing/TenantBillingWorkspace.tsx` with clearer commercial truth labels, readiness cues, and manual-vs-provider-backed billing wording so the tenant billing surface stops overstating self-serve readiness when gateway or invoice posture is still partial
- started Slice 3 in the root admin leads lane by adding explicit post-conversion aftermath visibility: lead detail now resolves linked customer and booking artifacts into one `Conversion aftermath` card with direct next-step links, and the lead list or kanban surfaces now hint whether customer or booking downstream state likely exists instead of making operators infer it from stage only
- continued into Slice 4 by upgrading the tenant overview revenue surface in `frontend/src/apps/tenant/TenantApp.tsx` from a narrower capture card into a broader monthly value board that now frames paid revenue, outstanding revenue, booking proof, source contribution, and follow-up or CRM attention as one tenant-facing BookedAI revenue-proof story
- hardened that value board so partial tenants do not get misleading revenue wording: source contribution now derives from booking and payment signal labels instead of a booking-reference shortcut, and paid revenue can now fall back to paid invoice totals when higher-level revenue metrics are still absent
- re-reviewed the public homepage and pitch surfaces after the new operator request and locked the next public split in `docs/development/homepage-pitch-realignment-plan-2026-04-23.md`: `bookedai.au` should become a simpler Google-like product-first landing page, while `pitch.bookedai.au` should absorb the current homepage's long-form narrative and pitch content
- synced that public-direction change into `project.md`, `docs/development/implementation-progress.md`, and `docs/development/sprint-13-16-user-surface-delivery-package.md` so homepage simplification plus homepage-to-pitch migration is now an explicit planned slice, not just chat context
- re-confirmed the public-surface split after operator review: `bookedai.au` is the planned simple modern Google-like product-first landing surface, `pitch.bookedai.au` is the canonical home for the full current homepage narrative, and `product.bookedai.au` remains the deeper live runtime
- durable planning note for this wave lives at `docs/development/homepage-pitch-realignment-plan-2026-04-23.md`; the immediate implementation sequence is to slim `frontend/src/apps/public/PublicApp.tsx`, migrate long-form homepage sections into `frontend/src/apps/public/PitchDeckApp.tsx`, and keep CTA hierarchy aligned to `Open Web App` first
- implemented that public realignment slice: `frontend/src/apps/public/PublicApp.tsx` is now a shorter product-first landing surface with a simpler live-search-first flow, and `frontend/src/apps/public/PitchDeckApp.tsx` now carries the migrated homepage narrative layers so the long-form story sits on `pitch.bookedai.au` instead of crowding `bookedai.au`
- production frontend verification passed for the refactor with `npm --prefix frontend run build`
- polished `frontend/src/apps/public/PublicApp.tsx` one step further toward the requested Google-like product-first landing style by replacing the heavier shared hero and CTA blocks with simpler local sections, while keeping `pitch.bookedai.au` as the deeper narrative surface
- re-ran `npm --prefix frontend run build` after that polish pass and it still passed
- investigated the public homepage "Continue booking" loading-fail path in `frontend/src/apps/public/HomepageSearchExperience.tsx` and the parallel demo implementation in `frontend/src/components/landing/sections/BookingAssistantSection.tsx`; both flows share the same booking-session POST fallback to `${getApiBaseUrl()}/booking-assistant/session`, so the next debugging slice should focus on runtime response shape or endpoint behavior rather than only CTA wiring
- locked a new durable public-flow baseline: if live `/api/v1/leads` or `/api/v1/bookings/intents` are degraded, user-facing booking submit should fall back safely to the legacy booking-session lane instead of leaving `Continue booking` in a loading-fail state
- locked a matching public UX rule across homepage and assistant surfaces: slow search must expose visible progress, explain what BookedAI is doing, and prompt for better input detail like suburb, timing, audience, or preference while ranking is still resolving
- synced that baseline into `project.md`, `docs/development/implementation-progress.md`, `docs/development/sprint-13-16-user-surface-delivery-package.md`, and `docs/architecture/current-phase-sprint-execution-plan.md`
- added `docs/development/public-search-booking-resilience-ux-2026-04-23.md` as the dedicated execution note for the new public-flow requirement set, and updated the homepage-to-pitch realignment doc so content split, slow-search UX, and degraded-write fallback now stay linked in one documentation chain
- locked a new marketplace-direction visual baseline for the first web session: the supplied strategy artwork is now stored at `frontend/public/branding/bookedai-marketplace-strategy-bg.jpg` and used in `frontend/src/apps/public/PublicApp.tsx` as the homepage hero backdrop plus lighter secondary texture, so the opening surface now communicates BookedAI's broader SME services marketplace ambition from first load
- locked the next durable product direction for the same flow: BookedAI should operate as an embeddable multi-tenant runtime for customer-owned SME websites, not only as `bookedai.au`; the dedicated architecture note is `docs/development/widget-plugin-multi-tenant-booking-architecture-2026-04-23.md`
- ran `python3 scripts/telegram_workspace_ops.py deploy-live` from the server workspace after the public homepage/pitch split; Docker rebuilt and restarted the live web/backend surfaces, proxy restarted cleanly, and n8n workflow `stwMomWRq2qJoaTy` was confirmed active
- re-confirmed Telegram/OpenClaw host deploy permission for trusted actor `8426853622`: live containers expose `deploy_live`, `host_shell`, and `full_project`, and the CLI container reports `host_exec_mode: nsenter-host`
- fixed the remaining OpenClaw/Telegram deploy gap: `deploy-live` now enters the host namespace when invoked from `/workspace/bookedai.au`, and the exact container command `cd /workspace/bookedai.au && python3 scripts/telegram_workspace_ops.py --telegram-user-id 8426853622 deploy-live` completed successfully
- published the full 2026-04-23 operator summary to Discord pinboard channel `1492396018086576268`; archive saved at `docs/development/telegram-sync/2026-04-23/144841-bookedai-full-day-operator-summary-2026-04-23.md` and Notion sync succeeded
- redeployed production again on operator request with `python3 scripts/telegram_workspace_ops.py deploy-live`; frontend build passed first, Docker rebuilt/restarted live web/backend surfaces, proxy restarted cleanly, and n8n workflow `stwMomWRq2qJoaTy` was reactivated
- post-deploy verification passed: `scripts/healthcheck_stack.sh` succeeded, `https://bookedai.au`, `tenant.bookedai.au`, `admin.bookedai.au`, `pitch.bookedai.au`, and `bot.bookedai.au` returned `200`, and the backend health route is `https://api.bookedai.au/api/health`
