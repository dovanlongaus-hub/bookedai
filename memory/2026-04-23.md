- pinned the production Cloudflare DNS automation path to support the new origin IP `34.40.192.68` through `CLOUDFLARE_AUTO_DNS_IPV4` or a manual CLI arg in `scripts/update_cloudflare_dns_records.sh`
- fixed a real boot-sync blocker in `scripts/update_cloudflare_dns_records.sh` by checking `configure_cloudflare_dns.sh` with `-f` instead of `-x`, then ran the Cloudflare DNS sync successfully against the configured production record set at `34.40.192.68`
- switched Cloudflare DNS automation back to always auto-detect mode for ongoing operations by adding `deploy/systemd/bookedai-cloudflare-dns.timer` and updating the installer to enable both boot-time and periodic reconciliation
- widened the Cloudflare auto-sync host list to cover the remaining public BookedAI hosts and upgraded IP discovery to prefer cloud metadata endpoints before public echo services
- fixed the `supabase-kong` startup path by changing the compose entrypoint to `/bin/bash /home/kong/kong-entrypoint.sh`, removing the mounted-script execute-bit dependency that had been leaving `supabase.bookedai.au` behind a `502`
- recreated `supabase-kong`, confirmed `supabase.bookedai.au` returned `401` again, then rechecked the stack until `bookedai-cloudflare-dns.timer` was active, `scripts/healthcheck_stack.sh` passed, and `bot.bookedai.au` recovered to `200` after the OpenClaw gateway finished warming up
- updated `scripts/verify_gcp_ingress_for_bookedai.sh` default `EXPECTED_IP` to `34.40.192.68` so GCP ingress checks match the current production target
- annotated `deploy/nginx/bookedai.au.conf` with the current production origin IP and synced the operator guidance in `README.md` plus `docs/users/administrator-guide.md`
- fixed the live deploy blocker where `supabase-kong` could not start because `supabase/volumes/api/kong-entrypoint.sh` had no execute bit; the file now ships as executable
- reran `bash scripts/deploy_live_host.sh` successfully, reactivated the existing n8n workflow `stwMomWRq2qJoaTy`, and confirmed the main public surfaces and backend health endpoint were responding again
- hardened tenant workspace HTML handling by replacing the backend’s script-only filter with an allowlist sanitizer and by sanitizing both legacy admin preview and tenant onboarding preview before `dangerouslySetInnerHTML`
- reduced legacy admin session persistence risk by migrating browser-stored admin session data from `localStorage` into `sessionStorage`, while still clearing and reading legacy keys for compatibility
- removed admin or tenant session-signing fallback to `ADMIN_API_TOKEN` and `ADMIN_PASSWORD`, and tightened bootstrap admin auth so `ADMIN_BOOTSTRAP_PASSWORD` plus an eligible admin identity is now required for the root Next.js admin login lane
- replaced the root Next.js admin lane’s default shared-password login path with per-user email verification codes: `/admin-login` now requests and verifies one-time codes through new admin auth routes, while password bootstrap auth is now break-glass only behind `ADMIN_ENABLE_BOOTSTRAP_LOGIN=1`
- verified the new root admin flow end-to-end in local Next dev with `BOOKEDAI_ENABLE_PRISMA=0`: request-code returned a debug code in development, verify-code issued `bookedai_admin_session`, and `/api/admin/auth/me` accepted that cookie and returned the signed operator session
- attempted `npx prisma migrate deploy` for `admin_email_login_codes`, but the current shell cannot resolve the configured database host `supabase-db`; live migration still needs a runtime where that hostname is reachable
- confirmed the deeper rollout blocker is schema reality, not only hostname reachability: the live `bookedai` database already contains legacy `tenants`, `leads`, `subscriptions`, and `audit_logs` tables but does not carry the root Prisma `users` chain, so the checked-in Prisma admin migrations cannot be applied there directly
- added a root-admin compatibility path for that reality: when Prisma is not enabled, admin email-code login now resolves identities from `tenant_user_memberships`, stores codes in legacy `tenant_email_login_codes` with `auth_intent='admin-sign-in'`, and maps `legacy:<tenant_id>:<email>` sessions back to real accessible tenants
- verified that compatibility path end-to-end against the live Postgres container by requesting an admin code for `future-swim`, seeing the row persist in `tenant_email_login_codes`, verifying the code, receiving `bookedai_admin_session`, and confirming `/api/admin/auth/me` returns the real `Future Swim` tenant
- synced the security posture changes into `README.md`, `project.md`, `docs/architecture/auth-rbac-multi-tenant-security-strategy.md`, `docs/development/env-strategy.md`, and `docs/development/implementation-progress.md`
- widened the OpenClaw operator lane from allowlisted host maintenance to true full-server `host/elevated` execution by adding `host-shell` to `scripts/telegram_workspace_ops.py`
- updated OpenClaw permission defaults and docs so trusted actors can use `host_shell` or `full_project` for unrestricted server commands outside `/workspace/bookedai.au`
- verified the same `host_shell` lane also works through the Telegram actor path by running the wrapper in-container with `BOOKEDAI_TELEGRAM_USER_ID=8426853622`; permissions include `host_shell` and `host-shell --cwd / --command 'pwd'` returns `/`
- removed the live Telegram `pairing required` blocker by switching OpenClaw Telegram DM policy to `allowlist`, adding `channels.telegram.allowFrom=[\"8426853622\"]`, elevating the `bookedai-host-cli` device token to full operator scopes, and clearing the stuck pending approval request in OpenClaw runtime state
- restarted `openclaw-bookedai-gateway` and `openclaw-bookedai-cli`, then re-verified trusted Telegram host control with `host-shell` and a host-level `apt-get update` smoke run
- fixed an OpenClaw reliability gap by adding `restart: unless-stopped` to `openclaw-cli`, so the node-host process recovers automatically when it loses the initial gateway connect race during boot
- widened the shared frontend admin shell into the first requirement-aligned enterprise IA package with `Overview`, `Tenants`, `Tenant Workspace`, `Catalog`, `Billing Support`, `Integrations`, `Reliability`, `Audit & Activity`, and `Platform Settings`
- kept admin hash routing backward-compatible by mapping older `#operations` links into the new `#overview` workspace while adding dedicated route homes for billing/support, integrations, audit chronology, and platform review
- split tenant selection from mutation in the shared admin runtime so operators review tenant scope in a directory lane before opening the mutable tenant workspace
- synced the admin IA change into `project.md`, `docs/architecture/admin-enterprise-workspace-requirements.md`, `docs/development/implementation-progress.md`, and `docs/development/sprint-13-16-user-surface-delivery-package.md`, then published the summary to Notion and Discord through `telegram_workspace_ops.py sync-doc`
