# 2026-04-28

- Closed the proposed `tenant.bookedai.au` SME owner QA follow-up live.
- Refreshed tenant gateway/workspace wording toward service-business owners: owner access, enquiry capture, booking revenue, customer follow-up, business profile, website widget, billing readiness, and follow-up history.
- Added authenticated tenant workspace Playwright coverage to frontend release gate; tenant smoke now covers gateway variants, admin workspace panels, finance read-only posture, and mobile containment.
- Stabilized Playwright preview with a static SPA dist server so smoke gates do not depend on non-TTY `vite preview`.
- Fixed the frontend Docker build blocker in `BookingAssistantSection.tsx`, then deployed with `bash scripts/deploy_live_host.sh`.
- Verification passed: frontend release gate, stack health at `2026-04-28T01:55:32Z`, and live Playwright smoke for tenant control/revenue variants plus `/future-swim`.
- Continued Zoho CRM booking-sync closeout with live UAT: phone-only Future Swim booking `v1-53a53835ae` returned `200`, reopened in the portal, and Zoho lead/contact/deal/task all reported `synced` with external ids.
- Hardened release-gate follow-up locally: fixed Telegram handoff claimed TTL/suppression, refreshed stale homepage smoke assertions, combined legacy/admin smoke cases, served Playwright from `dist`, and verified frontend smoke lanes plus backend release unittest lane.
- Continued tenant SME owner follow-up locally: tightened auth-card copy from tenant/internal language to owner/business workspace language and refreshed billing labels from commercial truth/payment posture to billing readiness/payment setup.
- Added tenant mobile stale-copy/overflow smoke and verified `npm --prefix frontend run build` plus `PLAYWRIGHT_SKIP_BUILD=1 npm --prefix frontend run test:playwright:tenant-smoke` (`4 passed`).
- Live deploy for this tenant follow-up remains pending a clean full frontend release-gate rerun because the dirty workspace had stale smoke-script restores during an earlier full-gate attempt.
- Updated the AI Mentor 1-1 tenant baseline in repo data: seed `013` now uses `aimentor@bookedai.au` for login/contact, password hash for `FirstHundred1M$`, and `+84908444095` for phone/WhatsApp/Telegram/iMessage.
- Added migration `023_ai_mentor_contact_login_update.sql` to upgrade already-seeded `tenant3 / 123` AI Mentor records and clear stale demo credentials.
- Synced the AI Mentor contact/login change into `project.md`, migration docs, implementation progress, Phase 19 roadmap detail, past-work log, and the AI Mentor plugin interface doc.
- Published the AI Mentor contact/login closeout to Notion and Discord via `telegram_workspace_ops.py sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-28/024447-ai-mentor-1-1-tenant-contact-and-login-refresh.md`.
- Applied migration `023` to host `supabase-db`; verification found `aimentor@bookedai.au` active, settings contact email/phone updated, 10 service rows owned by the new email, and zero stale `tenant3` credentials.
- Published a second runtime-applied closeout to Notion/Discord; archive entry is `docs/development/telegram-sync/2026-04-28/024650-ai-mentor-1-1-runtime-contact-login-applied.md`.
- Merged all open GitHub PRs into `main` and pushed: #17 Telegram group/channel silent-unless-addressed, #18 customer handoff sessions plus #19/#20 product handoff and metrics, and #21 admin claim/release handoff suppression.
- Resolved merge drift by preserving support-handoff failure/debounce, admin claim TTL suppression, group keyboard stripping, recent booking recall, and `hsess_` homepage/product context handoff into the Telegram bot.
- Verification before deploy: backend targeted suite (`100 passed`), frontend release gate (`build`, legacy smoke, live-read smoke, admin smoke, tenant smoke), and Docker production builds.
- Deployed live with `bash scripts/deploy_live_host.sh`; stack health passed at `2026-04-28T03:02:21Z`; live API health, tenant, and product URLs returned `200`.
- Synced the remaining AI Mentor/public-intake hardening to `main` at `3c95d2a`, then stabilized homepage A/B smoke assertions at `3f8bcb9` so release-gate QA tracks deterministic SME service-business wording.
- Full frontend release gate passed after the smoke stabilization: build, legacy smoke, live-read smoke, admin smoke, and tenant smoke all green.
- Deployed `3f8bcb9` live with `bash scripts/deploy_live_host.sh`; stack health passed at `2026-04-28T03:33:43Z`; API health, tenant, product, homepage, AI Mentor embed URL, and AI Mentor widget asset returned `200`.
- Remaining AI Mentor follow-up: static `OPTIONS` on `/partner-plugins/ai-mentor-pro-widget.js` returns `405`, so target CORS `204` and browser-level catalog/booking proof should stay in the D-1 rehearsal checklist.
- Published the final AI Mentor sync/deploy closeout to Notion and Discord; archive entry is `docs/development/telegram-sync/2026-04-28/033541-ai-mentor-tenant-sync-and-live-deploy-closeout.md`.
- Fixed the live `product.bookedai.au` booking error `Tenant session required` for search-to-booking fallback paths.
- Added migration `024_bookedai_web_booking_tenant.sql` for tenant slug `bookedai-au` / `BookedAI.au Web Booking` with login/contact `info@bookedai.au`, password `FirstHundredM$`, and contact phone/Telegram/WhatsApp/SMS `+61455301335`.
- Backend public-web and embedded-widget booking handlers now fall back to `bookedai-au` when no provider tenant resolves, while authenticated tenant-session mismatch validation remains intact.
- Applied migration `024` live, deployed with `bash scripts/deploy_live_host.sh`, passed stack health at `2026-04-28T04:00:02Z`, and live UAT created booking reference `v1-e13e9d61f0` under tenant `bookedai-au`.
- Published the Web Booking fallback closeout to Notion and Discord; archive entry is `docs/development/telegram-sync/2026-04-28/040056-bookedai-public-web-booking-tenant-fallback.md`.
- Ran a multi-agent read-only review for BookedAI.au across UX/UI, QA, security, and investor/WSTI narrative using existing review, Playwright QA, frontend-design, security, startup, content, and executive-briefing skill guidance.
- Strongest cross-lane recommendation: surface a public `Agent Activity / Audit Ledger` proof drawer or ticker so judges, owners, and investors can see enquiry -> match -> booking reference -> portal/payment posture -> follow-up/action evidence.
- Highest-risk security findings to verify/remediate: unauthenticated tenant preview operational reads, unauthenticated `actor_context.tenant_id` trust, payment return origin/tenant scoping, unsigned WhatsApp POSTs, portal reference throttling, and fail-open webhook idempotency.
- UX/narrative findings: tighten beachhead around appointment/class/consultation service SMEs, normalize live-vs-planned channel truth, clean pricing model, elevate WSTI demo path, and reduce surface-specific visual drift from the Apple-like system.
- QA recommendation: add one golden E2E path covering Ask -> Match -> Compare -> Book -> Confirm -> Portal -> Follow-up -> tenant/admin evidence, plus mobile CTA/tap-target/proof-surface assertions beyond current no-overflow smoke checks.
- Updated the live chess tenant credential: `co-mai-hung-chess-class` now signs in as `chess@bookedai.au / FirstHundredM$`; migration `025_chess_tenant_contact_login_update.sql` was applied live and stale `tenant1 / 123` plus `chess` demo credentials are inactive.
- Verification passed: live password-auth API returned `ok` for `chess@bookedai.au` with `tenant_admin`, old `tenant1 / 123` returned `401`, and live browser smoke on `tenant.bookedai.au/co-mai-hung-chess-class` reached `Connected as chess@bookedai.au` with write controls enabled.
- Published the chess tenant login closeout to Notion and Discord; archive entry is `docs/development/telegram-sync/2026-04-28/041731-chess-tenant-login-refresh.md`.
- Implemented the first WSTI/investor-proof follow-up locally: homepage `Agent activity proof`, `Live/In rollout/Next` channel truth labels, `?demo=wsti` judge mode, proof-oriented CTAs, and `$49+/mo` pricing copy.
- Verification passed: `npm --prefix frontend run build`, legacy homepage smoke subset (`4 passed`), exact `public-homepage-responsive.spec.ts` (`4 passed`), full frontend release gate, live deploy through `bash scripts/deploy_live_host.sh`, stack health at `2026-04-28T04:18:32Z`, and live `?demo=wsti` Playwright smoke with `overflow=0` and no console errors.
- Published the WSTI homepage local closeout to Notion/Discord; archive entries are `041235-bookedai-wsti-investor-proof-homepage-pass.md` and the `$49+/mo` correction `041313-correction-bookedai-wsti-homepage-pricing-copy.md`.
- Published the live deploy closeout to Notion/Discord; archive entry is `docs/development/telegram-sync/2026-04-28/041940-bookedai-wsti-homepage-proof-live-deploy-closeout.md`.
- Updated the live AI Mentor tenant baseline: `ai-mentor-doer` now displays as `AI Mentor 1-1 Pro`, signs in as `aimentor@bookedai.au / FirstHundredM$`, and uses `+61481993178` for phone/Telegram/WhatsApp/iMessage.
- Refreshed seed migration `013_ai_mentor_tenant_seed.sql`, added/applied migration `026_ai_mentor_pro_contact_login_update.sql`, and verified DB tenant/plugin/contact fields plus active credential.
- Verification passed: live password-auth API returned `ok` with `tenant_admin`, the previous password returned `401`, and live browser smoke on `tenant.bookedai.au/ai-mentor-doer` reached `Connected as aimentor@bookedai.au` with write controls enabled.
- Published the AI Mentor 1-1 Pro tenant login closeout to Notion and Discord; archive entry is `docs/development/telegram-sync/2026-04-28/042506-ai-mentor-1-1-pro-tenant-login-refresh.md`.
- Continued Sprint B security hardening after the multi-agent WSTI/investor review and deployed it live.
- Tenant operational reads for bookings, leads, integrations, billing, team, and revenue metrics now require an authenticated tenant session; unauthenticated raw `actor_context.tenant_id` is rejected.
- Payment intent creation now rejects cross-tenant booking references, and payment return URLs ignore untrusted `Origin` headers unless they are BookedAI-owned HTTPS origins or local dev.
- Repaired release-gate fixture drift by adding `stripe_webhook_secret=""` to lifecycle test `Settings` factories.
- Verification passed: focused backend hardening suite (`59 passed`), backend release unittest lane (`102 tests OK`), full release gate, deploy-live, stack health at `2026-04-28T04:41:07Z`, unsigned/signed tenant probes, public fallback probe, and WSTI homepage shell probe.
- Published the local and live hardening closeouts to Notion/Discord; final live archive entry is `docs/development/telegram-sync/2026-04-28/044343-bookedai-sprint-b-tenant-payment-security-live-deploy-closeout.md`.
- Fixed and deployed the AI Mentor tenant mismatch login defect on `tenant.bookedai.au`: valid `aimentor@bookedai.au / FirstHundredM$` credentials now route to the credential-owned `ai-mentor-doer` session instead of failing when the requested path carries another tenant.
- Frontend tenant workspace reload now sends the session token for overview and booking reads, so the root-gateway AI Mentor login redirects to `/ai-mentor-doer` and loads the connected workspace instead of showing the workspace attention error.
- Verification passed: backend tenant route tests (`42 passed`), frontend typecheck/build, live deploy, stack health at `2026-04-28T04:39:48Z`, live API mismatch smoke, and Playwright browser smoke from `https://tenant.bookedai.au/` to `AI Mentor 1-1 Pro`.
- Published the mismatch-fix closeout to Notion/Discord; archive entry is `docs/development/telegram-sync/2026-04-28/044231-ai-mentor-tenant-login-mismatch-fix.md`.
- Fixed and deployed tenant logout return behavior: Sign out now clears both tenant-slug and gateway/default sessions, then redirects back to `https://tenant.bookedai.au/` for a clean next-account login.
- Docker root disk hit `100%` during the first deploy retry; unused Docker builder cache/images were pruned, freeing about `20GB`, and the second deploy completed successfully.
- Verification passed: frontend typecheck/build, live deploy, stack health at `2026-04-28T07:58:00Z`, and Playwright login/logout smoke showing AI Mentor logout returns to the gateway with no tenant session localStorage keys left.
- Published the tenant logout closeout to Notion/Discord; archive entry is `docs/development/telegram-sync/2026-04-28/080026-tenant-logout-returns-to-login-gateway.md`.
- Enabled Claude Code auto-approve posture for this workspace by setting project `.claude/settings.json` `permissions.defaultMode=bypassPermissions` and VS Code Claude extension settings `allowDangerouslySkipPermissions=true` plus `initialPermissionMode=bypassPermissions`.
- Ran outbound WhatsApp test sends to `+84908444095` through both configured business providers.
- Meta Cloud API returned HTTP `403` / `(#200) You do not have the necessary permissions to send messages on behalf of this WhatsApp Business Account`; no Meta message id was issued.
- Twilio accepted the WhatsApp request as `SMfe5ccb43d712f699bd3480956f7f7bb7`, then the message moved to `failed` with Twilio error `63112`, indicating the connected Meta/WABA sender is disabled, restricted, or still pending verification.
- Ran a read-only multi-agent QA/architecture/UI/Stripe review focused on `product.bookedai.au`, intelligent AI search, full booking-to-care lifecycle, CRM/email, retention, and enterprise mobile UX.
- Targeted backend regression passed: `.venv-backend/bin/pytest` on search, booking assistant runtime, v1 booking, Stripe webhook, WhatsApp, and Zoho feedback suites returned `73 passed`.
- Targeted product Playwright run returned `4 passed / 4 failed`; failures covered stale/missing CTA/spec text, hidden `Care` text assertion, event attendance CTA mismatch, and strict-mode duplicate swim name in popup.
- Highest-priority review findings: verified Stripe return before showing paid, reconcile tenant subscriptions from Stripe webhooks, centralize booking capture for portal tokens/idempotency/audit/outbox, harden messaging care identity beyond bare booking references, and simplify product mobile AI search into one result stack with accessible preview sheets and 44px tap targets.
- Implemented the first Stripe critical fix locally: checkout success URLs now include `session_id={CHECKOUT_SESSION_ID}` while cancel URLs stay unchanged and payment still remains `requires_action` until webhook reconciliation.
- Verification passed: `.venv-backend/bin/pytest backend/tests/test_api_v1_booking_routes.py backend/tests/test_stripe_webhook_routes.py` returned `30 passed`.
- Continued the product AI-search/booking QA hardening slice locally after the multi-agent review.
- Messaging-origin booking capture now mints portal access tokens, stores only token hashes, and returns tokenized portal/QR links for support follow-up.
- Product/mobile UAT fixes covered trial CTA wording, event attendance wording, popup preview accessibility, stable selectors, and touch-target sizing.
- Verification passed: focused backend booking/messaging/Stripe/Telegram/WhatsApp suite returned `123 passed`; product Playwright UAT returned `8 passed`; frontend `npm run build` succeeded.
- Published the local closeout to Notion/Discord; archive entry is `docs/development/telegram-sync/2026-04-28/134005-product-ai-search-booking-qa-hardening-local-closeout.md`.
- Remaining follow-ups: live deploy, Stripe subscription/webhook reconciliation, and stronger messaging identity checks beyond tokenized portal links.
- Completed the proposed product follow-up live: Stripe subscription webhook reconciliation, messaging bare-reference identity gate, AI Mentor widget CORS preflight, and API-domain TrustedHost env sync.
- Fixed `/partner-plugins/` nginx precedence with `^~`, so live `OPTIONS https://product.bookedai.au/partner-plugins/ai-mentor-pro-widget.js` now returns `204` with CORS headers while GET remains `200`.
- Fixed deploy env sync so `https://api.bookedai.au` stays in `CORS_ALLOW_ORIGINS`; stack health passed at `2026-04-28T14:12:25Z` and `https://api.bookedai.au/api/health` returned `200`.
- Verification passed: focused backend product/messaging/Stripe suite (`155 passed`), backend release unittest lane (`103 tests OK`), search eval (`14/14`), frontend build, Playwright legacy/live-read/admin/tenant smoke lanes, and deploy-live.
- The single release-gate wrapper still has a pre-existing `.env.production.example` checksum drift; manual equivalent lanes passed for this closeout.
- Published the live closeout to Notion/Discord; archive entry is `docs/development/telegram-sync/2026-04-28/141351-product-ai-search-booking-qa-live-closeout.md`.
