# 2026-04-28

- Closed the proposed `tenant.bookedai.au` SME owner QA follow-up live.
- Refreshed tenant gateway/workspace wording toward service-business owners: owner access, enquiry capture, booking revenue, customer follow-up, business profile, website widget, billing readiness, and follow-up history.
- Added authenticated tenant workspace Playwright coverage to frontend release gate; tenant smoke now covers gateway variants, admin workspace panels, finance read-only posture, and mobile containment.
- Stabilized Playwright preview with a static SPA dist server so smoke gates do not depend on non-TTY `vite preview`.
- Fixed the frontend Docker build blocker in `BookingAssistantSection.tsx`, then deployed with `bash scripts/deploy_live_host.sh`.
- Verification passed: frontend release gate, stack health at `2026-04-28T01:55:32Z`, and live Playwright smoke for tenant control/revenue variants plus `/future-swim`.
- Continued Zoho CRM booking-sync closeout with live UAT: phone-only Future Swim booking `v1-53a53835ae` returned `200`, reopened in the portal, and Zoho lead/contact/deal/task all reported `synced` with external ids.
- Hardened release-gate follow-up locally: fixed Telegram handoff claimed TTL/suppression, refreshed stale homepage smoke assertions, combined legacy/admin smoke cases, served Playwright from `dist`, and verified frontend smoke lanes plus backend release unittest lane.
- Continued tenant SME owner follow-up locally: tightened auth-card copy from tenant/internal language to owner/business workspace language and refreshed billing labels from commercial truth/payment posture to billing readiness/payment setup.
- Added tenant mobile stale-copy/overflow smoke and verified `npm --prefix frontend run build` plus `PLAYWRIGHT_SKIP_BUILD=1 npm --prefix frontend run test:playwright:tenant-smoke` (`4 passed`).
- Live deploy for this tenant follow-up remains pending a clean full frontend release-gate rerun because the dirty workspace had stale smoke-script restores during an earlier full-gate attempt.
