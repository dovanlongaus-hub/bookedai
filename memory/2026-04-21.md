# 2026-04-21

## Telegram Bot Runtime

- Activated `@Bookedairevenuebot` on OpenClaw for the BookedAI repository
- Fixed Telegram webhook routing to `https://api.bookedai.au/telegram-webhook`
- Enabled trusted Telegram user access for operator user `8426853622`
- Set model path to `openai-codex/gpt-5.4`

## Workspace Reliability

- Opened repo write permissions so the OpenClaw runtime user can edit project files
- Added workspace context files at repo root so Telegram tasks can run with expected startup context

## Code Review And Planning

- Added `docs/development/codebase-review-2026-04-21.md` as a repo-state code review snapshot covering architecture review, code smells, refactor priorities, and a next-sprint technical action plan
- Review conclusion: the repo is materially implemented and on the right modularization path, but the main technical debt is still concentrated in `backend/api/v1_routes.py`, `backend/services.py`, `backend/tests/test_api_v1_routes.py`, `frontend/src/apps/tenant/TenantApp.tsx`, and `frontend/src/components/landing/assistant/BookingAssistantDialog.tsx`
- Recommended next engineering sequence: finish tenant and portal handler extraction first, split route tests by bounded context, fix the known public-web fallback regressions, then move on to integrations and communications before touching deeper search/booking and `backend/services.py` decomposition
- Completed the tenant and portal handler extraction pass: `backend/api/v1_tenant_routes.py` now points fully to `backend/api/v1_tenant_handlers.py` for tenant and portal endpoints, and the updated modules passed `python3 -m py_compile`
- Split tenant and portal coverage out of `backend/tests/test_api_v1_routes.py` into `backend/tests/test_api_v1_tenant_routes.py` and `backend/tests/test_api_v1_portal_routes.py`; compile checks passed for the new test files, while direct pytest execution remains blocked because the current virtualenvs do not include `pytest`

## Follow-Up

- When Telegram tasks modify architecture or setup, sync repo docs in the same workstream
- Keep using this daily note to preserve continuity between Telegram-driven requests
- Keep Telegram memory entries short so session continuity does not turn into context bloat

## Homepage Architecture Refresh

- Redesigned the public homepage `#architecture` section in `frontend/src/components/landing/sections/ArchitectureInfographicSection.tsx`
- Replaced the older investor-style infographic with a clearer layered architecture layout showing explicit system roles, role ownership, and the runtime stack from surfaces through access control, orchestration, integrations, and cloud foundation
- Verified the frontend build passes after the update with `npm run build` from `frontend/`
- Expanded the section again with larger parent frames, nested subframes, larger readable text, stronger color separation between layers, and a fuller infrastructure foundation breakdown
- Shifted the section messaging and hierarchy further toward a sales-deck presentation: clearer commercial headline, staged value-story framing, buyer-friendly section labels, and infrastructure proof positioned as credibility support
- Pushed the section further toward a premium pitch-deck style with three opening thesis slides (`Why now`, `Why this wins`, `Why it scales`) and shorter, more commercial copy in the role cards
- Added a stronger keynote-style finish with premium headline typography and a closing thesis panel that frames BookedAI as the operating layer for service revenue

## Release Alignment And Deployment

- Restored backend compatibility for tenant session signing so actor-specific secrets no longer break older fallback auth flows or test fixtures
- Restored public-web search fallback behavior for  and  flows while preserving strict tenant-first behavior for tenant-scoped requests
- Added  so Docker builds stop inheriting broken host  permissions and the production frontend image builds cleanly again
- Re-synced , , and active roadmap or sprint documents so the repo truth now matches the deployed runtime: multi-surface production frontend remains under , while root Next.js is still a parallel lane
- Patched frontend and upload Nginx rules to return  for dotfile paths like , then redeployed production and verified  and  both return  while homepage and API health remain good

## Remaining Risk

- The root Next.js lane builds successfully but is still not the single production frontend source; any migration should be explicit and staged
- The API key pasted in chat should be rotated after this session because it has been exposed outside normal secret storage

## Release Alignment And Deployment

- Restored backend compatibility for tenant session signing so actor-specific secrets no longer break older fallback auth flows or test fixtures
- Restored public-web search fallback behavior for public_web and embedded_widget flows while preserving strict tenant-first behavior for tenant-scoped requests
- Added frontend/.dockerignore so Docker builds stop inheriting broken host node_modules permissions and the production frontend image builds cleanly again
- Re-synced project.md, README.md, and active roadmap or sprint documents so the repo truth now matches the deployed runtime: multi-surface production frontend remains under frontend/, while root Next.js is still a parallel lane
- Patched frontend and upload Nginx rules to return 404 for dotfile paths like /.env, then redeployed production and verified bookedai.au/.env and upload.bookedai.au/.env both return 404 while homepage and API health remain good

## Remaining Risk

- The root Next.js lane builds successfully but is still not the single production frontend source; any migration should be explicit and staged
- The API key pasted in chat should be rotated after this session because it has been exposed outside normal secret storage

## Search Fallback Hardening

- Hardened `backend/api/v1_routes.py` public-web fallback assembly so route-level search now normalizes fallback candidates, drops malformed or duplicate web results, and keeps a deterministic top-3 fallback payload even if the upstream service returns noisy data
- Split search-heavy route coverage out of `backend/tests/test_api_v1_routes.py` into `backend/tests/test_api_v1_search_routes.py` so the matching and fallback lane can be verified independently from communications and admin coverage
- Verified compile health for `backend/api/v1_routes.py`, `backend/tests/test_api_v1_routes.py`, and `backend/tests/test_api_v1_search_routes.py` with `python3 -m py_compile`; runtime test execution is still blocked in the available shell environments because FastAPI and pytest are not installed there
- Started the next bounded-context extraction pass for integrations: added `backend/api/v1_integration_handlers.py`, moved the current integration and outbox handler implementations there behind a transitional `from api.v1_routes import *` import, rewired `backend/api/v1_integration_routes.py` to the new handler module, and verified compile health for the integration route stack with `python3 -m py_compile`
- Continued the bounded-context extraction with communications: added `backend/api/v1_communication_handlers.py`, moved lifecycle email, SMS, and WhatsApp handlers there behind the same transitional `from api.v1_routes import *` import pattern, rewired `backend/api/v1_communication_routes.py`, and verified compile health for the communication route stack with `python3 -m py_compile`
- Continued with search and matching extraction: added `backend/api/v1_search_handlers.py`, moved `/api/v1/matching/search` and `/api/v1/booking-trust/checks` handler implementations there behind the same transitional wildcard import pattern, rewired `backend/api/v1_search_routes.py`, and verified compile health for the search route stack plus `backend/tests/test_api_v1_search_routes.py` with `python3 -m py_compile`
- Completed the first booking extraction pass: added `backend/api/v1_booking_handlers.py`, moved lead creation, conversation session start, booking path resolution, booking intent creation, and payment intent creation there behind the same transitional wildcard import pattern, rewired `backend/api/v1_booking_routes.py`, and verified compile health across the current v1 route modules and split route test files with `python3 -m py_compile`
- Started the dependency cleanup pass for extracted v1 handlers: replaced `from api.v1_routes import *` with explicit `from api.v1_routes import (...)` imports in `backend/api/v1_booking_handlers.py`, `backend/api/v1_search_handlers.py`, `backend/api/v1_communication_handlers.py`, and `backend/api/v1_integration_handlers.py`, then re-ran `python3 -m py_compile` across the v1 route stack; `backend/api/v1_tenant_handlers.py` remains on the wildcard import temporarily because it is still the largest extracted module and should be narrowed in a follow-up pass
- Completed the tenant handler cleanup follow-up: removed the wildcard import from `backend/api/v1_tenant_handlers.py`, replaced it with explicit imports plus direct `datetime` imports, and verified compile health for the tenant route stack and split tenant or portal tests with `python3 -m py_compile`
- Split the remaining `backend/tests/test_api_v1_routes.py` monolith by bounded context: moved the leftover booking assertions into `backend/tests/test_api_v1_booking_routes.py`, communications assertions into `backend/tests/test_api_v1_communication_routes.py`, and integrations assertions into `backend/tests/test_api_v1_integration_routes.py`; `backend/tests/test_api_v1_routes.py` is now only a legacy placeholder, and compile checks passed across the full split route test set with `python3 -m py_compile`

## Homepage Refinement

- Updated the public homepage top-of-page structure so `frontend/src/components/landing/Header.tsx` now supports a homepage-specific compact `menu-only` mode instead of a sticky full topbar
- Added `frontend/src/components/landing/sections/HomepageBrandStatementSection.tsx` to make the homepage open with a larger, cleaner BookedAI logo-first section before the hero content
- Simplified `frontend/src/components/landing/sections/HeroSection.tsx` and `frontend/src/components/landing/data.ts` by trimming hero copy, removing the extra `Proof stack` and `Flow status` blocks, and keeping the hero focused on CTA plus the live product preview
- Verified the frontend build passes after the homepage changes with `npm run build` from `frontend/`
- Polished the same homepage pass again by making the logo-first section feel more premium with a cleaner framed surface, larger logo scale, and tighter supporting signals, while also upgrading the compact homepage menu icon to a more polished glass-button treatment
- Refined the hero again so the live preview now shows the full BookedAI logo lockup with slogan instead of a square-only mark, removed the extra `Website enquiries / Call capture / Booking handoff` chips, and replaced them with cleaner, lighter summary cards that are easier to scan on first glance
- Expanded the homepage refresh into a broader sales-deck pass: enlarged and re-centered the top homepage logo section, extended the hero phone demo to show booking input plus post-book Stripe or calendar or email-ready states, redesigned the `Homepage at a glance` and `Conversion command center` sections for clearer scanability, added an architecture-summary sketch near the top of the architecture section, restored partner and customer logo proof plus a lighter logo wall, reintroduced the team section into the homepage flow, and added section-level CTA buttons in the refreshed overview, solution, architecture, partners, and team sections
- Refined the compact homepage menu again so the `compactMenuOnly` header now opens a smaller anchored dropdown instead of a large screen-covering overlay, with icon-first navigation tiles, shorter labels, and a tighter quick-action area for roadmap, tenant, sales, and trial access
- Started the `backend/services.py` decomposition with a low-risk geo slice: added `backend/service_layer/geo_utils.py`, moved Google Maps URL, map snapshot, geocode query, Nominatim geocoding, and haversine helpers there with backward-compatible aliases, updated `backend/services.py` to import those helpers instead of defining them inline, exported the new helpers from `backend/service_layer/__init__.py`, and verified compile health for the affected backend modules with `python3 -m py_compile`
- Continued the `backend/services.py` decomposition with a catalog asset slice: added `backend/service_layer/catalog_assets.py`, moved category or service image maps plus `resolve_service_image_url` there, updated `backend/services.py` to import the resolver instead of owning the asset tables inline, exported the resolver from `backend/service_layer/__init__.py`, and verified compile health for the affected backend modules with `python3 -m py_compile`
- Reoriented the next `backend/services.py` decomposition slices around the user-prioritized WEBSITE, CALLS, and FOLLOW-UP lanes, then completed the WEBSITE helper extraction first: added `backend/service_layer/website_import_utils.py`, moved HTML text extraction, related-page discovery, product-page discovery, URL detection, search-result URL parsing, JSON-LD flattening and extraction, price extraction, business-context extraction, structured-service extraction, and imported-image resolution helpers there with backward-compatible aliases, updated `backend/services.py` to import those helpers instead of defining them inline, exported the public website parsing helpers from `backend/service_layer/__init__.py`, and verified compile health with `python3 -m py_compile`
- Continued the earlier improvements with a FOLLOW-UP slice from `backend/services.py`: added `backend/service_layer/followup_copy.py`, moved booking manual follow-up URL generation plus booking, consultation, and demo confirmation or internal notification copy builders there, rewired `backend/services.py` methods to delegate to the new helper module, exported the new helpers from `backend/service_layer/__init__.py`, and verified compile health with `python3 -m py_compile`
- Added the `service_query_parsing` slice to continue decomposing backend services by context: created `backend/service_layer/service_query_parsing.py`, moved service query signal parsing, tokenization, preferred-location extraction, and the category or synonym or follow-up keyword tables there with backward-compatible aliases, updated `backend/services.py` to import those parsing helpers instead of owning them inline, exported the new parsing helpers from `backend/service_layer/__init__.py`, and verified compile health with `python3 -m py_compile`
- Continued the context-based backend service split with a CALLS or meeting-scheduling slice: added `backend/service_layer/calls_scheduling.py`, moved customer portal URL, QR code URL, amount formatting, Google Calendar link building, Zoho calendar configuration checks, Zoho access-token retrieval, and Zoho meeting-event creation there, rewired `backend/services.py` methods to delegate to the new scheduling helpers, exported the new helpers from `backend/service_layer/__init__.py`, and verified compile health with `python3 -m py_compile`
- Continued the context-based backend service split with a first `booking_assistant_runtime` slice: added `backend/service_layer/booking_assistant_runtime.py`, moved effective-query building, recent-user-context extraction, service and event curation, category-intent shortlist filtering, service-match gating, and clarification-prompt generation there with backward-compatible aliases, rewired `backend/services.py` to use the extracted runtime helpers instead of owning those methods inline, exported the new helpers from `backend/service_layer/__init__.py`, and verified compile health with `python3 -m py_compile`

## Frontend Hardening Follow-Up

- Added broader frontend scan-path blocking for php, backup, temp, and config-like filenames so config.php, phpinfo.php, config.js, aws-config.js, and config.php.bak now return 404 instead of SPA fallback HTML
- Rebuilt web and beta-web after the hardening pass and verified bookedai.au still returns 200 while api.bookedai.au/api/health stays healthy
- Fixed tenant auth runtime regression in backend/api/v1_tenant_handlers.py by explicitly importing private helpers that star-import did not include
- Fixed a duplicate request argument syntax error in backend/api/route_handlers.py that blocked backend rebuilds
- Rebuilt backend and beta-backend, refreshed proxy upstream routing, and verified tenant password auth now returns 401 for bad credentials instead of 500 or 502

## Repo Cleanup Baseline

- Tightened .gitignore for transient local artifacts including .codex-tmp-venv, .pytest_cache, tmp_pg, tsconfig.tsbuildinfo, and temporary ACME challenge files
- Removed leftover local cleanup noise so the remaining worktree is mostly intentional source, docs, runtime-surface, and backend modularization delta
- Confirmed the next cleanup step is no longer transient-file removal, but deciding how to package and review the large intentional code and document change set

## Homepage Pricing Refresh

- Updated the public-facing landing and register-interest pricing copy away from the earlier free-setup launch language to a clearer `49$+` entry-price message, including homepage pricing signals, starter plan copy, footer summary, and supporting public CTA surfaces; verified the frontend build still passes with `npm run build`

## Homepage First-Screen Polish

- Applied a final homepage first-screen polish pass focused on mobile spacing and premium visual balance: tightened vertical rhythm in the brand-intro and hero copy blocks, reduced visual weight on the right-side phone-demo cards with softer borders, lighter shadows, and cleaner spacing, and verified the frontend build still passes with `npm run build`

## Production Redeploy

- Ran the full production redeploy from the VPS with `sudo bash scripts/deploy_production.sh`, which rebuilt and restarted the production and beta web/backend images, restarted the proxy, and reprovisioned n8n workflows
- Verified post-deploy health: `https://api.bookedai.au/api/health` returned `{"status":"ok","service":"backend"}`, and the core public surfaces returned live HTTP success codes including homepage, product, beta, admin, tenant, demo, portal, Hermes, upload, and n8n; `calendar.bookedai.au` returned the expected redirect response
