- synced local `main` after confirming `origin/main` had no newer commits; local remained ahead with a dirty worktree and no destructive git commands were used
- reviewed and recovered portal continuation: live UAT exposed valid `v1-*` booking references failing in `portal.bookedai.au`; optional enrichment now degrades safely, portal auto-login smoke asserts real workspace render, and live `v1-db55e991fd` verification passed
- reviewed and polished tenant surfaces: unauthenticated tenant/Future Swim UAT passed core checks, then follow-ups shipped for email-code accessible name stability, buyer-facing access/trust posture, hidden raw runtime labels, and compressed Future Swim mobile detail
- reviewed and polished product/homepage booking flow: live UAT created bookings including `v1-c71906524b` and `v1-db55e991fd`; follow-ups improved semantic/accessibility posture, mobile prompt layout, price-trust copy, and customer-safe automation warning copy
- reviewed live admin UAT on `admin.bookedai.au/admin`: health, negative login, real login, and 10/10 workspaces passed across desktop/tablet/mobile with only `200` API responses and no console/page/request failures
- implemented admin investor/SME polish: calmer mobile top bar/session layout, KPI definitions, booking search/table containment, and Reliability language changed from prompt-internal wording to AI quality/automation wording; local Playwright evidence is under `output/playwright/admin-polish-local-2026-04-26/`
- published the latest admin polish closeout to Notion and Discord via `python3 scripts/telegram_workspace_ops.py sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/014420-admin-investor-sme-polish-follow-up.md`
- hardened the checked-in production deploy script to build backend/web/beta-web sequentially by default and bring up already-built images, matching the memory-safe deploy path that avoided frontend Docker build `143`
- added a tenant-smoke Playwright lane to the frontend scripts and root release gate so tenant gateway copy plus email-code accessibility remain part of promote-or-hold verification
- fixed and redeployed the portal valid-reference UAT blocker for `v1-2fd9f35965`; live portal detail and care-turn now return `200`, browser console is clean, and mobile overflow remains `0`
- deploy recovered after a transient Hermes container-name conflict; production compose now shows backend/web/beta/proxy/hermes/n8n running and `bash scripts/healthcheck_stack.sh` passes
- published the portal fix closeout to Discord via `python3 scripts/telegram_workspace_ops.py sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/014701-portal-valid-reference-production-fix.md`, while Notion returned HTTP `403`
- aligned the homepage chess proof prompt with the verified tenant runtime: quick prompt now targets `Book Co Mai Hung Chess Sydney pilot class this week`, and the proof row names `Co Mai Hung Chess / Verified tenant booking / Grandmaster proof`
- live prompt smoke confirmed the new chess prompt returns a Co Mai Hung tenant-backed result with no public-web fallback warning, no console/request failures, and no horizontal overflow; proxy restart plus n8n workflow activation were completed manually after the deploy script returned late with a syntax closeout error despite containers starting
- shipped admin investor/SME polish live by copying the verified `frontend/dist` bundle into the production and beta web containers after Docker frontend rebuilds were killed; live admin browser UAT passed desktop/mobile login, Reliability copy, console/page-error, and horizontal-overflow checks with evidence under `output/playwright/admin-live-polish-2026-04-26/`
- implemented the next tenant UAT recommendation as measurable A/B telemetry: `tenant_variant=control|revenue_ops`, persisted assignment, revenue-ops gateway headline variant, and tenant funnel events for auth, Google/email-code, panel, and mobile-detail interactions
- verification for tenant A/B telemetry passed with frontend typecheck, production build, and `cd frontend && npx playwright test tests/tenant-gateway.spec.ts --workers=1 --reporter=line`
- deployed tenant A/B telemetry live; stack health passed and live mobile smoke on `https://tenant.bookedai.au/?tenant_variant=revenue_ops` confirmed headline, persisted variant, event emission, and no `390px` overflow
- implemented the next portal UAT recommendation as measurable action-IA telemetry: `portal_variant=control|status_first`, status-first default action order, local/dataLayer/custom-event funnel events, and customer-facing Pay/Help/Change-plan states
- portal verification passed with frontend typecheck, production build, and focused Playwright portal coverage for status-first order, telemetry, reschedule submission, and mobile no-overflow behavior
- deployed the portal status-first A/B update live; production smoke on `portal.bookedai.au` confirmed labels `Status/Pay/Reschedule/Ask for help/Change plan/Cancel`, loaded/pay-click telemetry, no console/request failures, stack health pass, and mobile overflow `0`
- published the portal status-first A/B closeout to Notion and Discord via `python3 scripts/telegram_workspace_ops.py sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/021419-portal-status-first-a-b-telemetry-live.md`
- enabled live tenant admin access for Co Mai Hung Chess: invited `chess@bookedai.au`, then repaired the broken claim path by inserting the password credential directly with Supabase superuser access so `chess@bookedai.au` / `[REDACTED_TENANT_PASSWORD]` now logs into tenant `co-mai-hung-chess-class` as `tenant_admin`
- ran a live admin health/UAT pass against `admin.bookedai.au`: web app and core authenticated admin APIs returned healthy responses; detailed note saved to `docs/development/admin-live-uat-2026-04-26.md`
- follow-up tenant admin access verified: `chess@bookedai.au` on tenant `co-mai-hung-chess-class` remains the active `tenant_admin` login with password `[REDACTED_TENANT_PASSWORD]`, after the live claim endpoint path proved unreliable and the credential was repaired directly in Postgres
- live admin regression follow-up confirmed `https://admin.bookedai.au/admin` serves and authenticates correctly, with healthy responses across overview, bookings, messaging, services, partners, tenants, config, and tenant detail showing Co Mai Hung membership state
- repaired local Playwright admin smoke runtime by supplying Debian browser libraries through a workspace-scoped `LD_LIBRARY_PATH`; after that, the admin smoke suite passed and the only failure found was a stale Reliability test expectation caused by the newer AI quality/automation copy and updated action labels
- synced local `main` after confirming `origin/main` had no newer commits; local remained ahead with a dirty worktree and no destructive git commands were used
- reviewed and recovered portal continuation: live UAT exposed valid `v1-*` booking references failing in `portal.bookedai.au`; optional enrichment now degrades safely, portal auto-login smoke asserts real workspace render, and live `v1-db55e991fd` verification passed
- reviewed and polished tenant surfaces: unauthenticated tenant/Future Swim UAT passed core checks, then follow-ups shipped for email-code accessible name stability, buyer-facing access/trust posture, hidden raw runtime labels, and compressed Future Swim mobile detail
- reviewed and polished product/homepage booking flow: live UAT created bookings including `v1-c71906524b` and `v1-db55e991fd`; follow-ups improved semantic/accessibility posture, mobile prompt layout, price-trust copy, and customer-safe automation warning copy
- reviewed live admin UAT on `admin.bookedai.au/admin`: health, negative login, real login, and 10/10 workspaces passed across desktop/tablet/mobile with only `200` API responses and no console/page/request failures
- implemented admin investor/SME polish: calmer mobile top bar/session layout, KPI definitions, booking search/table containment, and Reliability language changed from prompt-internal wording to AI quality/automation wording; local Playwright evidence is under `output/playwright/admin-polish-local-2026-04-26/`
- published the latest admin polish closeout to Notion and Discord via `python3 scripts/telegram_workspace_ops.py sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/014420-admin-investor-sme-polish-follow-up.md`
- hardened the checked-in production deploy script to build backend/web/beta-web sequentially by default and bring up already-built images, matching the memory-safe deploy path that avoided frontend Docker build `143`
- added a tenant-smoke Playwright lane to the frontend scripts and root release gate so tenant gateway copy plus email-code accessibility remain part of promote-or-hold verification
- fixed and redeployed the portal valid-reference UAT blocker for `v1-2fd9f35965`; live portal detail and care-turn now return `200`, browser console is clean, and mobile overflow remains `0`
- deploy recovered after a transient Hermes container-name conflict; production compose now shows backend/web/beta/proxy/hermes/n8n running and `bash scripts/healthcheck_stack.sh` passes
- published the portal fix closeout to Discord via `python3 scripts/telegram_workspace_ops.py sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/014701-portal-valid-reference-production-fix.md`, while Notion returned HTTP `403`
- aligned the homepage chess proof prompt with the verified tenant runtime: quick prompt now targets `Book Co Mai Hung Chess Sydney pilot class this week`, and the proof row names `Co Mai Hung Chess / Verified tenant booking / Grandmaster proof`
- live prompt smoke confirmed the new chess prompt returns a Co Mai Hung tenant-backed result with no public-web fallback warning, no console/request failures, and no horizontal overflow; proxy restart plus n8n workflow activation were completed manually after the deploy script returned late with a syntax closeout error despite containers starting
- shipped admin investor/SME polish live by copying the verified `frontend/dist` bundle into the production and beta web containers after Docker frontend rebuilds were killed; live admin browser UAT passed desktop/mobile login, Reliability copy, console/page-error, and horizontal-overflow checks with evidence under `output/playwright/admin-live-polish-2026-04-26/`
- implemented the next tenant UAT recommendation as measurable A/B telemetry: `tenant_variant=control|revenue_ops`, persisted assignment, revenue-ops gateway headline variant, and tenant funnel events for auth, Google/email-code, panel, and mobile-detail interactions
- verification for tenant A/B telemetry passed with frontend typecheck, production build, and `cd frontend && npx playwright test tests/tenant-gateway.spec.ts --workers=1 --reporter=line`
- deployed tenant A/B telemetry live; stack health passed and live mobile smoke on `https://tenant.bookedai.au/?tenant_variant=revenue_ops` confirmed headline, persisted variant, event emission, and no `390px` overflow
- implemented the next portal UAT recommendation as measurable action-IA telemetry: `portal_variant=control|status_first`, status-first default action order, local/dataLayer/custom-event funnel events, and customer-facing Pay/Help/Change-plan states
- portal verification passed with frontend typecheck, production build, and focused Playwright portal coverage for status-first order, telemetry, reschedule submission, and mobile no-overflow behavior
- deployed the portal status-first A/B update live; production smoke on `portal.bookedai.au` confirmed labels `Status/Pay/Reschedule/Ask for help/Change plan/Cancel`, loaded/pay-click telemetry, no console/request failures, stack health pass, and mobile overflow `0`
- published the portal status-first A/B closeout to Notion and Discord via `python3 scripts/telegram_workspace_ops.py sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/021419-portal-status-first-a-b-telemetry-live.md`
- enabled live tenant admin access for Co Mai Hung Chess: invited `chess@bookedai.au`, then repaired the broken claim path by inserting the password credential directly with Supabase superuser access so `chess@bookedai.au` / `[REDACTED_TENANT_PASSWORD]` now logs into tenant `co-mai-hung-chess-class` as `tenant_admin`
- ran a live admin health/UAT pass against `admin.bookedai.au`: web app and core authenticated admin APIs returned healthy responses; detailed note saved to `docs/development/admin-live-uat-2026-04-26.md`
- follow-up tenant admin access verified: `chess@bookedai.au` on tenant `co-mai-hung-chess-class` remains the active `tenant_admin` login with password `[REDACTED_TENANT_PASSWORD]`, after the live claim endpoint path proved unreliable and the credential was repaired directly in Postgres
- live admin regression follow-up confirmed `https://admin.bookedai.au/admin` serves and authenticates correctly, with healthy responses across overview, bookings, messaging, services, partners, tenants, config, and tenant detail showing Co Mai Hung membership state
- repaired local Playwright admin smoke runtime by supplying Debian browser libraries through a workspace-scoped `LD_LIBRARY_PATH`; after that, the admin smoke suite passed and the only failure found was a stale Reliability test expectation caused by the newer AI quality/automation copy and updated action labels
- finished the local admin smoke follow-up by updating `frontend/tests/admin-prompt5-preview.spec.ts` to match current Reliability and tenant-workspace UI behavior; focused reruns for the last 2 failing cases passed after switching brittle text/element assertions to container-scoped checks compatible with sticky layout and duplicate hidden nodes
- ran live tenant preview UAT against `https://tenant.bookedai.au/future-swim` and `https://tenant.bookedai.au/co-mai-hung-chess-class` on desktop/mobile; preview shell, section navigation, and API-backed data all loaded with no horizontal overflow, with evidence under `output/playwright/tenant-live-uat-2026-04-26/`
- captured tenant catalog/data truth for live UAT: Future Swim currently shows `6/6` search-ready published records with valid image URLs and mixed pricing copy, while Co Mai Hung Chess shows only `1/9` search-ready and most rows remain `review`/inactive without booking URLs
- documented the tenant live UAT findings in `docs/development/tenant-live-uat-2026-04-26.md`; current open risks are incomplete billing posture on both tenants, preview mode exposing broad operator metrics before sign-in, and Future Swim outbound booking URLs returning `406` to raw HEAD probes pending normal-browser verification
- continued live tenant UAT with Playwright against `https://tenant.bookedai.au/future-swim` and `https://tenant.bookedai.au/co-mai-hung-chess-class`; captured desktop/mobile evidence under `output/playwright/tenant-live-uat-2026-04-26/` and summarized findings in `docs/development/tenant-live-uat-2026-04-26.md`
- live preview shell rendered broadly across Overview, Experience Studio, Catalog, Plugin, Bookings, Enquiries/Leads, Ops, Integrations, Billing, and Team with no horizontal overflow in the captured desktop/mobile passes
- Future Swim preview looked materially healthier than Co Mai Hung Chess: `future-swim` showed `6/6` search-ready catalog rows with working image responses and realistic mixed pricing, while `co-mai-hung-chess-class` showed only `1/9` search-ready and most rows still in review/inactive state
- both tenant previews still expose unfinished commercial posture, especially billing setup not started; Future Swim outbound booking links also returned `406` to a raw HEAD probe and need follow-up verification with normal browser GET behavior before treating that as a user-facing bug
- continued tenant live UAT with both next steps: chess auth initiation now verified via email-code flow on `https://tenant.bookedai.au/` for `chess@bookedai.au` with `200` from `/api/v1/tenant/auth/email-code/request`, but full sign-in still needs inbox/code access because the visible production tenant surface is email-code-first and did not expose the prior password path during this pass
- browser-style GET verification replaced the earlier HEAD-only uncertainty for outbound tenant links: Future Swim `caringbah` and `leichhardt` booking URLs resolve `200`, but published catalog row `https://futureswim.com.au/locations/miranda/` resolves to a real `404`; the published chess pilot booking URL `https://bookedai.au/?assistant=open` resolves `200`
- expanded `docs/development/tenant-live-uat-2026-04-26.md` with auth findings, outbound link verification, and a dedicated 12-case chess UAT matrix covering preview, auth, catalog readiness, billing, team access, booking link, and mobile layout
- prepared `backend/migrations/sql/020_future_swim_miranda_booking_url_hotfix.sql` after live GET verification proved the published Future Swim Miranda booking URL `https://futureswim.com.au/locations/miranda/` is a real `404`; intended replacement is the live locations page `https://futureswim.com.au/locations/`
- attempted to apply the Miranda hotfix live, but the current runtime did not have `psql`, `sudo`, or a usable `supabase-db` Docker context, so the repo-side fix is ready while live apply remains blocked on host database access
- resumed live WhatsApp full-flow testing with the QA-VI skill and `python3 scripts/telegram_workspace_ops.py whatsapp-bot-status --api-base https://api.bookedai.au --bot-base https://bot.bookedai.au`; readiness showed backend/webhook `200`, primary live provider posture around `whatsapp_meta`, and fallback configured as `whatsapp_evolution`
- confirmed code path from `backend/service_layer/communication_service.py`: WhatsApp send tries primary `meta`, then falls back to `evolution` when primary returns queued/unavailable; inbound webhook route still accepts both Twilio-style form posts and Meta JSON payloads
- live outbound send-path probe reached the API successfully but did not deliver: response stayed `delivery_status="queued"`, provider resolved to `whatsapp_evolution`, and warnings showed primary `whatsapp_meta` unavailable followed by Evolution `500 Internal Server Error` / `Connection Closed`
- live inbound webhook probe to `https://api.bookedai.au/api/webhooks/whatsapp` returned `200` with `{"status":"processed","messages_processed":1}`, so receive/logging path is currently alive while reply/send is blocked at the provider layer
- current WhatsApp conclusion: full bot flow is not end-to-end healthy yet because inbound receive works but outbound delivery/reply is broken by provider integration state; next follow-up should inspect live `WHATSAPP_PROVIDER` / fallback secrets plus direct Meta and Evolution connectivity
- widened tenant sign-in on `tenant.bookedai.au` so `TenantAuthWorkspaceEmail` now offers both `Email code` and `Email and password` sign-in cards in `sign-in` mode, wired through `TenantApp.tsx` to the existing `/api/v1/tenant/auth/password` backend route; verified with `cd frontend && npm run build`
- repaired the live chess tenant password lane without direct DB access: `POST /api/v1/tenant/account/claim` returned `500` when `username` matched the email, but succeeded when claimed as `username="chess"` with `email="chess@bookedai.au"`; after that, `POST /api/v1/tenant/auth/password` succeeded for `username="chess@bookedai.au"`, `password="[REDACTED_TENANT_PASSWORD]"`, `tenant_ref="co-mai-hung-chess-class"`
- followed up the WhatsApp outbound blocker: backend Evolution sendText handling now keeps the v2 `text` payload and guarded legacy retry, provider readiness is less optimistic for Evolution fallback, and the touched backend files were hot-patched into live backend/beta-backend containers without a full dirty-worktree deploy
- live probes after the patch still queue outbound messages under `whatsapp_evolution`; direct internal Evolution probe shows instance `bookedai61481993178` is `connecting`, so the remaining blocker is QR/session reconnection rather than backend payload shape
- updated `project.md`, `docs/development/implementation-progress.md`, `docs/development/next-phase-implementation-plan-2026-04-25.md`, and `docs/architecture/current-phase-sprint-execution-plan.md` with the Phase 19 WhatsApp provider/session state
- added `python3 scripts/telegram_workspace_ops.py fix-openclaw-approvals` to repair OpenClaw exec approval policy when `allow-always` fails under effective `ask: always`
- the repair aligns `openclaw.json` `tools.exec.ask` and host `exec-approvals.json` defaults to `on-miss`, preserving allowlist prompts while making durable approvals valid again
- updated OpenClaw/operator docs and env action vocabulary with the new `openclaw_runtime_admin` scope; live apply still needs to run from the privileged OpenClaw CLI workspace because the default shell cannot read `/home/dovanlong/.openclaw-bookedai-v3/openclaw.json`
- published the OpenClaw approval repair closeout to Notion and Discord via `python3 scripts/telegram_workspace_ops.py sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/054404-openclaw-approval-policy-repair-command.md`
- added `python3 scripts/telegram_workspace_ops.py enable-openclaw-full-access` for the operator-requested full OpenClaw posture from `bot.bookedai.au` webchat and Telegram
- the full-access command sets exec to gateway/full/off, mirrors host approval defaults to full/off, enables elevated full by default, allows webchat elevated from `*`, and keeps Telegram scoped to trusted ids unless `--telegram-open` is passed
- published the full OpenClaw webchat/Telegram access command closeout to Notion and Discord; archive entry is `docs/development/telegram-sync/2026-04-26/054737-openclaw-full-webchat-telegram-access-command.md`
- operator asked to temporarily remove Evolution from outbound WhatsApp; `.env` now sets `WHATSAPP_FALLBACK_PROVIDER=twilio`, backend/beta-backend were recreated with the new env, and current live send path is Meta primary then Twilio fallback
- direct provider probes show Meta Graph can read `+61 455 301 335` / `BookedAI.au`, but Meta send returns `(#133010) Account not registered`; Twilio Account and Messages calls return `401 Authenticate`, so outbound remains queued/manual-review until provider activation/credentials are repaired
- user-requested send-data test reran both backend communication routes: WhatsApp API probe recorded `e440e489-6bb5-42ff-aa64-d6c1ed791cc2` as `queued` via `whatsapp_twilio`, SMS/Twilio probe recorded `e9694927-2b1e-48a9-ac72-8418e61bc57c` as `queued` via `sms_twilio`; direct provider probes reconfirmed Meta `Account not registered` and Twilio `401 Authenticate`
- operator then asked to make Twilio the default chat path; `.env` now has `WHATSAPP_PROVIDER=twilio` and empty `WHATSAPP_FALLBACK_PROVIDER`, backend/beta-backend were recreated, stack health passed, and Twilio-only WhatsApp probe `87f12c98-e867-4bb1-a06d-21ba98ffabf0` queued via `whatsapp_twilio`
- user-requested BookedAI booking agent WhatsApp probe from configured sender `+61455301335` to `+61481993178` recorded `ebecbf49-2f05-449d-9137-001745f96d7b` as `queued` via `whatsapp_twilio`; provider still reports delivery unavailable/manual-review
- Twilio WhatsApp Auth Token was configured live for Account SID ending `9758` and API-key auth was removed from the WhatsApp path; Twilio account auth now passes as active Trial account `BookedAI.au`
- direct Twilio probe from configured sender `whatsapp:+61455301335` to `whatsapp:+61481993178` fails with `63007` (`Twilio could not find a Channel with the specified From address`), confirming `+61455301335` is not registered as a Twilio WhatsApp sender
- direct Twilio sandbox probe from `whatsapp:+14155238886` to `whatsapp:+61481993178` succeeds with Twilio SID `SMc9fb54c0be9b33e70c00974aeda6c2af` in `queued` state, so credentials and recipient sandbox path are working
- user supplied Twilio API key `SK...a9a6`; live test against Account SID ending `9758` returned Twilio `401 Authenticate`, so it was not compatible with this account/path; restored the working Auth Token config and reconfirmed account auth + stack health
- operator requested Meta as the official chat connection with Twilio default sender fallback; live `.env` now uses `WHATSAPP_PROVIDER=meta`, `WHATSAPP_FALLBACK_PROVIDER=twilio`, `WHATSAPP_META_PHONE_NUMBER_ID=1033646899839617`, and `WHATSAPP_FROM_NUMBER=+14155238886`
- explicit Meta routing note: use `Bookedai.au1` / `+1 555-939-9566` / phone number id `1033646899839617` for Meta Cloud API, not the old `+61 455 301 335` Meta phone id
- post-change probe from the BookedAI WhatsApp agent to `+61481993178` recorded `b683d9ef-1c40-4b17-b92a-d5e1e0bb9586`, fell back from Meta to Twilio, and received Twilio SID `SMd5e0be05f296371308e42ad4fa2cc179` in queued state
- user-requested configured WhatsApp chat test recorded `8fae24ce-2cad-4f61-be42-4e3c1276a76d`, fell back from Meta to Twilio, and received Twilio SID `SMd864cecbec3b28dc7f95e77070cd38ed` in queued state
- rebuilt and redeployed the live production stack through `python3 scripts/telegram_workspace_ops.py deploy-live`; backend, beta-backend, web, and beta-web images built successfully, production containers were recreated, proxy restarted, and n8n workflow activation completed
- post-deploy verification passed: `bash scripts/healthcheck_stack.sh`, `https://api.bookedai.au/api/health`, and `https://tenant.bookedai.au/` all returned healthy responses
- live tenant browser smoke on `https://tenant.bookedai.au/?tenant_variant=revenue_ops` confirmed the revenue-ops headline, persisted variant/event telemetry, zero console warnings/errors, and `390px` mobile overflow `0`
- aligned the bookedai.au default email sender/support identity on `info@bookedai.au`: SMTP username/from now default there when env values are omitted, portal/customer-care support fallbacks use the same address, and focused backend tests passed
- fixed and redeployed `tenant.bookedai.au/2017-btc` login CTA recovery: `Open tenant sign-in` and activation `Open sign-in` now open the catalog auth workspace, logout resets auth mode to `sign-in`, and live smoke confirmed password sign-in enables after input with no horizontal overflow
- replaced the embedded pitch video on `bookedai.au` and `pitch.bookedai.au` with `https://upload.bookedai.au/videos/df25/8woKUebBF8HMMD_RMSA0LQ.mp4`; direct byte-range probe returned `206 video/mp4`
- deployed the video replacement live, verified both production bundles reference the new URL, stack health passed, and sync-doc published the closeout to Notion/Discord at `docs/development/telegram-sync/2026-04-26/080831-public-and-pitch-video-replacement-deployed.md`
- added the shared Messaging Automation Layer foundation in `backend/service_layer/messaging_automation_service.py`, moving customer-care policy out of WhatsApp-only route handling
- added Telegram-first customer webhook support at `/api/webhooks/telegram` with optional secret-token verification, Telegram Bot API replies, and `telegram_inbound` Inbox events
- WhatsApp inbound paths now call the same shared messaging agent policy for conversation window, booking-reference resolution, booking intake, portal-grounded care answers, and audited cancel/reschedule request queuing
- updated project/progress/phase/sprint/README/DESIGN docs plus env examples; verification passed for py_compile and focused communication/webhook tests
- published the Messaging Automation Telegram-first closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/103831-messaging-automation-telegram-first-foundation.md`
- extended Telegram into the first customer-facing representative flow: service-search replies now return catalog options plus web booking continuation, and `Book 1` with contact details can create a real contact/lead/booking intent/reference/portal link
- focused Telegram/WhatsApp webhook regression passed after the search/chat-booking extension
- published the Telegram search-and-booking closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/104957-telegram-messaging-automation-search-and-booking.md`
- clarified that the BookedAI Booking AI Agent is a separate customer-facing runtime, not the OpenClaw/operator Telegram bot for programming/deploying this repo; customer Telegram now uses `BOOKEDAI_CUSTOMER_TELEGRAM_*` env names and `/api/webhooks/bookedai-telegram`
- verification after the separation passed with backend py_compile and the focused communication/webhook regression (`23 passed`)
- published the customer Booking AI Agent separation closeout to Notion and Discord; archive entry is `docs/development/telegram-sync/2026-04-26/105614-customer-booking-ai-agent-separated-from-openclaw.md`
- named the customer-facing chat agent `BookedAI Manager Bot`; Telegram display name should be `BookedAI Manager Bot`, preferred username `@BookedAI_Manager_Bot`, with `@BookedAIBookingBot`, `@BookedAIServiceBot`, and `@BookedAIHelpBot` as fallbacks
- published the agent naming closeout to Notion and Discord; archive entry is `docs/development/telegram-sync/2026-04-26/105937-bookedai-concierge-agent-naming.md`
- added the omnichannel AI Revenue Engine product requirement message across `project.md`, `README.md`, `DESIGN.md`, implementation progress, next-phase plan, and current phase/sprint plan
- mapped the requirement into Phase 19-23: omnichannel intake/status care, widget/web-chat distribution, payment/receivable continuity, reusable retention templates, and release-gated capture-to-retention verification
- published the closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/104557-omnichannel-ai-revenue-engine-product-requirement-and-phase-plan.md`
- user finalized the customer-facing chat agent name as `BookedAI Manager Bot`; do not store the supplied Telegram token in repo files, docs, memory, or logs, and use `BOOKEDAI_CUSTOMER_TELEGRAM_BOT_TOKEN` only in the live secret environment
- published the final naming closeout to Notion and Discord; archive entry is `docs/development/telegram-sync/2026-04-26/111314-bookedai-manager-bot-final-naming.md`
- user corrected the Telegram handle to `@BookedAI_Manager_Bot`; updated service metadata, tests, env comments, and docs to use the underscore handle
- connected `BookedAI Manager Bot` Telegram service search to the bookedai.au chat response shape, added a `Find more on Internet near me` expansion action through the BookedAI public search service, and tightened booking lookup identity to explicit reference or safe phone/email match
- verification passed with backend py_compile and focused communication/webhook/search regression (`26 passed`); sync-doc archive is `docs/development/telegram-sync/2026-04-26/112541-bookedai-manager-bot-service-search-and-internet-expansion.md`
- aligned channel entrypoints per user request: website chat now calls `POST /api/chat/send` directly, Telegram remains on `POST /api/webhooks/telegram` or `/api/webhooks/bookedai-telegram` and replies through Telegram `sendMessage`; legacy booking-assistant chat routes stay as compatibility aliases
- entrypoint alignment verification passed with backend py_compile, chat-send plus Telegram webhook tests (`7 passed`), and frontend `tsc --noEmit`; sync-doc archive is `docs/development/telegram-sync/2026-04-26/113132-bookedai-chat-and-telegram-entrypoints-aligned.md`
- added `scripts/customer_telegram_bot_manager.py` to activate/test `BookedAI Manager Bot` from env secrets, including recent-chat lookup for `chat_id`; live send is blocked until `BOOKEDAI_CUSTOMER_TELEGRAM_BOT_TOKEN` is present in the secret environment and a Telegram `chat_id` is available from a user who started the bot
- published the blocked activation helper closeout to Notion/Discord; archive entry is `docs/development/telegram-sync/2026-04-26/113501-bookedai-manager-bot-activation-helper-added.md`
- published the recent-chat lookup helper update to Notion/Discord; archive entry is `docs/development/telegram-sync/2026-04-26/113612-bookedai-manager-bot-recent-chat-lookup-added.md`
- clarified and enforced the surface split: website demo/product chat is a public web-user search surface across BookedAI data plus Internet expansion, while Telegram is a private customer-thread surface that can search/chat like the website but loads booking-care details only by booking reference or safe phone/email identity, never Telegram chat id alone
- verification passed with backend py_compile, Telegram/chat-send tests (`8 passed`), and frontend `tsc --noEmit`; sync-doc archive is `docs/development/telegram-sync/2026-04-26/114010-bookedai-web-and-telegram-customer-identity-flow-clarified.md`
- created and live-synced OpenClaw manifest `bookedai-booking-customer-agent` (`BookedAI Booking Customer Agent`) for always-on customer requests across web chat and Telegram; it uses OpenAI auth, allows BookedAI-mediated internet search, and has no repo-write/deploy/host-shell authority
- verification confirmed live OpenClaw runtime manifest has `openai_auth=OPENAI_API_KEY`, `internet_search=true`, and `elevated=false`; sync-doc archive is `docs/development/telegram-sync/2026-04-26/114419-openclaw-bookedai-booking-customer-agent-created.md`
- activated `@BookedAI_Manager_Bot` live: token stored in live secret env, Telegram webhook set to `https://api.bookedai.au/api/webhooks/bookedai-telegram`, backend/beta-backend rebuilt with the webhook route/env, controlled sendMessage test succeeded, and internal webhook-to-OpenAI-to-Telegram loop returned `200`
- patched Telegram send logging so new backend logs no longer include Telegram Bot API URLs with tokens; post-rebuild log probe confirmed no `api.telegram.org` token URL was emitted for the latest loop test
- published the live activation closeout to Notion/Discord; archive entry is `docs/development/telegram-sync/2026-04-26/115234-bookedai-manager-bot-activated-live.md`
- completed the user-requested full retest and live redeploy after Telegram activation: py_compile, focused backend regression (`27 passed`), frontend typecheck/build, and the full release gate all passed; `python3 scripts/telegram_workspace_ops.py deploy-live` rebuilt/recreated production and `bash scripts/healthcheck_stack.sh` passed at `2026-04-26T12:05:03Z`
- post-deploy Telegram verification confirmed the customer bot token is set in the backend env, webhook target remains `/api/webhooks/bookedai-telegram` with `pending_update_count=0`, and a fresh internal Telegram webhook event returned `200` / `messages_processed=1`; new backend logs still do not expose Telegram Bot API token URLs
- improved `@BookedAI_Manager_Bot` service-search replies for Telegram: compact BookedAI-style result summaries, quick-book copy per option, inline `Open BookedAI to view and book`, `Book 1/2/3`, and Internet expansion controls; focused tests passed (`27 passed`), live deploy completed, stack health passed, and a fresh internal Telegram webhook probe returned `200`
- completed full UAT for the BookedAI customer booking agent and documented it in `docs/development/customer-booking-agent-uat-2026-04-26.md`; fixed Telegram callback acknowledgement, fixed web chat ranking so `chess class Sydney` no longer ranks swim ahead of chess, aligned lifecycle Settings test constructors, ran full release gate, deployed live, and verified health plus live web/Telegram probes
- fixed the remaining homepage service-booking email path so `/booking-assistant/session` no longer uses provider catalog emails such as Future Swim branch addresses for customer confirmation copy, internal lead notification, follow-up mailto, response contact email, or workflow metadata
- homepage service booking operational email now routes through `BOOKING_BUSINESS_EMAIL`, defaulting to `info@bookedai.au`; focused regression passed with `.venv/bin/python -m pytest backend/tests/test_booking_assistant_service.py -q` (`13 passed`)
- published the homepage service booking email closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/121903-homepage-service-booking-email-recipient-aligned.md`
- refined `BookedAI Manager Bot` Telegram result UX: service-search replies now use compact plain-text option cards plus inline `Open full results`, per-option `View n`, per-option `Book n`, and gated Internet expansion controls
- booking-intent capture replies now include an `Open booking portal` inline button beside the reference/portal URL, preserving the portal-first handoff in Telegram
- verification passed with backend py_compile for messaging/communication/routes and `.venv/bin/python -m pytest backend/tests/test_telegram_webhook_routes.py backend/tests/test_chat_send_routes.py -q` (`9 passed`)
- published the Telegram result UX closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/123646-bookedai-manager-bot-telegram-result-ux.md`
- improved homepage proof-vertical shortcuts: public chips now target Future Swim, Co Mai Hung Chess, and WSTI AI Event / Western Sydney Startup Hub, and the homepage search runtime shows fast catalog/shortcut previews while live ranking continues
- fast-preview keeps the broad `near me` guardrail intact; focused frontend typecheck/build passed and Playwright shortcut regression passed, while a broader existing guardrail grep was terminated with exit `143` before completion
- published the homepage shortcut fast-preview closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/123239-homepage-shortcut-fast-preview.md`
- standardized BookedAI-managed customer booking support/contact defaults on `info@bookedai.au` and `+61455301335` for Telegram, WhatsApp, or iMessage; provider catalog contact details should not override that fallback in customer-booking handoffs
- added backend contact constants/env defaults plus portal/care-turn/communication copy updates; verification passed with backend py_compile, focused backend regression (`51 passed`), and frontend `tsc --noEmit`
- published the customer booking support-contact defaults closeout to Notion and Discord; final archive entry is `docs/development/telegram-sync/2026-04-26/125830-customer-booking-support-contact-defaults-final-note.md`
- tenant-owned homepage/product service bookings now keep `info@bookedai.au` as the BookedAI email identity, CC the saved tenant service email on the internal lead notification, and queue tenant notification through Messaging Automation with Telegram as the default target
- verification passed with backend py_compile and `.venv/bin/python -m pytest backend/tests/test_booking_assistant_service.py -q` (`14 passed`)
- published the tenant booking email/Telegram notification closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/123901-tenant-booking-email-cc-and-telegram-notification.md`
- locked the requested architecture/phase/sprint planning baseline across `project.md`, `README.md`, `DESIGN.md`, `docs/development/implementation-progress.md`, `docs/development/next-phase-implementation-plan-2026-04-25.md`, and `docs/architecture/current-phase-sprint-execution-plan.md`
- current urgent execution order is UI/UX stabilization first, canonical booking flow second, Messaging Automation Layer consolidation third
- canonical booking flow for upcoming work is `Ask -> Match -> Compare -> Book -> Confirm -> Portal -> Follow-up`; Phase/Sprint `17-23` now maps stabilization, ledger control, messaging care, widget runtime, wallet/Stripe return, billing truth, templates, and release governance
- installed verified `>300 installs` global Codex skill candidates for BookedAI work: system design, FastAPI/Python backend, REST API design, Next.js/React/TypeScript, UI design, QA Playwright, DevOps CI/CD, business analysis, startup analyst, and startup financial modeling
- skipped or removed skill candidates below the requested threshold, including direct CEO/CTO/product-manager/solution-architect/OpenAI Agents SDK entries; removed `langchain` after install because the CLI reported `Snyk Critical Risk`
- expanded the global skill set to `>200 installs` candidates relevant to BookedAI: architecting-solutions, openai-agents-sdk, react-19, nextjs-react-redux-typescript-cursor-rules, api-security-hardening, backend-security-coder, frontend-design, c-level-advisor, devops, and stock-analyzer
- attempted additional `>200 installs` candidates that did not install cleanly: `404kidwiz/claude-supercode-skills` clone failed auth for solution-architect/product-manager, and `alirezarezvani/claude-skills` did not expose the searched senior-architect/ceo-advisor basenames through the CLI; installed `c-level-advisor` from that repo instead
- updated Telegram customer booking continuity: `BookedAI Manager Bot` now uses `BookedAI.au`/`https://bookedai.au` copy, includes `https://portal.bookedai.au` plus QR on order handoffs, asks how to handle an existing booking before searching another option, and supports keep-current-booking search with a return-to-order menu
- upgraded Telegram customer replies to HTML-safe rich text plus inline keyboard action cards; the Telegram sender now passes `parse_mode="HTML"` for generated customer bot replies
- verification passed for the Telegram/customer chat lane with backend py_compile, `test_telegram_webhook_routes.py` (`10 passed`), Telegram plus chat-send tests (`11 passed`), and rich Telegram sender coverage (`12 passed` focused lane)
- installed `>300 installs` AI chat design/build skills: `chatgpt-app-builder`, `chat-ui`, `assistant-ui`, `streaming`, `primitives`, `setup`, and `update`; skipped direct chatbot-designer/conversation skills under 300 installs
- removed `assistant-ui/tools` after install because the Skills CLI reported `Gen High Risk`, despite it meeting the install-count threshold
- implemented the Customer Booking Agent UAT follow-ups: `messaging_channel_sessions` now stores compact per-channel shortlist/query/reply-control state, Telegram delivery/callback status snapshots are retained, and `/api/customer-agent/health` plus `/api/admin/customer-agent/health` expose protected operator health for the customer agent
- added `scripts/customer_agent_uat.py` for web-chat plus Telegram webhook/callback UAT probes and added the `chess-class-sydney-live-safe` search eval case; full release gate passed, live deploy completed, stack health passed, live web-chat UAT returned `Kids Chess Class - Sydney Pilot` first, and health returned `200` with a valid admin token
- installed `>300 installs` Testing/QA skills: `test-automator`, `playwright`, `e2e-testing-automation`, `ai-regression-testing`, `visual-regression-testing`, `browserstack`, and `testrail`; `qa-testing-playwright` was already installed
- tried `run-acceptance-tests` for UAT/acceptance coverage but removed it immediately because the Skills CLI reported `Snyk Critical Risk`; direct UAT-planning skills found were below the requested install threshold
- reviewed the source through the installed architecture/backend-security/API-hardening/frontend/devops/AI-agent/chat/testing skill lenses and fixed shared confirmation email HTML rendering so dynamic fields are escaped and unsafe CTA URLs fall back to `https://bookedai.au`
- added regression coverage for script/HTML/image-markup/`javascript:` injection in confirmation email rendering; backend customer-agent regression (`34 passed`) and frontend typecheck passed
- documented the closeout in `docs/development/source-code-review-and-security-hardening-2026-04-26.md`, implementation progress, and the active phase/sprint plan, then published Notion/Discord via sync-doc archive `docs/development/telegram-sync/2026-04-26/135315-source-review-and-confirmation-email-security-hardening.md`
- merged and pushed all current staged project changes to `main` via commits `c894251` and `5bad301`; redacted the live chess tenant password from memory/dream files before committing
- deployed live with `python3 scripts/telegram_workspace_ops.py deploy-live`; backend/web/beta images built, containers recreated, proxy restarted, and n8n workflow activated
- post-deploy verification passed: `bash scripts/healthcheck_stack.sh`, `https://api.bookedai.au/api/health`, `bookedai.au`/`portal.bookedai.au` HEAD checks, and live `/api/chat/send` chess smoke returned `Kids Chess Class - Sydney Pilot` first
- completed the proposed Phase 23 release-gate security follow-up: added `backend/tests/test_release_gate_security.py`, allowlisted Messaging Automation provider URLs to `http/https`, and wired security/chat-send/Telegram/WhatsApp fixtures into `scripts/run_release_gate.sh`
- verification passed with focused pytest (`45 passed`), unittest channel/security lane (`22 tests`), frontend typecheck, and full `RUN_SEARCH_REPLAY_GATE=false bash scripts/run_release_gate.sh` (`all checks passed`); sync-doc archive is `docs/development/telegram-sync/2026-04-26/140825-phase-23-release-gate-security-fixtures-added.md`
- installed `>300 installs` professional writing/marketing/startup voice skills: `content-writer`, `content-marketing`, `startup-validator`, `marketing-plan`, `launch-marketing`, `press-release`, `mailchimp-automation`, `startup-ideation`, `startup-canvas`, and `brainstorm-experiments-new`
- skipped direct human/brand-voice/copywriter skill candidates because the matching search results were below the requested `>300 installs` threshold
- applied those writing/marketing/startup skills to the active BookedAI planning docs: `project.md`, master PRD, implementation roadmap, current phase/sprint plan, next-phase plan, and implementation progress now carry a content/wording/layout baseline
- new planning language leads with service-SME pain, measurable booking/revenue outcomes, shipped proof, launch/email/support follow-up, and customer-safe degraded-state wording before implementation inventory
- current plain-English product line for docs/public-copy planning is `BookedAI turns missed service enquiries into booked revenue. It captures intent across chat, calls, email, web, and messaging apps, then helps book, follow up, track payment posture, and show operators what revenue was won or still needs action.`
- published the content/wording planning closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/141225-bookedai-content-wording-and-launch-planning-alignment.md`
- upgraded selected wording for SME and investor/judge appeal: SME line now leads with `Never lose a service enquiry to slow replies again`, investor line frames BookedAI as an omnichannel AI Revenue Engine with auditable revenue actions, and the financial model is explicit as setup fee + SaaS + performance-aligned commission/revenue share where appropriate
- tightened active omnichannel product wording to avoid overclaiming instant payment collection: BookedAI now `supports payment and receivable follow-up` and records customer-care actions with operator-visible revenue evidence
- published the SME/investor/judge wording closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/143051-bookedai-sme-investor-judge-wording-upgrade.md`
- added `docs/development/phase-execution-operating-system-2026-04-26.md` as the PM control system for agent lanes, UAT gates, deploy-live gates, phase closeout, and Sprint 19 execution board
- project execution now explicitly keeps Phase `0-16` as delivered baselines and runs active gated delivery through Phase/Sprint `17-23`; each runtime phase must pass UAT, deploy live, smoke, docs, Notion/Discord, and next-phase handoff before advancing
- published the PM phase operating-system closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/143633-bookedai-pm-phase-execution-operating-system.md`
- started Sprint 19 execution under the new board with a Phase 17 P0-1 check: portal/booking focused tests passed (`17 passed`) and live stack health passed at `2026-04-26T14:37:15Z`; deploy-live was not applicable because this pass changed docs/control only
- published the Sprint 19 P0-1 verification closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/143759-bookedai-sprint-19-p0-1-phase-gate-verification.md`
- added `docs/development/whatsapp-provider-posture-decision-2026-04-26.md` for Sprint 19 P0-2: WhatsApp inbound/policy remains active, Twilio is configured as default outbound, Evolution outbound fallback is disabled, Meta registration is blocked, and Twilio delivery remains queued/manual-review until credentials or sender posture are repaired
- published the WhatsApp provider posture decision to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/144011-bookedai-sprint-19-p0-2-whatsapp-provider-posture-decision.md`
- closed Sprint 19 P0-3 live: Telegram now enforces `X-Telegram-Bot-Api-Secret-Token` in production, Evolution enforces HMAC-SHA256 with `WHATSAPP_EVOLUTION_WEBHOOK_SECRET`, and live missing-credential probes return `403`
- verification passed with py_compile, focused webhook/config tests (`23 passed`), security/backend slice (`34 passed`), backend release lane (`47 tests OK`), search eval (`14/14`), tenant smoke (`3 passed`), deploy-live, and stack health at `2026-04-26T15:17:38Z`
- published the Sprint 19 P0-3 closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/151912-bookedai-sprint-19-p0-3-webhook-verification-closed-live.md`
- implemented Sprint 19 P1-7 frontend UI/UX stabilization: product/homepage phone helpers now use `aria-describedby`, admin bookings become contained responsive cards below `720px`, and product fallback copy no longer says booking confirmation is enabled later
- verification passed with frontend `tsc --noEmit` and focused admin Playwright (`2 passed`); product regression rerun hit exit `143` while another workspace Playwright smoke/build lane was active, so product full-suite rerun remains a pre-promotion follow-up
- published the P1-7 frontend closeout to Notion/Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/150208-sprint-19-p1-7-frontend-ui-ux-stabilization.md`
- completed Sprint 19 P0-4 local backend patch: customer Telegram and Evolution webhooks now use the existing idempotency/webhook-event gate already used by WhatsApp, duplicate events return `messages_processed=0`, and additive ledger indexes live in migration `022`
- verification passed with backend py_compile and focused WhatsApp/Telegram webhook tests (`21 passed`); live deploy/evidence drawer surfacing remains a follow-up
- published the P0-4 closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/151107-sprint-19-p0-4-inbound-webhook-idempotency-routing.md`
- implemented Sprint 19 P0-7 DevOps patch surface: added `.env.production.example` SHA-256 guard, release-gate checksum verification, and docs notes that GitHub Actions/branch protection remains a GitHub settings/token-scope follow-up
- verification passed for the P0-7 patch with checksum guard, shell syntax checks, `git diff --check`, and full `RUN_SEARCH_REPLAY_GATE=false bash scripts/run_release_gate.sh` (frontend smoke lanes, tenant smoke, backend unittest `49 tests`, search eval `14/14`)
- implemented Sprint 19 P0-5 public assistant tenant validation: v1 public assistant routes now reject mismatched `actor_context.tenant_id` when a valid tenant Bearer session is present, while `/api/chat/send` remains unchanged because it does not accept `actor_context`
- verification passed with backend py_compile, chat-send plus v1 booking route tests (`8 passed`), and v1 search route tests (`21 passed`); deploy-live/live smoke remain pending if this patch is promoted
- published the P0-5 closeout to Notion and Discord via `sync-doc`; archive entry is `docs/development/telegram-sync/2026-04-26/151022-sprint-19-p0-5-public-assistant-tenant-validation.md`
- pushed Sprint 19 hardening/review docs to `main` through commit `4dc9436` after local release gate passed (`RUN_SEARCH_REPLAY_GATE=false bash scripts/run_release_gate.sh`, backend unittest `49 tests`, search eval `14/14`)
- redeployed live through `python3 scripts/telegram_workspace_ops.py deploy-live`; backend, beta-backend, web, and beta-web rebuilt/recreated, proxy restarted, and n8n workflow activation succeeded
- post-deploy verification passed: `bash scripts/healthcheck_stack.sh` at `2026-04-26T15:45:24Z`, `https://api.bookedai.au/api/health`, and HEAD checks for `bookedai.au`, `portal.bookedai.au`, and `tenant.bookedai.au`
- GitHub rejected `.github/workflows/release-gate.yml` because the current OAuth token lacks `workflow` scope; the workflow file remains local/untracked and needs a token with workflow permission or manual GitHub UI/API setup
