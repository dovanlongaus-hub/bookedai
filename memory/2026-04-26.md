- synced the local `main` workspace after confirming `origin/main` had no newer commits to merge; local was one commit ahead plus a dirty working tree
- prepared the pending public/pitch/product booking UX updates for GitHub sync, including persistent booking confirmation, pitch video/architecture polish, shared calendar/share helpers, portal smoke script, and matching docs/archive notes
- kept the sync path focused on `origin/main`; no destructive git commands were used
