- collapsed the public homepage into a single search-first screen so the page now behaves like one focused Google-like or ChatGPT-like surface instead of a long landing page
- rewired `frontend/src/apps/public/PublicApp.tsx` so the homepage only shows the main search shell plus same-page results rendering through `HomepageSearchExperience`
- removed the old bottom-fixed search composer from `frontend/src/apps/public/HomepageSearchExperience.tsx`, keeping results and the desktop BookedAI flow in one primary page layout with the booking flow on the right
- tightened homepage copy toward one-page enterprise search UX: less marketing scaffolding, clearer search prompt, and stronger emphasis on search, results, and booking continuation in the same screen
- polished `frontend/src/shared/components/PartnerMatchCard.tsx` toward a more Google-like result hierarchy with clearer source row, blue-link style result titles, green location line, cleaner snippet structure, and lighter metadata chips for price, phone, and next action
- tightened homepage and search-results copy in `frontend/src/apps/public/PublicApp.tsx` plus `HomepageSearchExperience.tsx` toward a more enterprise-premium tone, and applied a small pixel-level spacing pass to hero, results column, and right-rail booking workspace to unify the single-page search layout
- synchronized `project.md`, `docs/development/implementation-progress.md`, and `docs/development/sprint-13-16-user-surface-delivery-package.md` around a stricter priority: homepage is now the lead responsive web-app UX stream and should keep moving toward a ChatGPT-like single-workspace search model rather than a broadened landing page
- pushed `frontend/src/apps/public/PublicApp.tsx` one step further into that app-shell direction with tighter responsive copy, calmer workspace framing, and a more product-like homepage composer above the existing results + booking workspace
- added another UX-pro homepage polish pass across `frontend/src/apps/public/HomepageSearchExperience.tsx` and `frontend/src/shared/components/PartnerMatchActionFooter.tsx`: loading now shows a cleaner skeleton-like results state, the right rail opens with a stronger BookedAI operator-flow panel, result action footers feel more premium and responsive, and the whole homepage flow reads more like one cohesive responsive app instead of separate search and booking cards
- expanded the homepage search intelligence layer in `HomepageSearchExperience.tsx`: while search is running, BookedAI now surfaces follow-up prompts for missing location, time, and brief context, plus similar-intent search suggestions the user can tap immediately; the same nearby-intent suggestions now also appear in low-confidence and no-result states so the AI can keep collecting useful context instead of leaving the user stuck
- fixed the homepage booking regression where shortlisted live-read matches could fail with `Selected service was not found`; `frontend/src/apps/public/HomepageSearchExperience.tsx` now keeps non-catalog matches on the authoritative v1 booking-intent path instead of forcing them through the legacy catalog booking session
- reran a production Playwright smoke test on `bookedai.au` using a public-web shortlist (`Private swim coaching near Caringbah this weekend`) and confirmed the old `Selected service was not found` message no longer appears; the remaining live blocker is now CORS on `https://api.bookedai.au/api/v1/leads` and `https://api.bookedai.au/api/v1/bookings/intents`, which surfaces in-browser as `Failed to fetch`
- narrowed the homepage legacy-session fallback boundary again: successful live-read bookings now render directly from the authoritative v1 intent response instead of re-hydrating eligible matches through the older catalog-session flow; `cd frontend && npx tsc --noEmit` and `npm --prefix frontend run build` both passed for this refinement
- fixed the live homepage `Load failed` booking blocker by casting nullable bind params in `backend/repositories/contact_repository.py` and `backend/repositories/lead_repository.py`, eliminating the production `asyncpg.exceptions.AmbiguousParameterError` raised during public-web contact lookup and lead reuse
- verified the backend fix end-to-end: `./.venv/bin/python -m pytest backend/tests/test_phase2_repositories.py -q`, `./.venv/bin/python -m pytest backend/tests/test_api_v1_contract.py -q`, `python3 scripts/telegram_workspace_ops.py deploy-live`, and `bash scripts/healthcheck_stack.sh` all passed; direct `curl` calls to `/api/v1/leads` and `/api/v1/bookings/intents` now return `200` with `Access-Control-Allow-Origin: https://bookedai.au`
- reran a live Playwright smoke test on `https://bookedai.au` with `kids swimming lessons sydney` and confirmed homepage booking now completes through the thank-you/portal state instead of failing at confirmation; residual console noise remains from downstream `/api/v1/sms/messages/send` and `/api/v1/whatsapp/messages/send` returning `422` after the booking itself succeeds
- eliminated the post-booking messaging console noise on the homepage by normalizing Australian local mobile inputs to E.164 in `frontend/src/shared/utils/phone.ts` before SMS/WhatsApp automation, and reusing that path in both `HomepageSearchExperience.tsx` and `BookingAssistantDialog.tsx`
- hardened `backend/service_layer/communication_service.py` so Twilio/WhatsApp provider auth failures and upstream 5xxs now degrade to `queued` with manual-review warnings instead of surfacing as `502` transport failures after the booking is already confirmed
- verified the messaging cleanup with `./.venv/bin/python -m pytest backend/tests/test_lifecycle_ops_service.py -q`, `./.venv/bin/python -m pytest backend/tests/test_api_v1_communication_routes.py -q`, `cd frontend && npx tsc --noEmit`, `npm --prefix frontend run build`, `python3 scripts/telegram_workspace_ops.py deploy-live`, direct `curl` probes to `/api/v1/sms/messages/send` and `/api/v1/whatsapp/messages/send`, and a fresh live Playwright booking smoke test showing zero browser console errors plus queued SMS/WhatsApp follow-up state
- fixed the portal false-negative load state in `frontend/src/apps/portal/PortalApp.tsx` by stopping the portal from treating generic `?ref=` tracker values as booking references; the portal now hydrates only from `booking_reference` plus camel-case compatibility `bookingReference`
- continued the live `bookedai.au` homepage redesign on `2026-04-24` toward a single-screen responsive web-app workspace: `frontend/src/apps/public/PublicApp.tsx` now keeps the page search-first, lighter, and more ChatGPT-like instead of a longer landing-page composition
- tightened the shipped public search runtime in `frontend/src/apps/public/HomepageSearchExperience.tsx` around cleaner same-page results, a stronger right-rail BookedAI workflow on desktop, and more premium result-card plus action-footer presentation
- added richer in-search assistance to the homepage runtime: while search is running, BookedAI now exposes staged progress, asks for missing context like area, time, and brief details, and suggests nearby intent refinements or similar searches instead of leaving the user in a passive wait state
- current homepage priority is explicitly UX-first: keep reducing noisy explanatory chrome, keep the surface product-like rather than marketing-like, and keep pushing the public homepage toward a polished responsive app shell that runs the full BookedAI search-to-booking flow
- smoothed the homepage search flow further in `frontend/src/apps/public/HomepageSearchExperience.tsx` by making the in-search refinement prompts feel more conversational, closer to AI follow-up chat bubbles than static helper panels
- kept pushing the homepage UX away from noisy guidance and toward a cleaner conversational search-to-shortlist flow, while preserving similar-intent suggestions so users can quickly pivot to a nearby query when confidence is low
- another production build verification completed successfully after the search-flow polish, confirming the public homepage runtime still compiles cleanly after the latest UX refinements
- added a final smoothing pass to `frontend/src/apps/public/HomepageSearchExperience.tsx` for live testing: shortlist cards now enter with a softer staged motion using a fast premium easing curve, keeping the public search flow closer to a conversational app than a static results grid
- continued the UX cleanup on `2026-04-24`: the right rail is now lighter and less dashboard-like, the preview modal is more compact and decision-focused, and both `PublicApp.tsx` and `HomepageSearchExperience.tsx` now read more like a calm AI workspace than an enterprise control panel
- fixed another live portal regression in `backend/service_layer/tenant_app_service.py`: portal booking snapshot now tolerates legacy or malformed `booking_intent_id` values and non-object JSON payloads instead of throwing a 500 for older references like `v1-cb79f8e371`; added a regression test in `backend/tests/test_api_v1_portal_routes.py`
- hardened `frontend/src/apps/portal/PortalApp.tsx` so portal bootstrap ignores generic `?ref=` tracker tokens and only reads booking-specific params (`booking_reference`, `bookingReference`), preventing false `BOOKING NOT AVAILABLE` loads from values like `v1-*`
- deployed that portal fix live through `python3 scripts/telegram_workspace_ops.py deploy-live`; `bash scripts/healthcheck_stack.sh` passed and production rebuilt/restarted cleanly
- ran a full live production booking smoke test on `bookedai.au` using `qa+20260424T061705Z@bookedai.au`, which created booking reference `v1-98738517ed` successfully through lead, booking-intent, payment-intent, email, and CRM handoff
- fixed the follow-up portal regressions for that live booking in `backend/service_layer/tenant_app_service.py`: portal snapshot now skips UUID-only payment/audit queries for legacy-style booking intent ids and casts mixed `service_merchant_profiles` join keys to text so `portal.bookedai.au` can reopen `v1-*` bookings safely
- fixed production CORS runtime config by adding `https://portal.bookedai.au` to `CORS_ALLOW_ORIGINS` in `.env`; after redeploy, direct API calls now return `Access-Control-Allow-Origin: https://portal.bookedai.au` and the live portal opens `v1-98738517ed` cleanly with no browser console errors
- synced the current homepage, booking, messaging, portal, and delivery docs set to Notion and prepared the repo worktree for a direct `origin/main` GitHub update, while intentionally excluding local-only tooling state like `.clawhub`, `skills/`, and `memory/.dreams/*`

- Homepage redesign moved further toward a professional chat-style experience using the frontend-design-pro skill.
- Updated `frontend/src/apps/public/PublicApp.tsx`, `frontend/src/apps/public/HomepageSearchExperience.tsx`, and `frontend/src/apps/public/homepageContent.ts` to reduce landing-page chrome, tighten copy, and center the search/composer flow.
- Direction set: homepage should feel closer to ChatGPT, with a calmer search-first layout and less dashboard/enterprise framing.
- Full frontend build remains blocked by pre-existing TypeScript errors in `frontend/src/apps/tenant/TenantApp.tsx`, not by the homepage edits.
- completed a fresh live redeploy via `bash scripts/deploy_live_host.sh`; production images rebuilt, core containers restarted cleanly, and `bash scripts/healthcheck_stack.sh` passed at `2026-04-24T08:41:03Z`
- redesigned `demo.bookedai.au` into a dark premium chat-first runtime instead of the older narrative demo landing page
- split the new demo surface into modular frontend pieces under `frontend/src/apps/public/demo/` so header, chat rail, result cards, booking panel, and flow state stay easier to iterate safely
- connected the demo host to the real public BookedAI v1 flow: session prime, live-read search + booking-path insight, authoritative lead/booking-intent creation, and payment-intent preparation
- replaced the still-active mock `frontend/src/apps/public/DemoLandingApp.tsx` route with the modular demo runtime so the shipped demo host now actually uses the declared BookedAI public APIs instead of local `setTimeout` simulation
- changed demo autoplay to `Tutor for kids` with `Math` follow-up because that query currently returns real live candidates from `/api/v1/matching/search`, while the previous swimming demo query was returning zero candidates in the current tenant data
- removed the fake inline `paid` transition from the demo booking modal; the UI now stays truthful to the current v1 contract by showing `payment opened` or `awaiting confirmation` when `/api/v1/payments/intents` returns only a pending intent or no checkout URL yet
- extended `backend/api/v1_booking_handlers.py` so `/api/v1/payments/intents` now creates real Stripe Checkout URLs for `stripe_card` when service pricing exists, returns direct partner booking URLs for `partner_checkout`, and persists `payment_url` plus payment dependency state back into the booking/payment mirrors
- added backend regression coverage for those new payment-intent branches and verified them with `cd backend && ../.venv-backend/bin/python -m unittest tests.test_api_v1_booking_routes tests.test_api_v1_contract`
- added `framer-motion` to the frontend package so the new demo runtime can use higher-quality staged motion instead of only static CSS transitions
- local verification for the redesign passed with `cd frontend && npx vite build`; full repo `tsc --noEmit` remains noisy elsewhere and was not used as the decisive gate for this demo-only pass
- switched the public internet entry host again on `2026-04-24`: `bookedai.au` and `www.bookedai.au` now redirect to `https://pitch.bookedai.au/` at the production nginx proxy
- updated `scripts/healthcheck_stack.sh` so stack verification now asserts the apex redirect target and then probes `pitch.bookedai.au` as the canonical public web surface
- synchronized `project.md`, `README.md`, `docs/architecture/system-overview.md`, `docs/development/implementation-progress.md`, and `docs/development/sprint-13-16-user-surface-delivery-package.md` to reflect the new public-entry routing
- live verification passed after proxy reload: direct origin and Cloudflare edge requests to `https://bookedai.au` now return `301 Location: https://pitch.bookedai.au/`, and `bash scripts/healthcheck_stack.sh` passed at `2026-04-24T08:51:26Z`
- refined the demo hero toward a more literal ChatGPT-like booking homepage with logo-only nav, centered large composer, approved quick-booking chips, instant chip-triggered search, subtle animated gradient motion, and browser voice input
- verified that follow-up demo refinement with `cd frontend && npm run build`
- tightened the demo chat behavior so AI now asks at most 1-2 short clarifying questions, shows results immediately, and uses a lightweight typing animation instead of long assistant blocks
- verified that chat-interaction pass with `cd frontend && npm run build`
- upgraded the demo results area into a real booking panel with desktop `40/60` layout, mobile slide-up bottom sheet, richer booking cards, fade/hover polish, and skeleton loading
- verified that results-panel pass with `cd frontend && npm run build`
- moved the demo booking path to an inline modal flow: `Book Now` now opens a modal with progress, minimal confirmation, Stripe-ready payment step, success animation, and email/SMS previews
- kept local autofill memory for demo booking details and verified the modal-flow pass with `cd frontend && npm run build`
- added an autoplay homepage walkthrough after 3 seconds idle so the demo now auto-types a kids-swimming query, shows results, opens booking, and lands on success to explain the product in about 10 seconds
- verified that autoplay pass with `cd frontend && npm run build`
- redesigned `frontend/src/apps/public/FutureSwimApp.tsx` into a more premium tenant-specific Future Swim booking surface with calmer swim-school branding, stronger centre cards, and a clearer receptionist-to-booking flow
- removed hardcoded Future Swim centre and price cards from the page runtime; it now loads the live Future Swim tenant catalogue from `/api/booking-assistant/catalog` so visible pricing and venue details follow the current seeded catalogue instead of stale UI constants
- wired the Future Swim booking form more tightly into BookedAI APIs: tenant-scoped search still uses `/v1/matching/search`, date+time requests now use the authoritative public assistant lead+booking-intent flow, and enquiry-only submissions still create a qualified lead plus confirmation email
- local verification passed with `cd frontend && npm run build`; direct scraping of `https://futureswim.com.au` remains blocked from this runtime by upstream ModSecurity `406`, so the safe truth source for now is the live Future Swim catalogue already seeded from that website
- implemented a new premium chess public tenant surface in `frontend/src/apps/public/ChessGrandmasterApp.tsx`, positioned as `GM Mai Hung Chess Academy` rather than exposing the raw internal `tenant1` label
- wired the new chess surface into `frontend/src/app/AppRouter.tsx` so it can load on `chess.bookedai.au`, `/chess-grandmaster`, or `/tenant1-chess`
- the chess tenant runtime uses tenant-scoped `/v1/matching/search`, BookedAI lead capture, authoritative public assistant lead+booking-intent creation when slot data is provided, and lifecycle email follow-up, mirroring the Future Swim full-flow pattern for a grandmaster-led chess academy
- verified the new chess tenant surface with `cd frontend && npm run build`
- added `chess.bookedai.au` across frontend hosted-html generation, frontend nginx host routing, production nginx vhost config, bootstrap/deploy scripts, and Cloudflare auto-DNS defaults so the chess academy can ship as a first-class hostname
- successfully created the Cloudflare A record for `chess.bookedai.au` pointing to `34.40.192.68`; live app cutover is still pending the production host deploy because this runtime cannot run `bash scripts/deploy_live_host.sh` against the real Docker host
- turned the new Grandmaster Chess Academy operator requirement into `docs/architecture/demo-grandmaster-chess-revenue-engine-blueprint.md`, locking the next product step for `demo.bookedai.au` as a full recurring-revenue academy proof rather than only a chat-plus-booking demo
- aligned that blueprint with current repo reality: `demo.bookedai.au` remains first-minute product proof, `chess.bookedai.au` remains the tenant-branded runtime, and the next API wave should add assessment, placement, coach-report, report-generation, and retention seams on top of the existing public v1 flow
- locked the template rule for later scale: chess is the first vertical template, not a hardcoded branch, so assessment rules, report prompts, billing policy, and retention policy should stay tenant-configurable for future SME tenants
- implemented the first academy runtime slice in code: new v1 assessment and placement endpoints now score the Grandmaster Chess Academy flow and return class recommendation, slot posture, and subscription suggestion
- rewired `demo.bookedai.au` into `search -> assessment -> placement -> booking` for the chess demo, and blocked booking until placement is complete so the surface behaves more like a real academy intake flow than a generic booking demo
- added parent-report preview generation and persistence on booking success: backend stores the generated preview under `booking_intents.metadata_json`, and the demo now shows the parent-facing report preview in both the success modal and persistent booking panel
- synthesized the active requirement-side docs into a new root `prd.md`, giving the repo one consolidated PRD aligned to current `project.md` truth across public, tenant, admin, portal, widget/plugin, search-truth, pricing, and chess revenue-engine requirements
- synchronized that documentation pass into `docs/development/implementation-progress.md` and `docs/development/roadmap-sprint-document-register.md` so later requirement changes can treat `prd.md` as part of the normal source-of-truth chain
- expanded `prd.md` and execution docs again so BookedAI is now explicitly defined as a dual-layer AI agent system: customer-facing search/conversation agent plus a revenue-operations agent, with a third customer-care/status agent for returning users
- locked the missing lifecycle scope into requirements: AI-led lead follow-up, payment and invoice reminders, CRM sync, webhook/API triggers, tenant billing and financial summaries, retention-risk handling, and customer-state replies by verified phone/email/booking context
- updated the roadmap, current execution plan, Sprint 13-16 package, and video docs so implementation and pitch materials now show the same end-to-end story from demand capture through retention and billing
- extended the chess academy slice into `portal.bookedai.au`: portal detail now carries `academy_report_preview`, shows parent-facing progress and next-class context, and adds auditable `pause` plus `downgrade` request flows beside `reschedule` and `cancel`
- tightened the docs again so agent coordination is explicit, not implied: `Search and Conversation Agent -> Revenue Operations Agent -> Customer Care and Status Agent` now has a declared handoff contract and shared context requirements in `prd.md`
- locked `Grandmaster Chess Academy` as the first required connected-agent implementation before wider multi-tenant generalization; roadmap, current execution plan, and chess blueprint now all prioritize chess-first sequencing for the next wave
- redesigned `demo.bookedai.au` into a more professional full-flow academy workspace: header status, flow rail, intake composer, academy matches, revenue workflow, guarded booking, payment truth, and parent-report continuity now present one connected `intent -> assessment -> placement -> booking -> report/retention` proof
- added `frontend/src/apps/public/demo/DemoFlowRail.tsx` and refined `DemoLandingApp`, `DemoHeader`, `DemoChatStage`, `DemoResultsPanel`, `DemoBookingPanel`, and demo prompt copy while preserving the existing API-backed runtime contracts
- verified the redesign with `npm --prefix frontend run build` plus Playwright desktop/mobile screenshots under `output/playwright/`; local browser smoke still shows expected `401 Unauthorized` API calls because Vite is not pointed at an authenticated local backend
- published the redesign closeout through `python3 scripts/telegram_workspace_ops.py sync-doc`; Notion page and Discord summary were both sent, with archive note `docs/development/telegram-sync/2026-04-24/160415-demo-bookedai-au-full-flow-workspace-redesign.md`
- implemented the first chess Slice 2/3 handoff: `POST /api/v1/subscriptions/intents`, migration `018_academy_subscription_and_agent_action_runs.sql`, `academy_subscription_intents`, and `agent_action_runs`
- subscription intent creation now queues revenue-ops actions for subscription confirmation, payment reminder, CRM sync, report generation, and retention evaluation; academy student snapshots include latest subscription intent plus recent actions
- added direct revenue-ops queue endpoints `POST /api/v1/reports/generate` and `POST /api/v1/retention/evaluate`, both backed by `agent_action_runs`, audit, and outbox
- demo booking success now calls the subscription intent endpoint after report-preview generation and shows the queued revenue-agent actions in the booking panel
- verification passed with backend py_compile, `tests/test_api_v1_assessment_routes.py tests/test_api_v1_academy_routes.py tests/test_api_v1_contract.py`, and `npm --prefix frontend run build`
- published the subscription/revenue-ops closeout through `python3 scripts/telegram_workspace_ops.py sync-doc`; Notion and Discord both succeeded, archive note `docs/development/telegram-sync/2026-04-24/161812-chess-subscription-handoff-and-revenue-ops-action-ledger.md`
- published the direct report/retention queue endpoint closeout through `python3 scripts/telegram_workspace_ops.py sync-doc`; archive note `docs/development/telegram-sync/2026-04-24/162144-chess-revenue-ops-report-and-retention-queue-endpoints.md`
- redesigned the shipped public homepage toward a ChatGPT-like BookedAI web app shell: desktop sidebar, compact top bar, short `bookedai.au` hero banner, prompt chips, and a calmer chat-to-booking workspace in `PublicApp.tsx` plus `HomepageSearchExperience.tsx`
- verified the homepage redesign with `npm --prefix frontend run build` and Playwright desktop/mobile screenshots under `output/playwright/`; local static preview still shows the inherited unauthenticated `/api/v1/conversations/sessions` `401`
- published the homepage redesign closeout through `python3 scripts/telegram_workspace_ops.py sync-doc`; Notion and Discord both succeeded, archive note `docs/development/telegram-sync/2026-04-24/163412-public-homepage-chatgpt-like-web-app-redesign.md`
- continued the homepage app-shell polish by adding a compact `chat -> match -> book` flow rail and locking the booking form behind shortlist match selection; refreshed screenshots are `output/playwright/publicapp-chatgpt-redesign-desktop-v2.png` and `output/playwright/publicapp-chatgpt-redesign-mobile-v2.png`
- published that polish continuation through `python3 scripts/telegram_workspace_ops.py sync-doc`; Notion and Discord both succeeded, archive note `docs/development/telegram-sync/2026-04-24/164022-public-homepage-chat-to-booking-polish-continuation.md`
- continued the chess connected-agent implementation deeply by adding tenant/admin revenue-ops ledger APIs: `GET /api/v1/agent-actions`, `GET /api/v1/agent-actions/{action_run_id}`, and `POST /api/v1/agent-actions/{action_run_id}/transition`
- `agent_action_runs` can now move beyond queued into `in_progress`, `sent`, `completed`, `failed`, `manual_review`, or `skipped`, with audit and outbox evidence for each transition
- frontend v1 contracts now include list and transition helpers for revenue-agent actions, ready for a dedicated tenant/admin operator UI pass
- verification passed with backend py_compile, academy/assessment/contract route tests, and `npm --prefix frontend run build`
- added the first tracked chess revenue-ops worker: `workers/academy_actions.py` plus `POST /api/v1/agent-actions/dispatch`
- queued `agent_action_runs` now move through `in_progress` into `sent`, `completed`, `manual_review`, or `failed` based on current policy, with `job_runs` evidence from the tracked scheduler wrapper
- frontend v1 contracts now expose `dispatchRevenueAgentActions` beside the list and transition helpers
- verification passed with worker/academy route tests, academy/assessment/contract tests, and `npm --prefix frontend run build`
- added the first admin UI for the chess revenue-ops ledger: `frontend/src/features/admin/revenue-ops-action-ledger.tsx`, mounted inside Reliability
- the panel supports tenant-scoped listing, status/action filters, dispatch control, summary counts, and quick `completed` / `manual_review` transitions for `agent_action_runs`
- verification passed with academy worker/route/contract tests and `npm --prefix frontend run build`
- fixed the live `pitch.bookedai.au` BookedAI package registration flow: `/register-interest` now routes to `RegisterInterestApp` before host-level pitch/product runtimes, and `https://pitch.bookedai.au` is allowed by production CORS for the configured API base
- hardened pricing consultation creation so calendar, Stripe, event-store, or dual-write side-effect failures degrade into manual follow-up instead of failing the customer-visible submit
- compacted the package selector into icon-led, line-clamped option boxes to reduce mobile text overflow and vertical sprawl
- verified live end-to-end from pitch pricing to registration confirmation `CONS-391FA9D5`, with stack health passing and no app console errors or horizontal overflow
- polished `demo.bookedai.au`: fixed desktop results clipping, viewport-safe booking modal scroll, branded image fallback for result cards, and richer revenue-agent action timeline copy/status treatment
- fixed a demo flow bug where assessment API failure could be misreported as global search failure; assessment errors now stay isolated and bundle suggestions do not show without a real shortlist
- verification passed with `npm --prefix frontend run build` and Playwright CLI smoke screenshot `output/playwright/demo-polish-2026-04-24.png`; local smoke still shows expected unauthorized API responses outside authenticated production runtime
- ran full live QA on `product.bookedai.au` with desktop/mobile screenshots and a production booking smoke; initial finding was a post-booking `/api/v1/payments/intents` browser CORS symptom caused by a backend 500, not by preflight
- fixed the product payment-intent backend lookup in `backend/api/v1_booking_handlers.py` by resolving booking intent via UUID text or `v1-*` booking reference, casting mixed service/tenant keys, and using the booking row tenant for payment mirror writes
- polished the product shell in `ProductApp.tsx` and `BookingAssistantDialog.tsx` so desktop gets a wider product-proof frame and mobile shows quick-search cards in the first screen instead of a mostly blank canvas
- verification passed with backend route tests, frontend build, live backend/web deploy recovery, stack healthcheck, direct product-origin payment curl, and final Playwright booking `v1-1ed89a5995` with all observed API calls `200`, no console errors, and no failed browser requests
- hardened the production deploy scripts after that recovery exposed a transient Docker Compose `No such container`/orphan recreate failure around `hermes`: `deploy_production.sh` now retries with orphan cleanup, while `healthcheck_stack.sh` validates running Compose services instead of fixed production container names
- verification passed with shell syntax checks, live host Compose service status for `hermes/backend/beta*/n8n/proxy/web`, and `bash scripts/healthcheck_stack.sh` passing at `2026-04-24T17:05:26Z`
- completed a deeper live QA pass on `pitch.bookedai.au` after the package-registration hotfix: mobile pricing -> `register-interest` -> submitted package registration now returns API `200` and confirmation `CONS-022CCEA9`
- removed the mobile register-page fixed bottom CTA that overlaid content, tightened package-card title wrapping, and adjusted the shared landing footer so brand copy, release badge, and CTA buttons no longer compress into narrow columns on mobile
- verification passed with `npm --prefix frontend run build`, `./.venv/bin/python -m pytest backend/tests/test_pricing_consultation_resilience.py -q`, `python3 scripts/telegram_workspace_ops.py deploy-live`, `bash scripts/healthcheck_stack.sh`, CORS preflight from `https://pitch.bookedai.au`, and Playwright desktop/tablet/mobile live screenshots under `output/playwright/`
- continued the admin revenue-ops ledger: summary counts now stay tenant-wide instead of filter-local, operators can filter by student or booking reference, evidence drawers show action input/result JSON, and compact icon-led controls cover refresh, dispatch, complete, and manual-review actions
- fixed the live ledger API permission gap by adding idempotent `bookedai_app` grants for `academy_subscription_intents` and `agent_action_runs` in migration `018`, then reapplying migrations through the host workflow
- verification passed with academy route/worker tests, frontend build, live deploy, stack healthcheck at `2026-04-24T17:22:32Z`, and production probes for `GET /api/v1/agent-actions` plus `POST /api/v1/agent-actions/dispatch`
- added the requested post-booking Thank You handoff across the BookedAI full booking surfaces: homepage search, product/embedded assistant, Future Swim tenant runtime, and Grandmaster Chess tenant runtime now show a thank-you confirmation with a 5-second countdown and return to the main screen automatically
- verified the live product booking flow end to end on `product.bookedai.au`: search -> shortlist -> booking submit -> payment intent -> email/SMS/WhatsApp automation all returned `200`, Thank You countdown appeared, and booking `v1-ce0d20a95d` returned to the canonical main surface after 5 seconds with no console errors or horizontal overflow
- verification passed with `backend/tests/test_api_v1_booking_routes.py`, `npm --prefix frontend run build`, `python3 scripts/telegram_workspace_ops.py deploy-live`, `bash scripts/healthcheck_stack.sh`, and Playwright screenshots under `output/playwright/bookedai-thankyou-flow-*.png`
- added `frontend/tests/demo-bookedai-full-flow.spec.ts` to lock `/demo` live-read regression coverage for partial assessment failure and mobile search -> assessment -> placement -> booking -> report -> revenue handoff
- fixed demo QA findings: assessment/placement response normalization now tolerates direct and backend shapes, placement slots become valid `datetime-local` values, quick assessment answers keep the latest shortlist, and no-checkout-url payment states no longer say checkout opened
- verification passed with `PLAYWRIGHT_PUBLIC_ASSISTANT_MODE=live-read npx playwright test tests/demo-bookedai-full-flow.spec.ts --project=live-read` and `npm --prefix frontend run build`
- published the demo deep-QA closeout through `python3 scripts/telegram_workspace_ops.py sync-doc`; Notion and Discord both succeeded, archive note `docs/development/telegram-sync/2026-04-24/171740-demo-bookedai-deep-qa-regression-and-fixes.md`
- completed full go-live QA and deploy after fixing a contract-test expectation, demo CORS for `/api/v1/conversations/sessions`, and `chess.bookedai.au` DNS/proxy posture
- release verification passed with backend 47-test batch, backend booking/contract subset after CORS fix, frontend build, demo Playwright live-read regression, two `deploy-live` runs, stack healthcheck, API health, demo CORS preflight, revenue-ops API probe, and browser smoke across demo, pitch/register, product, admin, and chess
- published the full go-live closeout through `python3 scripts/telegram_workspace_ops.py sync-doc`; Notion and Discord both succeeded, archive note `docs/development/telegram-sync/2026-04-24/235014-full-flow-go-live-qa-and-release.md`
