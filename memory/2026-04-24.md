- collapsed the public homepage into a single search-first screen so the page now behaves like one focused Google-like or ChatGPT-like surface instead of a long landing page
- rewired `frontend/src/apps/public/PublicApp.tsx` so the homepage only shows the main search shell plus same-page results rendering through `HomepageSearchExperience`
- removed the old bottom-fixed search composer from `frontend/src/apps/public/HomepageSearchExperience.tsx`, keeping results and the desktop BookedAI flow in one primary page layout with the booking flow on the right
- tightened homepage copy toward one-page enterprise search UX: less marketing scaffolding, clearer search prompt, and stronger emphasis on search, results, and booking continuation in the same screen
- polished `frontend/src/shared/components/PartnerMatchCard.tsx` toward a more Google-like result hierarchy with clearer source row, blue-link style result titles, green location line, cleaner snippet structure, and lighter metadata chips for price, phone, and next action
- tightened homepage and search-results copy in `frontend/src/apps/public/PublicApp.tsx` plus `HomepageSearchExperience.tsx` toward a more enterprise-premium tone, and applied a small pixel-level spacing pass to hero, results column, and right-rail booking workspace to unify the single-page search layout
- synchronized `project.md`, `docs/development/implementation-progress.md`, and `docs/development/sprint-13-16-user-surface-delivery-package.md` around a stricter priority: homepage is now the lead responsive web-app UX stream and should keep moving toward a ChatGPT-like single-workspace search model rather than a broadened landing page
- pushed `frontend/src/apps/public/PublicApp.tsx` one step further into that app-shell direction with tighter responsive copy, calmer workspace framing, and a more product-like homepage composer above the existing results + booking workspace
- added another UX-pro homepage polish pass across `frontend/src/apps/public/HomepageSearchExperience.tsx` and `frontend/src/shared/components/PartnerMatchActionFooter.tsx`: loading now shows a cleaner skeleton-like results state, the right rail opens with a stronger BookedAI operator-flow panel, result action footers feel more premium and responsive, and the whole homepage flow reads more like one cohesive responsive app instead of separate search and booking cards
- expanded the homepage search intelligence layer in `HomepageSearchExperience.tsx`: while search is running, BookedAI now surfaces follow-up prompts for missing location, time, and brief context, plus similar-intent search suggestions the user can tap immediately; the same nearby-intent suggestions now also appear in low-confidence and no-result states so the AI can keep collecting useful context instead of leaving the user stuck
- fixed the homepage booking regression where shortlisted live-read matches could fail with `Selected service was not found`; `frontend/src/apps/public/HomepageSearchExperience.tsx` now keeps non-catalog matches on the authoritative v1 booking-intent path instead of forcing them through the legacy catalog booking session
- reran a production Playwright smoke test on `bookedai.au` using a public-web shortlist (`Private swim coaching near Caringbah this weekend`) and confirmed the old `Selected service was not found` message no longer appears; the remaining live blocker is now CORS on `https://api.bookedai.au/api/v1/leads` and `https://api.bookedai.au/api/v1/bookings/intents`, which surfaces in-browser as `Failed to fetch`
- narrowed the homepage legacy-session fallback boundary again: successful live-read bookings now render directly from the authoritative v1 intent response instead of re-hydrating eligible matches through the older catalog-session flow; `cd frontend && npx tsc --noEmit` and `npm --prefix frontend run build` both passed for this refinement
- fixed the live homepage `Load failed` booking blocker by casting nullable bind params in `backend/repositories/contact_repository.py` and `backend/repositories/lead_repository.py`, eliminating the production `asyncpg.exceptions.AmbiguousParameterError` raised during public-web contact lookup and lead reuse
- verified the backend fix end-to-end: `./.venv/bin/python -m pytest backend/tests/test_phase2_repositories.py -q`, `./.venv/bin/python -m pytest backend/tests/test_api_v1_contract.py -q`, `python3 scripts/telegram_workspace_ops.py deploy-live`, and `bash scripts/healthcheck_stack.sh` all passed; direct `curl` calls to `/api/v1/leads` and `/api/v1/bookings/intents` now return `200` with `Access-Control-Allow-Origin: https://bookedai.au`
- reran a live Playwright smoke test on `https://bookedai.au` with `kids swimming lessons sydney` and confirmed homepage booking now completes through the thank-you/portal state instead of failing at confirmation; residual console noise remains from downstream `/api/v1/sms/messages/send` and `/api/v1/whatsapp/messages/send` returning `422` after the booking itself succeeds
- eliminated the post-booking messaging console noise on the homepage by normalizing Australian local mobile inputs to E.164 in `frontend/src/shared/utils/phone.ts` before SMS/WhatsApp automation, and reusing that path in both `HomepageSearchExperience.tsx` and `BookingAssistantDialog.tsx`
- hardened `backend/service_layer/communication_service.py` so Twilio/WhatsApp provider auth failures and upstream 5xxs now degrade to `queued` with manual-review warnings instead of surfacing as `502` transport failures after the booking is already confirmed
- verified the messaging cleanup with `./.venv/bin/python -m pytest backend/tests/test_lifecycle_ops_service.py -q`, `./.venv/bin/python -m pytest backend/tests/test_api_v1_communication_routes.py -q`, `cd frontend && npx tsc --noEmit`, `npm --prefix frontend run build`, `python3 scripts/telegram_workspace_ops.py deploy-live`, direct `curl` probes to `/api/v1/sms/messages/send` and `/api/v1/whatsapp/messages/send`, and a fresh live Playwright booking smoke test showing zero browser console errors plus queued SMS/WhatsApp follow-up state
- fixed the portal false-negative load state in `frontend/src/apps/portal/PortalApp.tsx` by stopping the portal from treating generic `?ref=` tracker values as booking references; the portal now hydrates only from `booking_reference` plus camel-case compatibility `bookingReference`
- continued the live `bookedai.au` homepage redesign on `2026-04-24` toward a single-screen responsive web-app workspace: `frontend/src/apps/public/PublicApp.tsx` now keeps the page search-first, lighter, and more ChatGPT-like instead of a longer landing-page composition
- tightened the shipped public search runtime in `frontend/src/apps/public/HomepageSearchExperience.tsx` around cleaner same-page results, a stronger right-rail BookedAI workflow on desktop, and more premium result-card plus action-footer presentation
- added richer in-search assistance to the homepage runtime: while search is running, BookedAI now exposes staged progress, asks for missing context like area, time, and brief details, and suggests nearby intent refinements or similar searches instead of leaving the user in a passive wait state
- current homepage priority is explicitly UX-first: keep reducing noisy explanatory chrome, keep the surface product-like rather than marketing-like, and keep pushing the public homepage toward a polished responsive app shell that runs the full BookedAI search-to-booking flow
- smoothed the homepage search flow further in `frontend/src/apps/public/HomepageSearchExperience.tsx` by making the in-search refinement prompts feel more conversational, closer to AI follow-up chat bubbles than static helper panels
- kept pushing the homepage UX away from noisy guidance and toward a cleaner conversational search-to-shortlist flow, while preserving similar-intent suggestions so users can quickly pivot to a nearby query when confidence is low
- another production build verification completed successfully after the search-flow polish, confirming the public homepage runtime still compiles cleanly after the latest UX refinements
- added a final smoothing pass to `frontend/src/apps/public/HomepageSearchExperience.tsx` for live testing: shortlist cards now enter with a softer staged motion using a fast premium easing curve, keeping the public search flow closer to a conversational app than a static results grid
- continued the UX cleanup on `2026-04-24`: the right rail is now lighter and less dashboard-like, the preview modal is more compact and decision-focused, and both `PublicApp.tsx` and `HomepageSearchExperience.tsx` now read more like a calm AI workspace than an enterprise control panel
- hardened `frontend/src/apps/portal/PortalApp.tsx` so portal bootstrap ignores generic `?ref=` tracker tokens and only reads booking-specific params (`booking_reference`, `bookingReference`), preventing false `BOOKING NOT AVAILABLE` loads from values like `v1-*`
- deployed that portal fix live through `python3 scripts/telegram_workspace_ops.py deploy-live`; `bash scripts/healthcheck_stack.sh` passed and production rebuilt/restarted cleanly
- synced the current homepage, booking, messaging, portal, and delivery docs set to Notion and prepared the repo worktree for a direct `origin/main` GitHub update, while intentionally excluding local-only tooling state like `.clawhub`, `skills/`, and `memory/.dreams/*`
