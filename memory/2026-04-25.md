- Added `scripts/smoke_local_basic_workflows.sh` (HTTP: backend health, bridge healthz + `public_upstream`, public catalog, internal 401, optional backend catalog).
- `frontend/scripts/run_playwright_suite.sh`: when `PLAYWRIGHT_EXTERNAL_SERVER=1`, skip build/preview and run Playwright only (fixes docker + external UI).
- `npm run test:playwright:basic-workflows` runs example flows only (homepage + bridge + public catalog); full legacy responsive remains `test:playwright:smoke`.
- Extended `example-orchestration-flows.spec.ts` with public catalog GET assertion.
