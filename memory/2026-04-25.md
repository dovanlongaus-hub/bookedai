- redesigned the public homepage in `frontend/src/apps/public/PublicApp.tsx` into an investor/customer acquisition surface: first viewport now states `Turn demand into booked revenue`, shows Grandmaster Chess/Future Swim/WhatsApp proof, and routes clearly to product, pitch, and roadmap
- kept the real `HomepageSearchExperience` on-page as the live proof workspace, surrounded by concise sections for the market problem, operating model, investor thesis, and customer value
- fixed homepage header responsive visibility by avoiding `hidden lg:flex`, because the legacy theme defines `.hidden { display:none!important }`
- verification passed with `npm --prefix frontend run build`, local Playwright desktop/mobile screenshots in `frontend/output/playwright/homepage-redesign-2026-04-25/`, and mobile no-horizontal-overflow at `390px`; local preview still shows expected unauthenticated conversation-session `401`
- deployed the redesigned homepage live with `python3 scripts/telegram_workspace_ops.py deploy-live`; compose hit the known transient container recreate race, retried with orphan cleanup, then completed
- live `https://bookedai.au` Playwright smoke confirmed the new H1/title/nav, mobile no-overflow at `390px`, and zero console errors; updated `scripts/healthcheck_stack.sh` to expect direct homepage serving instead of root redirect to pitch
- follow-up live public-surface smoke passed for home, product, pitch, `/roadmap`, tenant, portal, and admin login shell with `200`, expected copy, no failed browser requests, no console errors, and no mobile horizontal overflow
- ran live UAT/content/UX review for `portal.bookedai.au` and wrote `docs/development/portal-bookedai-uat-content-ux-review-2026-04-25.md`
- created public booking-intent UAT reference `v1-6879356dc2`; creation returned `200`, but portal snapshot and care-turn for that reference returned live `500`
- browser portal showed `Booking not available` / `Failed to fetch`; console exposed CORS symptoms because the `500` response lacked CORS headers
- invalid reference handling still returned structured `404 portal_booking_not_found` with CORS, and tested desktop/mobile error states had no horizontal overflow
- next priority: fix portal snapshot hydration for public `v1-*` booking-intent records before broader UI/content polish
- synchronized the chess verified-tenant search requirement into the detailed design/PRD stack and active roadmap/sprint/phase docs: `prd.md`, `DESIGN.md`, master PRD, chatbot/product design spec, Sprint 13-16 package, next-phase plan, roadmap register, sprint checklist, and frontend roadmap data
- locked ownership split: Phase/Sprint 17 preserves the current results-first chess tenant proof; Phase/Sprint 22 owns reusable verified-tenant template extraction across future verticals
- added verified BookedAI chess tenant treatment to public/product search results: reviewed Co Mai Hung / Grandmaster Chess matches now show tenant capability chips for Book, Stripe, QR payment/confirmation, calendar, email, WhatsApp Agent, and portal edit/revisit without forcing booking focus
- updated selected-booking and confirmation copy so chess tenant bookings keep QR/portal confirmation, Stripe/QR payment posture, calendar/email actions, and WhatsApp Agent follow-up visible; frontend production build passed
- polished the public/product search UX into a calmer ChatGPT-like workspace: stronger composer treatment, clearer chat status, compact scan-first result cards, and matching hierarchy across homepage/product assistant results
- tightened the product welcome state after browser review so the composer is visible in the first desktop viewport, then verified with `npm --prefix frontend run build` and local Playwright screenshots
- synced the product SaaS search UX review closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/054653-product-saas-search-ux-review-and-polish.md`
- synced the ChatGPT-like search workspace polish closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/053550-public-product-chatgpt-search-workspace-polish.md`
- refined the public/product search-results flow so completed searches no longer auto-scroll or focus booking; users can pause, scroll, compare compact result cards, open detail popups, and enter booking only through explicit `Book` actions
- moved homepage clarification/follow-up prompts into the BookedAI chat thread as actionable agent chips and removed the competing standalone clarification panel
- changed popup/product assistant results so the first match is no longer auto-selected after search; `View details` and `Book` now preserve a cleaner review-to-commit path
- synchronized `project.md`, README, DESIGN, implementation progress, roadmap, and current sprint plan around the results-first search UX contract; frontend build passed
- synced the search-results flow refinement closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/052035-public-product-search-results-flow-refinement.md`
- continued the AI-agent implementation by making `HomepageSearchExperience.tsx` keep a real visible chat thread with user turns, assistant replies, inline top-result cards, and quick suggestion chips
- added `POST /api/v1/agents/customer-turn` so the customer-facing search/conversation agent has a reusable backend turn contract with reply, search payload, missing context, suggestions, and next-agent handoff metadata
- homepage live-read search now calls the backend customer-turn contract first and renders its reply/search payload in the chat workspace
- popup/product assistant search now also calls the backend customer-turn contract first, keeping homepage and embedded/product assistant search behavior aligned while preserving legacy streaming fallback
- clarification answers now continue the chat and rerun search with added context, moving the public flow closer to `ask -> refine -> results -> book`
- added `POST /api/v1/revenue-ops/handoffs` so a booking lifecycle event can queue SME revenue-operations work in `agent_action_runs`
- homepage and popup/product post-booking automation now queue revenue-ops actions for lead follow-up, payment reminder, CRM sync, customer-care status monitoring, and webhook callback after payment/communication best-effort steps
- admin Reliability ledger filters now include generic SME action types beside the chess academy actions
- verification passed with focused backend search/academy/worker tests and `npm --prefix frontend run build`
- added `docs/development/next-phase-implementation-plan-2026-04-25.md` and synchronized `prd.md`, `project.md`, the roadmap, sprint execution plan, sprint package, and implementation-progress docs around the next phase sequence
- locked the post-release order as Phase 17 full-flow stabilization, Phase 18 revenue-ops ledger control, Phase 19 customer-care/status agent, Phase 20 widget/plugin runtime, Phase 21 billing/receivables truth, Phase 22 reusable templates, and Phase 23 release governance
- synced the next-phase closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/004348-next-phase-implementation-plan-and-full-flow-closeout.md`
- started Phase 18 by extending `GET /api/v1/agent-actions` with entity/agent/dependency filters and summary counts, adding admin entity/dependency filters, and adding a tenant `Ops` panel for read-only revenue-ops action visibility
- verification passed with focused academy/worker tests, frontend typecheck, and frontend production build
- synced Phase 18 closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/010506-phase-18-revenue-ops-ledger-tenant-visibility.md`
- switched the AI-agent continuation work back onto `main`, reapplied the AI-agent and Future Swim stashes cleanly, confirmed no merge markers or whitespace errors, and re-verified with focused backend tests plus frontend production build
- continued Phase 18 on `main`: revenue-ops action runs now expose lifecycle-event filtering plus derived dependency, policy, approval, and evidence summary metadata; admin Reliability and tenant Ops both render the new posture fields
- synced the Phase 18 action metadata closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/010947-phase-18-revenue-ops-action-metadata-and-tenant-admin-evidence-posture.md`
- replaced the main public/product/demo/shared header brand treatment with the uploaded BookedAI logo asset and responsive cropped frames so top-left branding stays visible without overflow
- upgraded the shared landing menu defaults to Product, Live Demo, Tenant Login, and Roadmap with lucide menu glyphs
- hardened the public homepage live-read booking flow: customer-agent turn now falls back to v1 matching/search, clarification no longer blocks service+location queries, and booking CTAs/status labels match the regression-tested flow
- verification passed with frontend typecheck/build, public homepage responsive Playwright coverage, and focused live-read booking smoke cases for v1 booking-intent submission and near-me location guardrails
- deployed the brand/menu and booking-flow hardening live with `telegram_workspace_ops.py deploy-live`; stack health passed and live browser smoke confirmed demo/product/pitch/tenant render without page errors or horizontal overflow
- Discord closeout was sent after live verification; the first Notion sync succeeded, while later re-sync attempts for the updated live-verification note returned HTTP 403
- added the uploaded `Chess_screen` asset before the public homepage and pitch hero areas as a responsive chess academy proof band; frontend typecheck/build, deploy-live, stack health, and live pitch Playwright smoke at mobile/desktop widths passed with no horizontal overflow
- refined the final `pitch.bookedai.au` CTA so the latest uploaded image sits in a full-width 3:2 frame and the verbose footer positioning/release text is hidden on pitch; frontend build and local Playwright checks at `390px` and `1440px` passed with no horizontal overflow
- adjusted the top `pitch.bookedai.au` chess proof image into a padded 3:2 `object-contain` screen frame so it fits the card without being cropped; frontend build, deploy-live, stack health, and local/live Playwright checks passed at `390px` and `1440px`
- optimized the uploaded logo, chess proof, final contact proof, and pitch team images into local WebP assets; production pitch smoke confirmed no `upload.bookedai.au/images` requests, stack health passed, and mobile/desktop had no horizontal overflow
- simplified the tenant gateway login UX into a Google-first enterprise access screen with email-code fallback and explicit create-account mode
- hardened tenant Google auth so gateway sign-in without an active membership returns `tenant_google_membership_not_found` instead of silently creating a new workspace; the create-account path still creates new tenant workspaces intentionally
- verification passed with focused tenant backend auth tests, frontend production build, and local Playwright desktop/mobile tenant gateway QA with no console errors or mobile horizontal overflow
- diagnosed `admin.bookedai.au` login fail as `/api/` static routing on the admin host: live `/api/admin/login` returned frontend nginx `405`, and `/api/health` returned the admin HTML shell
- added explicit `/api/` backend proxying for the admin host in `deploy/nginx/bookedai.au.conf` plus frontend-container `frontend/nginx/default.conf`, so the shared Vite admin login reaches FastAPI again after deploy
- restarted the live proxy only after `nginx -t` passed; `admin.bookedai.au/api/health` now returns backend JSON and bad-password login probes return backend `401`, confirming the live login route is no longer swallowed by SPA fallback
- reorganized the shipped admin workspace into a compact sidebar shell grouped by Operate, Tenants, Revenue, and Platform, with Catalog stacked so partner profile actions remain viewport-safe
- pushed commit `71436df` to `origin/main`, deployed live with `telegram_workspace_ops.py deploy-live`, and verified stack health plus live admin probes; `/api/health` returns backend JSON and bad-password login now returns backend `401`
- full QA passed before deploy: frontend typecheck, admin smoke, backend `pytest` across `backend/tests`, and `scripts/run_release_gate.sh`
- redesigned `portal.bookedai.au` into an enterprise customer workspace with secure lookup, booking/payment/support status cards, shared overview/edit/reschedule/pause/downgrade/cancel navigation, provider/customer detail panels, academy progress, timeline, and sticky desktop action rail
- fixed portal request-composer close behavior so it returns to overview and syncs URL state cleanly; request mutations remain queued through the existing portal backend endpoints
- added focused Playwright portal coverage for render, reschedule submission, and mobile no-overflow; verification passed with frontend build, `.venv` backend portal route unittest, and external-server Playwright portal smoke
- tightened the product/public booking runtime UX: progressive first matches during slow live search, explicit select-vs-book behavior, compact result icon action rows, portal-first QR confirmation, continue-chat action, and a persistent Thank You confirmation that no longer auto-returns
- verified the booking UX pass with focused backend booking/search/communication tests, frontend production build, and the live-read Playwright smoke for v1 booking intent plus near-me location guardrail
- ran full live QA on `tenant.bookedai.au`; fixed Future Swim workspace failures from asyncpg audit nullable-filter typing, revenue-metrics integer interval typing, and a React hook-order crash after loading
- deployed refreshed backend/web services and verified stack health, live tenant API `200` + CORS for all Future Swim endpoints, no browser console errors, and no mobile horizontal overflow on the gateway or `/future-swim`
- added the latest uploaded workspace screenshot to the tenant login hero as local optimized WebP assets, deployed web services, and verified live desktop/mobile tenant gateway layout with no console errors or horizontal overflow
- added a pitch architecture visual that explains the whole project as `customer surfaces -> AI agents -> booking core -> operations truth`
- updated the public roadmap code dataset to expose Phase/Sprint 17-23 directly, covering stabilization, revenue ops, customer care, widget runtime, billing truth, templates, and release governance
- synchronized `project.md`, `prd.md`, roadmap/progress docs, README, DESIGN, and system overview around the new roadmap and pitch architecture visualization baseline
- verification passed with frontend typecheck, frontend production build, and local Playwright CLI smoke for `/pitch-deck` plus `/roadmap`; Notion and Discord sync succeeded via `telegram_workspace_ops.py sync-doc`
- continued tenant AI operations automation on `main`: tenant integrations now expose an automation connection plan, and tenant admins/operators can run policy-gated queued revenue-ops actions through `POST /api/v1/tenant/operations/dispatch`
- synced the tenant AI operations automation closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/051218-tenant-ai-operations-automation-connection-and-dispatch.md`
- started Phase 19 customer-care/status delivery through WhatsApp: inbound configured WhatsApp messages now resolve booking identity, answer from portal booking/payment/support truth, and queue audited cancel/reschedule portal requests when the customer clearly asks to change an existing booking
- synchronized the WhatsApp customer-care scope into `project.md`, `prd.md`, README/DESIGN, roadmap/phase docs, implementation progress, system overview, and archive note `docs/development/telegram-sync/2026-04-25/052534-whatsapp-customer-care-booking-channel.md`
- verification passed with backend py_compile, focused WhatsApp webhook/portal/communication route tests, final live deploy after JSON-safe event metadata hardening, stack health, live API health, and live WhatsApp webhook route reachability
- post-deploy Discord closeout succeeded via `docs/development/telegram-sync/2026-04-25/052724-whatsapp-customer-care-booking-channel-deployed.md`; initial Notion sync succeeded, while the post-deploy Notion re-sync returned HTTP 403
- hardened the remaining `admin.bookedai.au` login failure path by forcing the admin host to use same-origin `/api` before `VITE_API_BASE_URL`, normalizing raw browser network errors, and redesigning the login screen as an enterprise control-plane entry surface
- verification passed with frontend typecheck/build, admin smoke, full backend pytest, release gate, and a hostname-mapped Playwright probe proving login posts to `/api/admin/login` on the admin origin
- tested the real admin credential live: fixed missing admin session signing-secret env exposure, added runtime outbox retry columns via migration `019`, corrected admin payment-attention customer lookup to join `contacts`, hot-restarted backend, and verified live admin login plus read-only workspace navigation across Overview, Billing Support, Messaging, Tenants, Tenant Workspace, Catalog, Integrations, Reliability, Audit & Activity, and Platform Settings
- committed and pushed `3d6cf20` for admin runtime schema/session hardening; full admin browser flow had all API calls return `200` with no console errors, page errors, or request failures
- continued Phase 19 intelligent AI on `main`: added a portal-native customer-care status turn at `POST /api/v1/portal/bookings/{booking_reference}/care-turn`, grounded in booking/payment/support/academy context plus recent revenue-ops action runs
- portal now has a customer-care status agent card for payment, class/report, support, and lifecycle-action questions; verification passed with focused portal/tenant/worker backend tests, frontend typecheck, and production build
- synced the Phase 19 portal customer-care status agent closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/052507-phase-19-portal-customer-care-status-agent.md`
- continued the same Phase 19 plan: portal care-turns now auto-queue `support_request` audit/outbox cases when customers ask for human support/escalation or report broken payment links, and admin Billing Support can review/escalate those cases
- verification passed with py_compile, focused portal tests, tenant/worker regression, frontend typecheck, and production build
- synced the Phase 19 portal care support escalation closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/053242-phase-19-portal-care-support-escalation.md`
- completed the BookedAI main WhatsApp lifecycle bot pass for `+61455301335` / `info@bookedai.au`: booking confirmations invite WhatsApp replies, inbound replies resolve booking truth safely, and clear cancel/reschedule requests queue audited portal requests.
- WhatsApp-driven change requests now send or record email confirmation, mirror CRM task state via lifecycle email sync, and surface recent request counts/details in tenant Bookings.
- verification passed with backend py_compile, focused WhatsApp/portal/tenant/communication tests, frontend typecheck/build, live deploy, stack health, API health, and live WhatsApp verify-route reachability.
- Discord closeout for the WhatsApp lifecycle bot succeeded; Notion sync retried twice and returned HTTP 403, matching the current Notion gateway failure mode already observed today.
- Added OpenClaw/Telegram `whatsapp-bot-status` operator command for the WhatsApp bot; it checks OpenClaw gateway health, API health, provider status, and webhook verify-route reachability without sending messages.
- Recreated the live OpenClaw gateway/CLI so `BOOKEDAI_TELEGRAM_ALLOWED_ACTIONS` includes `whatsapp_bot_status`; verified inside `openclaw-bookedai-cli` from `/workspace/bookedai.au` with trusted actor `8426853622`.
- Final readiness returned OK: OpenClaw live, API live, `whatsapp_twilio` connected, and WhatsApp verify route reached FastAPI with expected invalid-token `403`.
- Discord closeout for the OpenClaw WhatsApp readiness command succeeded; Notion sync again returned HTTP 403.
- Clarified the WhatsApp runtime as `BookedAI WhatsApp Booking Care Agent` for `bookedai.au`, covering all service/booking/payment/provider/status/support/cancel/reschedule questions tied to already-recorded bookings.
- Updated the OpenClaw readiness command output with agent identity, scope, and gateway boundary; verified from both repo and OpenClaw CLI mount without sending WhatsApp messages.
- Added repo-owned OpenClaw agent manifest `deploy/openclaw/agents/bookedai-whatsapp-booking-care-agent.json` plus `sync-openclaw-bookedai-agent`.
- OpenClaw v2026.4.15 rejected rich agent data in `openclaw.json agents.list`; restored schema-safe config, installed the BookedAI WhatsApp agent as a runtime `agents/` manifest, recreated OpenClaw, and verified gateway + WhatsApp readiness OK.
- redesigned the admin workspace shell into an enterprise-style control plane: sticky top bar with session/profile/settings/logout actions, compact metric strip, grouped collapsible sidebar navigation, and polished responsive admin canvas
- verification passed with frontend typecheck, production build, local desktop/mobile Playwright layout checks with no overflow/errors, and the full admin smoke suite
- pushed commit `43f74ef` to `origin/main`, published a clean dist build from that commit into the live web container, verified stack health, admin API health, and live admin login-page render with no browser errors or horizontal overflow
- refined public/product search cards with Google Maps actions, top-left thumbnails/preview fallbacks, faster staged search progress, and early-match copy while keeping clarification inside BookedAI chat
- verified the search-card update with `npm --prefix frontend run build`; docs synchronized across PRD, DESIGN, phase roadmap, sprint package, roadmap register, and implementation progress
- configured the live WhatsApp/Twilio runtime with a webhook verify token, synced the BookedAI WhatsApp Booking Care Agent manifest into OpenClaw via host-level execution, and redeployed live
- post-deploy WhatsApp readiness passed: API and OpenClaw were live, `whatsapp_twilio` reported connected, invalid verify-token probe reached FastAPI with `403`, and the real verify-token challenge returned successfully
- current env uses Twilio API-key credentials and `WHATSAPP_FROM_NUMBER=whatsapp:+14155238886`, so the active sender posture is Twilio WhatsApp sandbox-style unless the production WhatsApp sender is switched in env
- attempted WhatsApp test send to `+61481993178`; BookedAI recorded the message as queued/manual-review, and direct Twilio verification returned HTTP `401` / code `20003 Authenticate`, so the current Twilio API key/secret or account pairing must be corrected before real sends work
- switched the live WhatsApp customer-care sender from sandbox `whatsapp:+14155238886` to BookedAI number `+61455301335`, synced the OpenClaw manifest, redeployed live, and confirmed the backend container now has `WHATSAPP_FROM_NUMBER=+61455301335`
- direct Twilio provider probe now uses `from=whatsapp:+61455301335` but still returns HTTP `401` / code `20003 Authenticate`, confirming the remaining blocker is Twilio authentication rather than the BookedAI/OpenClaw sender mapping
- changed WhatsApp customer-care transport to direct Meta/WhatsApp Cloud API primary with `WHATSAPP_PROVIDER=meta` and `WHATSAPP_FALLBACK_PROVIDER=twilio`, keeping `+61455301335` as the customer-visible identity
- implemented backend fallback logic so Meta missing credentials or provider failure can fall through to Twilio backup; focused communication/lifecycle/integration tests passed and live deploy verified `whatsapp_meta` connected with fallback notes
- live test to `+61481993178` recorded message `1163a407-6033-4cd6-84b9-7c0ba1e64d52` as queued through Twilio backup because Meta credentials are not yet set and Twilio still returns delivery unavailable/auth failure
- ran a live UAT/content/UX review for `tenant.bookedai.au` and `/future-swim`: desktop/mobile routes returned `200`, tenant APIs returned `200`, no console/page errors/request failures or horizontal overflow were observed, and all workspace menu sections rendered
- captured evidence under `frontend/output/playwright/live-tenant-uat-2026-04-25/` and wrote the review artifact at `docs/development/tenant-bookedai-uat-content-ux-review-2026-04-25.md`
- top tenant UX findings: mobile sign-in Google iframe visually renders blank despite existing, mobile email CTA looks cramped, gateway copy is too auth-first, and `/future-swim` should lead with revenue proof before the dense activation/menu stack
- implemented the tenant UAT follow-up: gateway now leads with bookings/enquiries/follow-up outcome copy, includes a visible native Google CTA fallback, uses `Send code` on mobile, and Future Swim now leads with revenue proof before activation
- compressed the Future Swim mobile workspace menu and removed the duplicate lower revenue-proof block; `npm --prefix frontend run build` passed
- deployed live through the host-level deploy wrapper after a transient Docker compose name-conflict during direct deploy; final live Playwright smoke passed for tenant gateway and `/future-swim` with all tenant APIs `200`, no console/page errors, no request failures, and no horizontal overflow
- continued tenant UX polish by hiding the third-party Google iframe on mobile-visible auth surfaces, keeping only the native BookedAI Google CTA, reframing the gateway source line as trust copy, and correcting Future Swim `Next follow-up` to read as tenant attention signals
- deployed the polish live; final smoke evidence is under `frontend/output/playwright/live-tenant-ux-polish-2026-04-25/` with API/tenant routes `200`, no console/page errors, no request failures, no horizontal overflow, native Google CTA visible, and revenue proof appearing once
- locked the public BookedAI WhatsApp contact link to the Booking Customer Care Agent at `https://wa.me/61455301335`, replacing the old sandbox `wa.me/14155238886` link in the frontend landing data
- redeployed live after the link update; production web bundle contains `wa.me/61455301335`, API health passed, and WhatsApp readiness still reports `whatsapp_meta` connected for `+61455301335`
- configured live `WHATSAPP_META_PHONE_NUMBER_ID=777199388798945` for the `+61455301335` WhatsApp customer-care identity and redeployed; provider status now lists `whatsapp_meta_phone_number_id` plus `whatsapp_from_number`
- `WHATSAPP_META_ACCESS_TOKEN` is still unset, so Meta direct sending is not fully active yet; Twilio remains the configured backup path
- created and packaged a new reusable skill at `skills/qa-testing` covering professional QA/testing workflows, A/B testing briefs, and beginner-friendly manual testing guidance; because OpenClaw auto-loads `<workspace>/skills`, it is installed in the current runtime without extra config; packaged artifact: `skills/dist/qa-testing.skill`
- added a separate Vietnamese localized skill at `skills/qa-testing-vi` optimized for non-technical operators and beginners, then packaged it as `skills/dist/qa-testing-vi.skill`
- ran a focused UAT/QA review pass for `product.bookedai.au`; identified a mobile conversion bug where the free-trial CTA and flow explainer were hidden below the `sm` breakpoint, then fixed `frontend/src/apps/public/ProductApp.tsx` so mobile now shows a visible `Free Trial` CTA and the flow/status cards on small screens; frontend production build passed
- added `frontend/tests/product-app-regression.spec.ts` to lock the product runtime regression around mobile CTA visibility, flow explainer visibility, and register-interest attribution; Playwright browser download succeeded, but execution is still blocked in this runtime by missing system library `libnspr4.so`, while frontend production build still passes
- hardened product trial routing in `frontend/src/apps/public/ProductApp.tsx` so `Start Free Trial` now targets the canonical BookedAI register-interest URL via `brandHomeUrl` instead of depending on the current host origin; frontend production build passed
- continued the product-flow QA pass inside `BookingAssistantDialog`: fixed the product sticky ready-to-book banner so it appears for selected events as well as services, and changed the helper copy from "scroll down to book" to "review booking below" to reduce ambiguity; frontend production build passed
- tightened shortlist action wording in `BookingAssistantDialog` so result cards now say `Select & book` before selection and `Continue booking` after selection, reducing ambiguity between previewing results and advancing into the booking task; frontend production build passed
- continued the product-flow copy hardening for event support inside `BookingAssistantDialog`: booking-pane headers, selected-state summaries, and readiness cards now treat selected events as first-class booking targets instead of falling back to service-only copy; frontend production build passed after fixing the related TypeScript narrowing issue
- refined booking-form labels and submit CTAs for event vs service flows in `BookingAssistantDialog`: `Preferred time` becomes `Preferred attendance time` for events, summaries and sticky CTAs now distinguish `Request attendance` from `Book now`, and pre-submit status chips now reflect event attendance requests instead of service-only booking language; frontend production build passed
- softened post-submit confirmation wording in `BookingAssistantDialog` so the confirmation screen reads more naturally for both service bookings and event attendance requests, including request-focused portal actions, status headings, timeline copy, and confirmation footer text; frontend production build passed
- created workspace skills `qa-testing` and `qa-testing-vi` plus packaged `.skill` bundles under `skills/dist/`; both cover professional QA, UAT, smoke/regression/exploratory testing, A/B testing, and beginner-friendly manual testing
- verified OpenClaw workspace skill loading behavior so new skills under `/workspace/bookedai.au/skills` are available to the current runtime without extra config changes
- ran a product-page QA/UAT pass on `product.bookedai.au` and hardened `frontend/src/apps/public/ProductApp.tsx` so mobile now shows the product flow explainer and free-trial CTAs instead of hiding them below the `sm` breakpoint
- hardened product trial routing to use the canonical BookedAI register-interest path rather than `window.location.origin`, reducing host/routing dependence for trial conversion
- added `frontend/tests/product-app-regression.spec.ts` to lock mobile CTA visibility and register-interest attribution expectations for the product runtime; frontend production builds passed while full Playwright browser execution remained environment-limited
- continued product booking-flow UX fixes in `BookingAssistantDialog.tsx`: booking handoff banners now support selected events as well as services, copy was softened from `scroll down to book` to clearer review/continue wording, and shortlist booking actions were clarified toward `select` vs `continue booking` semantics to reduce search-to-book confusion
- synchronized the chess verified-tenant search requirement into the detailed design/PRD stack and active roadmap/sprint/phase docs: `prd.md`, `DESIGN.md`, master PRD, chatbot/product design spec, Sprint 13-16 package, next-phase plan, roadmap register, sprint checklist, and frontend roadmap data
- locked ownership split: Phase/Sprint 17 preserves the current results-first chess tenant proof; Phase/Sprint 22 owns reusable verified-tenant template extraction across future verticals
- added verified BookedAI chess tenant treatment to public/product search results: reviewed Co Mai Hung / Grandmaster Chess matches now show tenant capability chips for Book, Stripe, QR payment/confirmation, calendar, email, WhatsApp Agent, and portal edit/revisit without forcing booking focus
- updated selected-booking and confirmation copy so chess tenant bookings keep QR/portal confirmation, Stripe/QR payment posture, calendar/email actions, and WhatsApp Agent follow-up visible; frontend production build passed
- polished the public/product search UX into a calmer ChatGPT-like workspace: stronger composer treatment, clearer chat status, compact scan-first result cards, and matching hierarchy across homepage/product assistant results
- tightened the product welcome state after browser review so the composer is visible in the first desktop viewport, then verified with `npm --prefix frontend run build` and local Playwright screenshots
- synced the product SaaS search UX review closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/054653-product-saas-search-ux-review-and-polish.md`
- synced the ChatGPT-like search workspace polish closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/053550-public-product-chatgpt-search-workspace-polish.md`
- refined the public/product search-results flow so completed searches no longer auto-scroll or focus booking; users can pause, scroll, compare compact result cards, open detail popups, and enter booking only through explicit `Book` actions
- moved homepage clarification/follow-up prompts into the BookedAI chat thread as actionable agent chips and removed the competing standalone clarification panel
- changed popup/product assistant results so the first match is no longer auto-selected after search; `View details` and `Book` now preserve a cleaner review-to-commit path
- synchronized `project.md`, README, DESIGN, implementation progress, roadmap, and current sprint plan around the results-first search UX contract; frontend build passed
- synced the search-results flow refinement closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/052035-public-product-search-results-flow-refinement.md`
- continued the AI-agent implementation by making `HomepageSearchExperience.tsx` keep a real visible chat thread with user turns, assistant replies, inline top-result cards, and quick suggestion chips
- added `POST /api/v1/agents/customer-turn` so the customer-facing search/conversation agent has a reusable backend turn contract with reply, search payload, missing context, suggestions, and next-agent handoff metadata
- homepage live-read search now calls the backend customer-turn contract first and renders its reply/search payload in the chat workspace
- popup/product assistant search now also calls the backend customer-turn contract first, keeping homepage and embedded/product assistant search behavior aligned while preserving legacy streaming fallback
- clarification answers now continue the chat and rerun search with added context, moving the public flow closer to `ask -> refine -> results -> book`
- added `POST /api/v1/revenue-ops/handoffs` so a booking lifecycle event can queue SME revenue-operations work in `agent_action_runs`
- homepage and popup/product post-booking automation now queue revenue-ops actions for lead follow-up, payment reminder, CRM sync, customer-care status monitoring, and webhook callback after payment/communication best-effort steps
- admin Reliability ledger filters now include generic SME action types beside the chess academy actions
- verification passed with focused backend search/academy/worker tests and `npm --prefix frontend run build`
- added `docs/development/next-phase-implementation-plan-2026-04-25.md` and synchronized `prd.md`, `project.md`, the roadmap, sprint execution plan, sprint package, and implementation-progress docs around the next phase sequence
- locked the post-release order as Phase 17 full-flow stabilization, Phase 18 revenue-ops ledger control, Phase 19 customer-care/status agent, Phase 20 widget/plugin runtime, Phase 21 billing/receivables truth, Phase 22 reusable templates, and Phase 23 release governance
- synced the next-phase closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/004348-next-phase-implementation-plan-and-full-flow-closeout.md`
- started Phase 18 by extending `GET /api/v1/agent-actions` with entity/agent/dependency filters and summary counts, adding admin entity/dependency filters, and adding a tenant `Ops` panel for read-only revenue-ops action visibility
- verification passed with focused academy/worker tests, frontend typecheck, and frontend production build
- synced Phase 18 closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/010506-phase-18-revenue-ops-ledger-tenant-visibility.md`
- switched the AI-agent continuation work back onto `main`, reapplied the AI-agent and Future Swim stashes cleanly, confirmed no merge markers or whitespace errors, and re-verified with focused backend tests plus frontend production build
- continued Phase 18 on `main`: revenue-ops action runs now expose lifecycle-event filtering plus derived dependency, policy, approval, and evidence summary metadata; admin Reliability and tenant Ops both render the new posture fields
- synced the Phase 18 action metadata closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/010947-phase-18-revenue-ops-action-metadata-and-tenant-admin-evidence-posture.md`
- replaced the main public/product/demo/shared header brand treatment with the uploaded BookedAI logo asset and responsive cropped frames so top-left branding stays visible without overflow
- upgraded the shared landing menu defaults to Product, Live Demo, Tenant Login, and Roadmap with lucide menu glyphs
- hardened the public homepage live-read booking flow: customer-agent turn now falls back to v1 matching/search, clarification no longer blocks service+location queries, and booking CTAs/status labels match the regression-tested flow
- verification passed with frontend typecheck/build, public homepage responsive Playwright coverage, and focused live-read booking smoke cases for v1 booking-intent submission and near-me location guardrails
- deployed the brand/menu and booking-flow hardening live with `telegram_workspace_ops.py deploy-live`; stack health passed and live browser smoke confirmed demo/product/pitch/tenant render without page errors or horizontal overflow
- Discord closeout was sent after live verification; the first Notion sync succeeded, while later re-sync attempts for the updated live-verification note returned HTTP 403
- added the uploaded `Chess_screen` asset before the public homepage and pitch hero areas as a responsive chess academy proof band; frontend typecheck/build, deploy-live, stack health, and live pitch Playwright smoke at mobile/desktop widths passed with no horizontal overflow
- refined the final `pitch.bookedai.au` CTA so the latest uploaded image sits in a full-width 3:2 frame and the verbose footer positioning/release text is hidden on pitch; frontend build and local Playwright checks at `390px` and `1440px` passed with no horizontal overflow
- adjusted the top `pitch.bookedai.au` chess proof image into a padded 3:2 `object-contain` screen frame so it fits the card without being cropped; frontend build, deploy-live, stack health, and local/live Playwright checks passed at `390px` and `1440px`
- optimized the uploaded logo, chess proof, final contact proof, and pitch team images into local WebP assets; production pitch smoke confirmed no `upload.bookedai.au/images` requests, stack health passed, and mobile/desktop had no horizontal overflow
- simplified the tenant gateway login UX into a Google-first enterprise access screen with email-code fallback and explicit create-account mode
- hardened tenant Google auth so gateway sign-in without an active membership returns `tenant_google_membership_not_found` instead of silently creating a new workspace; the create-account path still creates new tenant workspaces intentionally
- verification passed with focused tenant backend auth tests, frontend production build, and local Playwright desktop/mobile tenant gateway QA with no console errors or mobile horizontal overflow
- diagnosed `admin.bookedai.au` login fail as `/api/` static routing on the admin host: live `/api/admin/login` returned frontend nginx `405`, and `/api/health` returned the admin HTML shell
- added explicit `/api/` backend proxying for the admin host in `deploy/nginx/bookedai.au.conf` plus frontend-container `frontend/nginx/default.conf`, so the shared Vite admin login reaches FastAPI again after deploy
- restarted the live proxy only after `nginx -t` passed; `admin.bookedai.au/api/health` now returns backend JSON and bad-password login probes return backend `401`, confirming the live login route is no longer swallowed by SPA fallback
- reorganized the shipped admin workspace into a compact sidebar shell grouped by Operate, Tenants, Revenue, and Platform, with Catalog stacked so partner profile actions remain viewport-safe
- pushed commit `71436df` to `origin/main`, deployed live with `telegram_workspace_ops.py deploy-live`, and verified stack health plus live admin probes; `/api/health` returns backend JSON and bad-password login now returns backend `401`
- full QA passed before deploy: frontend typecheck, admin smoke, backend `pytest` across `backend/tests`, and `scripts/run_release_gate.sh`
- redesigned `portal.bookedai.au` into an enterprise customer workspace with secure lookup, booking/payment/support status cards, shared overview/edit/reschedule/pause/downgrade/cancel navigation, provider/customer detail panels, academy progress, timeline, and sticky desktop action rail
- fixed portal request-composer close behavior so it returns to overview and syncs URL state cleanly; request mutations remain queued through the existing portal backend endpoints
- added focused Playwright portal coverage for render, reschedule submission, and mobile no-overflow; verification passed with frontend build, `.venv` backend portal route unittest, and external-server Playwright portal smoke
- tightened the product/public booking runtime UX: progressive first matches during slow live search, explicit select-vs-book behavior, compact result icon action rows, portal-first QR confirmation, continue-chat action, and a persistent Thank You confirmation that no longer auto-returns
- verified the booking UX pass with focused backend booking/search/communication tests, frontend production build, and the live-read Playwright smoke for v1 booking intent plus near-me location guardrail
- ran full live QA on `tenant.bookedai.au`; fixed Future Swim workspace failures from asyncpg audit nullable-filter typing, revenue-metrics integer interval typing, and a React hook-order crash after loading
- deployed refreshed backend/web services and verified stack health, live tenant API `200` + CORS for all Future Swim endpoints, no browser console errors, and no mobile horizontal overflow on the gateway or `/future-swim`
- added the latest uploaded workspace screenshot to the tenant login hero as local optimized WebP assets, deployed web services, and verified live desktop/mobile tenant gateway layout with no console errors or horizontal overflow
- added a pitch architecture visual that explains the whole project as `customer surfaces -> AI agents -> booking core -> operations truth`
- updated the public roadmap code dataset to expose Phase/Sprint 17-23 directly, covering stabilization, revenue ops, customer care, widget runtime, billing truth, templates, and release governance
- synchronized `project.md`, `prd.md`, roadmap/progress docs, README, DESIGN, and system overview around the new roadmap and pitch architecture visualization baseline
- verification passed with frontend typecheck, frontend production build, and local Playwright CLI smoke for `/pitch-deck` plus `/roadmap`; Notion and Discord sync succeeded via `telegram_workspace_ops.py sync-doc`
- continued tenant AI operations automation on `main`: tenant integrations now expose an automation connection plan, and tenant admins/operators can run policy-gated queued revenue-ops actions through `POST /api/v1/tenant/operations/dispatch`
- synced the tenant AI operations automation closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/051218-tenant-ai-operations-automation-connection-and-dispatch.md`
- started Phase 19 customer-care/status delivery through WhatsApp: inbound configured WhatsApp messages now resolve booking identity, answer from portal booking/payment/support truth, and queue audited cancel/reschedule portal requests when the customer clearly asks to change an existing booking
- synchronized the WhatsApp customer-care scope into `project.md`, `prd.md`, README/DESIGN, roadmap/phase docs, implementation progress, system overview, and archive note `docs/development/telegram-sync/2026-04-25/052534-whatsapp-customer-care-booking-channel.md`
- verification passed with backend py_compile, focused WhatsApp webhook/portal/communication route tests, final live deploy after JSON-safe event metadata hardening, stack health, live API health, and live WhatsApp webhook route reachability
- post-deploy Discord closeout succeeded via `docs/development/telegram-sync/2026-04-25/052724-whatsapp-customer-care-booking-channel-deployed.md`; initial Notion sync succeeded, while the post-deploy Notion re-sync returned HTTP 403
- hardened the remaining `admin.bookedai.au` login failure path by forcing the admin host to use same-origin `/api` before `VITE_API_BASE_URL`, normalizing raw browser network errors, and redesigning the login screen as an enterprise control-plane entry surface
- verification passed with frontend typecheck/build, admin smoke, full backend pytest, release gate, and a hostname-mapped Playwright probe proving login posts to `/api/admin/login` on the admin origin
- tested the real admin credential live: fixed missing admin session signing-secret env exposure, added runtime outbox retry columns via migration `019`, corrected admin payment-attention customer lookup to join `contacts`, hot-restarted backend, and verified live admin login plus read-only workspace navigation across Overview, Billing Support, Messaging, Tenants, Tenant Workspace, Catalog, Integrations, Reliability, Audit & Activity, and Platform Settings
- committed and pushed `3d6cf20` for admin runtime schema/session hardening; full admin browser flow had all API calls return `200` with no console errors, page errors, or request failures
- continued Phase 19 intelligent AI on `main`: added a portal-native customer-care status turn at `POST /api/v1/portal/bookings/{booking_reference}/care-turn`, grounded in booking/payment/support/academy context plus recent revenue-ops action runs
- portal now has a customer-care status agent card for payment, class/report, support, and lifecycle-action questions; verification passed with focused portal/tenant/worker backend tests, frontend typecheck, and production build
- synced the Phase 19 portal customer-care status agent closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/052507-phase-19-portal-customer-care-status-agent.md`
- continued the same Phase 19 plan: portal care-turns now auto-queue `support_request` audit/outbox cases when customers ask for human support/escalation or report broken payment links, and admin Billing Support can review/escalate those cases
- verification passed with py_compile, focused portal tests, tenant/worker regression, frontend typecheck, and production build
- synced the Phase 19 portal care support escalation closeout to Notion and Discord; archive stored at `docs/development/telegram-sync/2026-04-25/053242-phase-19-portal-care-support-escalation.md`
- completed the BookedAI main WhatsApp lifecycle bot pass for `+61455301335` / `info@bookedai.au`: booking confirmations invite WhatsApp replies, inbound replies resolve booking truth safely, and clear cancel/reschedule requests queue audited portal requests.
- WhatsApp-driven change requests now send or record email confirmation, mirror CRM task state via lifecycle email sync, and surface recent request counts/details in tenant Bookings.
- verification passed with backend py_compile, focused WhatsApp/portal/tenant/communication tests, frontend typecheck/build, live deploy, stack health, API health, and live WhatsApp verify-route reachability.
- Discord closeout for the WhatsApp lifecycle bot succeeded; Notion sync retried twice and returned HTTP 403, matching the current Notion gateway failure mode already observed today.
- Added OpenClaw/Telegram `whatsapp-bot-status` operator command for the WhatsApp bot; it checks OpenClaw gateway health, API health, provider status, and webhook verify-route reachability without sending messages.
- Recreated the live OpenClaw gateway/CLI so `BOOKEDAI_TELEGRAM_ALLOWED_ACTIONS` includes `whatsapp_bot_status`; verified inside `openclaw-bookedai-cli` from `/workspace/bookedai.au` with trusted actor `8426853622`.
- Final readiness returned OK: OpenClaw live, API live, `whatsapp_twilio` connected, and WhatsApp verify route reached FastAPI with expected invalid-token `403`.
- Discord closeout for the OpenClaw WhatsApp readiness command succeeded; Notion sync again returned HTTP 403.
- Clarified the WhatsApp runtime as `BookedAI WhatsApp Booking Care Agent` for `bookedai.au`, covering all service/booking/payment/provider/status/support/cancel/reschedule questions tied to already-recorded bookings.
- Updated the OpenClaw readiness command output with agent identity, scope, and gateway boundary; verified from both repo and OpenClaw CLI mount without sending WhatsApp messages.
- Added repo-owned OpenClaw agent manifest `deploy/openclaw/agents/bookedai-whatsapp-booking-care-agent.json` plus `sync-openclaw-bookedai-agent`.
- OpenClaw v2026.4.15 rejected rich agent data in `openclaw.json agents.list`; restored schema-safe config, installed the BookedAI WhatsApp agent as a runtime `agents/` manifest, recreated OpenClaw, and verified gateway + WhatsApp readiness OK.
- redesigned the admin workspace shell into an enterprise-style control plane: sticky top bar with session/profile/settings/logout actions, compact metric strip, grouped collapsible sidebar navigation, and polished responsive admin canvas
- verification passed with frontend typecheck, production build, local desktop/mobile Playwright layout checks with no overflow/errors, and the full admin smoke suite
- pushed commit `43f74ef` to `origin/main`, published a clean dist build from that commit into the live web container, verified stack health, admin API health, and live admin login-page render with no browser errors or horizontal overflow
- refined public/product search cards with Google Maps actions, top-left thumbnails/preview fallbacks, faster staged search progress, and early-match copy while keeping clarification inside BookedAI chat
- verified the search-card update with `npm --prefix frontend run build`; docs synchronized across PRD, DESIGN, phase roadmap, sprint package, roadmap register, and implementation progress
- configured the live WhatsApp/Twilio runtime with a webhook verify token, synced the BookedAI WhatsApp Booking Care Agent manifest into OpenClaw via host-level execution, and redeployed live
- post-deploy WhatsApp readiness passed: API and OpenClaw were live, `whatsapp_twilio` reported connected, invalid verify-token probe reached FastAPI with `403`, and the real verify-token challenge returned successfully
- current env uses Twilio API-key credentials and `WHATSAPP_FROM_NUMBER=whatsapp:+14155238886`, so the active sender posture is Twilio WhatsApp sandbox-style unless the production WhatsApp sender is switched in env
- attempted WhatsApp test send to `+61481993178`; BookedAI recorded the message as queued/manual-review, and direct Twilio verification returned HTTP `401` / code `20003 Authenticate`, so the current Twilio API key/secret or account pairing must be corrected before real sends work
- switched the live WhatsApp customer-care sender from sandbox `whatsapp:+14155238886` to BookedAI number `+61455301335`, synced the OpenClaw manifest, redeployed live, and confirmed the backend container now has `WHATSAPP_FROM_NUMBER=+61455301335`
- direct Twilio provider probe now uses `from=whatsapp:+61455301335` but still returns HTTP `401` / code `20003 Authenticate`, confirming the remaining blocker is Twilio authentication rather than the BookedAI/OpenClaw sender mapping
- changed WhatsApp customer-care transport to direct Meta/WhatsApp Cloud API primary with `WHATSAPP_PROVIDER=meta` and `WHATSAPP_FALLBACK_PROVIDER=twilio`, keeping `+61455301335` as the customer-visible identity
- implemented backend fallback logic so Meta missing credentials or provider failure can fall through to Twilio backup; focused communication/lifecycle/integration tests passed and live deploy verified `whatsapp_meta` connected with fallback notes
- live test to `+61481993178` recorded message `1163a407-6033-4cd6-84b9-7c0ba1e64d52` as queued through Twilio backup because Meta credentials are not yet set and Twilio still returns delivery unavailable/auth failure
- locked the public BookedAI WhatsApp contact link to the Booking Customer Care Agent at `https://wa.me/61455301335`, replacing the old sandbox `wa.me/14155238886` link in the frontend landing data
- redeployed live after the link update; production web bundle contains `wa.me/61455301335`, API health passed, and WhatsApp readiness still reports `whatsapp_meta` connected for `+61455301335`
- created and packaged a new reusable skill at `skills/qa-testing` covering professional QA/testing workflows, A/B testing briefs, and beginner-friendly manual testing guidance; because OpenClaw auto-loads `<workspace>/skills`, it is installed in the current runtime without extra config; packaged artifact: `skills/dist/qa-testing.skill`
- added a separate Vietnamese localized skill at `skills/qa-testing-vi` optimized for non-technical operators and beginners, then packaged it as `skills/dist/qa-testing-vi.skill`
- ran a focused UAT/QA review pass for `product.bookedai.au`; identified a mobile conversion bug where the free-trial CTA and flow explainer were hidden below the `sm` breakpoint, then fixed `frontend/src/apps/public/ProductApp.tsx` so mobile now shows a visible `Free Trial` CTA and the flow/status cards on small screens; frontend production build passed
- added `frontend/tests/product-app-regression.spec.ts` to lock the product runtime regression around mobile CTA visibility, flow explainer visibility, and register-interest attribution; Playwright browser download succeeded, but execution is still blocked in this runtime by missing system library `libnspr4.so`, while frontend production build still passes
- hardened product trial routing in `frontend/src/apps/public/ProductApp.tsx` so `Start Free Trial` now targets the canonical BookedAI register-interest URL via `brandHomeUrl` instead of depending on the current host origin; frontend production build passed
- continued the product-flow QA pass inside `BookingAssistantDialog`: fixed the product sticky ready-to-book banner so it appears for selected events as well as services, and changed the helper copy from "scroll down to book" to "review booking below" to reduce ambiguity; frontend production build passed
- tightened shortlist action wording in `BookingAssistantDialog` so result cards now say `Select & book` before selection and `Continue booking` after selection, reducing ambiguity between previewing results and advancing into the booking task; frontend production build passed
- continued the product-flow copy hardening for event support inside `BookingAssistantDialog`: booking-pane headers, selected-state summaries, and readiness cards now treat selected events as first-class booking targets instead of falling back to service-only copy; frontend production build passed after fixing the related TypeScript narrowing issue
- refined booking-form labels and submit CTAs for event vs service flows in `BookingAssistantDialog`: `Preferred time` becomes `Preferred attendance time` for events, summaries and sticky CTAs now distinguish `Request attendance` from `Book now`, and pre-submit status chips now reflect event attendance requests instead of service-only booking language; frontend production build passed
- softened post-submit confirmation wording in `BookingAssistantDialog` so the confirmation screen reads more naturally for both service bookings and event attendance requests, including request-focused portal actions, status headings, timeline copy, and confirmation footer text; frontend production build passed
- created workspace skills `qa-testing` and `qa-testing-vi` plus packaged `.skill` bundles under `skills/dist/`; both cover professional QA, UAT, smoke/regression/exploratory testing, A/B testing, and beginner-friendly manual testing
- verified OpenClaw workspace skill loading behavior so new skills under `/workspace/bookedai.au/skills` are available to the current runtime without extra config changes
- ran a product-page QA/UAT pass on `product.bookedai.au` and hardened `frontend/src/apps/public/ProductApp.tsx` so mobile now shows the product flow explainer and free-trial CTAs instead of hiding them below the `sm` breakpoint
- hardened product trial routing to use the canonical BookedAI register-interest path rather than `window.location.origin`, reducing host/routing dependence for trial conversion
- added `frontend/tests/product-app-regression.spec.ts` to lock mobile CTA visibility and register-interest attribution expectations for the product runtime; frontend production builds passed while full Playwright browser execution remained environment-limited
- continued product booking-flow UX fixes in `BookingAssistantDialog.tsx`: booking handoff banners now support selected events as well as services, copy was softened from `scroll down to book` to clearer review/continue wording, and shortlist booking actions were clarified toward `select` vs `continue booking` semantics to reduce search-to-book confusion
- continued `BookingAssistantDialog.tsx` QA on the product layout confirmation flow: post-submit portal action labels, request/follow-up headings, confirmation timeline wording, and footer guidance were softened from service-only `booking` language toward request-neutral copy so event attendance flows stay consistent after submit; frontend production build passed
- expanded `frontend/tests/product-app-regression.spec.ts` to cover the new product assistant UX wording: one regression now locks event-selection booking-pane copy (`Preferred attendance time`, `Attendance summary`, `Request attendance`, `Ready to request`), and another locks request-focused confirmation wording after a submitted service flow (`Edit details`, `Cancel request`, request/follow-up headings); frontend production build passed, while browser execution remains environment-limited by missing system libraries
- fixed the real product event-submit blocker in `BookingAssistantDialog.tsx`: selecting an event now clears stale service selection, submit no longer hard-fails on missing `selectedService`, and event attendance requests generate a valid confirmation/result state instead of entering the service-only booking-session path; frontend production build passed
- extended the backend `/booking-assistant/session` path to support native event attendance requests via synthetic `event:` service ids plus event metadata, relaxed zero-price schema validation for event responses, and reconnected frontend event submit to the real backend session endpoint; frontend production build passed, while backend test execution was blocked in this runtime because the local Python environment lacks installed test deps like `pytest` and `httpx`
- completed a fallback backend verification pass without Python package tooling: source-level checks confirmed the new event session fields exist in `backend/schemas.py`, zero-price event responses are allowed, `BookingAssistantService.create_session()` detects synthetic `event:` ids, builds an event proxy service, skips Stripe/manual payment URLs for event requests, and returns an attendance-request confirmation; matching frontend source checks confirmed event submit now posts event metadata into `/booking-assistant/session`
- ran live UAT/content review for `pitch.bookedai.au` using Playwright plus workspace QA skills; HTTP/render/CTA routing passed on desktop/tablet/mobile with no app console errors or request failures beyond Cloudflare RUM aborts during navigation
- found a serious mobile UX issue in the architecture-detail section: the page reaches roughly `44.9kpx` height and shows large blank/low-value scroll bands around the architecture operating-model area, making the pitch feel broken and too long on mobile
- content review recommendation: compress repeated architecture/proof sections into a shorter investor/SME narrative, move pricing/CTA closer to product proof, and rewrite pitch copy toward concrete buyer outcomes, live proof, and commercial terms instead of repeated system-layer language
- ran live UAT for `admin.bookedai.au`; found admin login API returning `500` after credential check because production Compose was not passing the session-signing secret family into backend containers
- fixed `docker-compose.prod.yml` so `backend` and `beta-backend` receive `SESSION_SIGNING_SECRET`, `TENANT_SESSION_SIGNING_SECRET`, and `ADMIN_SESSION_SIGNING_SECRET`; deployed live after clearing Docker build cache from a 100% root disk condition
- final admin UAT passed on desktop `1440x950` and mobile `390x844`: login plus all admin workspaces loaded with `200` API responses, no console/page/request failures, and no horizontal overflow; artifacts live under `output/playwright/admin-live-uat-2026-04-25/`
- reworked `pitch.bookedai.au` per UAT recommendations: shorter hero copy, live chess tenant proof after hero, pricing moved before architecture, and the oversized architecture-detail infographic removed in favor of one `demand -> qualification -> booking -> ops ledger` operating-model visual
- local pitch verification passed with `npm --prefix frontend run build` and Playwright desktop/mobile smoke; mobile page height dropped from the prior roughly `44.9kpx` live review issue to about `21.0kpx`, with no console errors, failed requests, or horizontal overflow
- live pitch verification passed after production web refresh: `pitch.bookedai.au` serves the new `AI receptionist and revenue ops for service businesses.` hero, pricing-before-architecture flow, and compressed operating-model section; final Playwright desktop/mobile artifacts are under `output/playwright/pitch-live-reword-uat-2026-04-25/` with no console/page/request errors or overflow
- follow-up deployment cleared the earlier container-only `PublicApp` build caveat: a clean frontend Docker build passed, `deploy-live` completed successfully, scoped `web`/`beta-web` refresh published the final hero copy, and post-deploy pitch smoke stayed clean on desktop/mobile
- removed the production Nginx redirect from `bookedai.au` / `www.bookedai.au` to `pitch.bookedai.au`; root HTTPS now proxies the normal frontend while HTTP upgrades to `https://bookedai.au`
- reloaded the live proxy after `nginx -t` passed; live checks returned `200` for `https://bookedai.au`, `https://www.bookedai.au`, and `https://pitch.bookedai.au`, with `http://bookedai.au` redirecting only to HTTPS root
- configured the live BookedAI WhatsApp customer-care runtime to use Meta as primary with Twilio as backup: `WHATSAPP_PROVIDER=meta`, `WHATSAPP_FALLBACK_PROVIDER=twilio`, `WHATSAPP_FROM_NUMBER=+61455301335`, and `WHATSAPP_META_PHONE_NUMBER_ID=777199388798945`
- stored the operator-provided Meta token only in runtime env; Graph ID lookup passed, but direct message POST still falls back because Meta returns the phone-number/object permission error for send attempts
- deployed fallback hardening so WhatsApp sends no longer 502 when Meta rejects a request; live test to `+61481993178` recorded the message, tried Meta first, then queued via Twilio/manual-review fallback
- rotated the BookedAI WhatsApp Meta runtime token from the operator-provided replacement key; live deploy completed, backend env reports the token present, Graph `id` lookup for `777199388798945` passes, and production send still queues via Twilio/manual-review fallback because Meta `POST /messages` returns code `100` / subcode `33`
- switched the live WhatsApp Meta Phone Number ID to `61564750276601`; backend containers were recreated directly after the full deploy session was interrupted, live env now reports the new ID, Graph `id` lookup passes, and production sends still queue via Twilio/manual-review fallback because Meta direct send remains blocked with code `100` / subcode `33`
- switched the live WhatsApp Meta Phone Number ID again to `1034706373065889`; Graph `id` lookup passes but direct Meta send returns `#133010 Account not registered`, so OpenClaw-agent test sends still queue via Twilio/manual-review fallback while backend health remains OK
- continued the pitch UAT rework locally: `PitchDeckApp` keeps pricing before architecture, the long architecture-detail section is removed, and the remaining architecture visual is a readable `demand -> qualification -> booking -> ops ledger` flow
- added native required/min-length validation to `RegisterInterestApp` core SME fields; frontend build and local Playwright smoke passed with no pitch overflow/errors and empty registration submit showing six invalid fields
- corrected the final live pitch hero back to `AI receptionist and revenue ops for service businesses.`, refreshed production web/beta-web, and verified post-deploy pitch desktop/mobile smoke clean
- continued pitch conversion UAT: `RegisterInterestApp` now sets `Register Interest | BookedAI SME Setup`; live desktop header CTA and mobile pricing CTA both reached `/register-interest`, preserved attribution, submitted to `/api/pricing/consultation` with `200`, returned confirmation references, and showed no app browser errors or horizontal overflow
- continued tenant UAT follow-up: `TenantApp` now adds gateway proof chips for booking flow, revenue proof, customer care, and automation, plus visible `info@bookedai.au` / `+61 455 301 335` support contacts.
- added Future Swim anchor IDs and mobile quick nav for revenue, activation, menu, bookings, and catalog; deployed live through the host wrapper and verified desktop/mobile tenant smoke clean with evidence in `frontend/output/playwright/live-tenant-ux-proof-nav-2026-04-25/`.
- deployed the latest pitch UAT rework live through `python3 scripts/telegram_workspace_ops.py deploy-live`; `pitch.bookedai.au` now serves `Convert service enquiries into confirmed bookings, follow-up, and revenue visibility.`, with pricing before architecture, one pricing section, no old architecture-detail section, and clean desktop/mobile production smoke screenshots at `output/playwright/pitch-live-final-{desktop,mobile}.png`.
- completed full tenant UAT after the proof/nav pass: restored modular `GET /api/v1/tenant/leads`, fixed tenant lead reporting against the normalized production schema (`contacts` + latest `booking_intents`), deployed live, and verified all Future Swim panels including Enquiries pass on desktop/mobile.
- added a standalone `/architecture` showcase page for technical buyers/investors: big-tech-style revenue-engine diagram, system lane map, design capability cards, product proof images, and enterprise posture; local typecheck/build and Playwright desktop/mobile smoke passed with no overflow or browser errors.
- deployed the architecture showcase live; `https://bookedai.au/architecture` and `https://pitch.bookedai.au/architecture` both pass desktop/mobile smoke with the architecture image, loaded proof images, no browser errors, and no horizontal overflow.
- upgraded the architecture block inside `PitchDeckApp` into a professional multi-layer infographic that groups surfaces, AI agents, booking/revenue modules, ops/control modules, integrations/partners, and infrastructure/technology stack; local typecheck/build plus desktop/mobile Playwright smoke passed with no overflow or browser errors.
- deployed the expanded pitch architecture infographic live; `pitch.bookedai.au` desktop/mobile smoke confirms all architecture layer, integration, and technology stack labels are present, pricing remains singular, and there are no browser errors or horizontal overflow.
- polished the live `bookedai.au` homepage hero after investor/customer review: the chess proof screenshot now uses a contained frame, the first fold has shorter visual proof language, and investor scan cards now surface `Wedge`, `Proof`, `Moat`, and `Scale`.
- fixed the backend `Settings` dataclass field order so WhatsApp Evolution defaults no longer block backend release-gate imports.
- final verification passed: frontend typecheck/build, legacy homepage smoke, live-read booking smoke, admin-smoke, backend v1/lifecycle unittest, search eval pack, live deploy, stack healthcheck, public-surface smoke, and hero Playwright check confirming `object-fit: contain` with no desktop/mobile overflow.
- upgraded homepage chat/full booking UX so `bookedai.au` shows `Ask -> Match -> Book -> Confirm`, clearer booking-brief helper cards, accessible form labels, and confirmation copy focused on QR portal, edit/reschedule/cancel request, and follow-up.
- expanded the live-read homepage booking smoke to verify the friendly full flow from query to selected match, booking form, authoritative v1 booking intent, portal confirmation, and no horizontal overflow.
- deployed the chat-flow polish live after fixing a Docker-only JSX arrow parsing issue in `PitchDeckApp`; stack health and public-surface smoke passed after deployment.
- tightened homepage chat UX so search results, booking-critical facts, action buttons, and suggestion chips now render inside assistant chat bubbles instead of a detached result board.
- live-read result candidates now merge catalog details where possible and show explicit missing-field labels (`Price not listed`, `Duration TBD`, `Location TBD`) instead of silently hiding booking facts.
- verified and deployed the inline-chat booking flow with frontend typecheck/build, live-read Playwright smoke, legacy responsive smoke, backend booking route tests, stack healthcheck, public-surface smoke, and non-mutating live homepage search/status smoke.
- refined homepage chat again into a Claude-style frame: scrollable conversation/results above, bottom composer with attach/input/voice/send, `Top research` result framing, and call/mail/map/provider/detail/book actions in the chat result cards.
- deployed the composer/result-frame polish live and verified stack health, public-surface smoke, mobile no-overflow, and non-mutating homepage composer/search/status behavior.
- reran full homepage UAT and A/B review: production desktop/tablet/mobile scored `19/19`, smoke regression plus backend booking route tests passed, screenshots/JSON live under `frontend/output/playwright/homepage-full-uat-ab-2026-04-25/`, and the recommended next A/B is a product-first `Start with a request` variant measured against search-start, booking-start, and investor-click metrics.
- clarified the original WhatsApp automation requirement as personal WhatsApp first: OpenClaw supervises the `BookedAI WhatsApp Booking Care Agent`, Evolution API/QR-session is the near-term provider while WhatsApp Business verification is pending, and Meta/Twilio remain later verified provider paths.
- updated the OpenClaw agent manifest, readiness command, PRD/project/README/OpenClaw docs, implementation progress, and long-term memory so the active webhook is `/api/webhooks/evolution` for the personal WhatsApp bridge.
- verified `py_compile`, focused WhatsApp/communication/lifecycle tests (`28 passed`), live `whatsapp-bot-status` (`whatsapp_evolution`, Evolution webhook `200`, safe to send), and synced the OpenClaw runtime manifest through host-level execution.
- attempted live WhatsApp send to `+61481993178`: backend recorded queued messages `e464080a-dcab-4611-9f7c-bff69c264dfe` and `eac0677a-bb9e-454e-9852-75b9f6787e63`, but Evolution could not send because the personal WhatsApp instance was still unpaired/`connecting`; Meta fallback returned HTTP `400`.
- upgraded the Evolution container from `atendai/evolution-api:v2.2.3` to `evoapicloud/evolution-api:v2.3.6` because v2.2.3 reproduced the known `connect -> {count:0}` QR issue; v2.3.6 successfully generated QR images under `output/whatsapp/`, but the operator still needs to scan/link the WhatsApp device before outbound messages can leave BookedAI.
- set up the OpenClaw-supervised WhatsApp agent for `+61455301335` as `BookedAI WhatsApp Booking Agent`: manifest now covers new booking intake plus existing booking-care/status requests, backend WhatsApp webhook now returns booking-intake replies for new booking messages without demanding an existing reference, and focused WhatsApp/communication/lifecycle tests passed (`29 passed`).
- generated the latest pairing QR at `output/whatsapp/bookedai-61455301335-agent-qr.png`; Evolution instance `bookedai` still reports `connecting` until the operator scans the QR with WhatsApp Linked Devices for `+61455301335`.
- after WhatsApp showed `can not link this device at this time`, recreated the unpaired Evolution instance and generated a fresh public QR at `https://bookedai.au/whatsapp/bookedai-61455301335-agent-qr-20260425-173533.png`; pairing-code endpoints were unavailable in the current Evolution image, and creating the instance with `number=61455301335` returned Evolution `400`.
- operator later reported the QR linked, but live Evolution still returned `connectionStatus=connecting`, `ownerJid=null`, and `number=null`; a backend send to `+61481993178` queued message `645bf33f-9d4e-4ba2-81ae-2d3af8dc6429` via Meta fallback, and direct Evolution send returned `500 Cannot read properties of undefined (reading 'find')`, so WhatsApp auto-chat cannot start until Evolution shows the instance as `open`.
- added Evolution session identity env (`CONFIG_SESSION_PHONE_CLIENT=BookedAI WhatsApp Booking Agent`, `CONFIG_SESSION_PHONE_NAME=Chrome`), recreated the unpaired `bookedai` instance, and published a fresh QR at `https://bookedai.au/whatsapp/bookedai-61455301335-agent-qr-20260425-180025.png`; this is intended to work around WhatsApp's `can not link this device at this time` rejection by presenting a cleaner linked-device identity.
- after the operator still saw the WhatsApp linked-device rejection, created a completely separate Evolution instance `bookedai61455301335` and published `https://bookedai.au/whatsapp/bookedai61455301335-qr-latest.png`; if this fresh instance is also rejected, the blocker is likely WhatsApp-side linked-device/rate-limit policy for the number rather than stale Evolution instance state.
- switched the OpenClaw WhatsApp Booking Agent test account to the alternate Australian number `+61481993178`: created Evolution instance `bookedai61481993178`, published QR `https://bookedai.au/whatsapp/bookedai61481993178-qr-latest.png`, updated live `.env` to `WHATSAPP_FROM_NUMBER=+61481993178` and `WHATSAPP_EVOLUTION_INSTANCE=bookedai61481993178`, synced the OpenClaw manifest, and restarted backend/beta-backend; instance remains `connecting` until the operator scans the QR from that WhatsApp account.
- operator reported the `+61481993178` account was linked, but Evolution still showed `bookedai61481993178` as `connecting` with `ownerJid=null`/`number=null` after polling; generated a refreshed QR at `https://bookedai.au/whatsapp/bookedai61481993178-qr-latest.png` and advised removing any half-linked device before rescanning.
- attempted outbound WhatsApp test to `+84908444095`; backend recorded message `fecd85cc-80bb-4b4d-aa91-e74a9ea92a14` as `queued`, because primary `whatsapp_evolution` remains unavailable while instance `bookedai61481993178` is still `connecting`, and Meta fallback returned HTTP `400`.
- ran a full WhatsApp flow test for `+84908444095`: readiness/API/webhook probes passed, backend env confirmed `WHATSAPP_FROM_NUMBER=+61481993178` and `WHATSAPP_EVOLUTION_INSTANCE=bookedai61481993178`, outbound API send recorded `c2b30388-5787-458a-9e02-3dc0a57e7c51` as queued via Meta fallback, direct Evolution send did not complete because the instance is still `connecting`, and simulated inbound Evolution webhook processed `messages_processed=1` while its auto-reply also queued due outbound provider unavailability.
- ran a full Twilio-channel code test without switching production provider: `CommunicationService` forced to `whatsapp_provider=twilio` returned `queued` for both configured sender `+61481993178` and sandbox sender `+14155238886`; direct Twilio REST probes for both returned HTTP `401` code `20003 Authenticate`, confirming the blocker is Twilio credential/account authentication. Simulated Twilio inbound form webhook still processed successfully with `messages_processed=1`.
- embedded the latest uploaded pitch video (`https://upload.bookedai.au/videos/e3d3/e0FeUWfasDxUbrvxObhHcw.mp4`) on both `bookedai.au` and `pitch.bookedai.au` with native controls, placing it after the opening proof/hero area so visitors can watch before continuing into product proof or the deeper pitch narrative; TypeScript/build, live deploy, stack health, production video-section smoke, and direct MP4 range checks passed.
- reposted the 2026-04-23 change summary to the Discord pinboard channel `1492396018086576268` through `python3 scripts/telegram_workspace_ops.py sync-doc --skip-notion --require-discord`; Discord returned `sent` and archive saved at `docs/development/telegram-sync/2026-04-25/182922-bookedai-23-april-change-summary.md`.
- redeployed the full live stack with `python3 scripts/telegram_workspace_ops.py deploy-live`; deployment completed, n8n intake workflow reactivated, stack health passed at `2026-04-25T19:07:49Z`, API `/api/health` returned ok, and live smoke passed for home, pitch, product, tenant, portal, and admin with HTTP 200, expected visible copy, no console errors, and no mobile overflow.
- tightened the pitch architecture/bottom polish locally: `PitchDeckApp` now uses a compact SVG architecture image with short support rails, the final CTA/footer are lighter, and the verbose `AI Revenue Engine for Service Businesses` brand-copy block is absent from the pitch page; frontend build and local Playwright desktop/mobile overflow checks passed.
- deployed the pitch architecture/footer polish live; stack health passed at `2026-04-25T19:52:25Z`, live pitch desktop/mobile smoke confirmed no horizontal overflow, architecture image present, removed footer copy absent, and uploaded pitch video range serving intact.
- removed the homepage booking confirmation auto-return/countdown: after a successful booking, the Thank You state, booking reference, QR portal, and follow-up actions remain visible until the customer chooses another action.
- added homepage A/B/funnel event coverage for product-first CTA/search/result/booking milestones and Playwright assertions that the old `Returning to the main BookedAI screen` countdown is absent after booking.
- verification passed with frontend typecheck/build, homepage responsive A/B Playwright, live-read booking smoke, legacy homepage smoke, and backend v1 booking route tests.
