- fixed the customer Telegram existing-booking payment/status false-escalation path: prior assistant history containing `Support contact` is no longer passed into portal-care support-queue detection, so neutral payment checks like `v1-6dfc0946e4` do not automatically queue support requests
- added booking portal and QR inline controls to Telegram existing-booking care replies so pending-payment/status answers have a direct order reopen path
- verification passed: `.venv/bin/python -m py_compile backend/service_layer/messaging_automation_service.py backend/tests/test_telegram_webhook_routes.py`; `.venv/bin/python -m pytest backend/tests/test_telegram_webhook_routes.py -q` (`14 passed`)
- operator sync archived the closeout at `docs/development/telegram-sync/2026-04-27/092323-customer-telegram-payment-care-false-escalation-fix.md`; Notion and Discord both succeeded
- rebuilt and redeployed the full live BookedAI stack with `python3 scripts/telegram_workspace_ops.py deploy-live`; backend, beta-backend, web, and beta-web images rebuilt, containers recreated, proxy restarted, and n8n workflow activated
- post-deploy verification passed with `bash scripts/healthcheck_stack.sh` at `2026-04-27T00:09:13Z`
- live HTTP smoke returned `200` for API health, public, product, portal, tenant, admin, pitch, beta, and bot health surfaces
- host Docker status showed BookedAI production containers newly up; one unrelated non-BookedAI container named `optimistic_greider` remained unhealthy and was not part of the deploy closeout
- removed stray unhealthy container `optimistic_greider` after confirming it had no mounts, ports, compose ownership, or restart policy; it was a leftover OpenClaw debug/grep container and BookedAI/OpenClaw/Supabase services remained up
- post-cleanup stack health passed again at `2026-04-27T00:11:53Z`; GitHub sync excluded `.github/workflows/release-gate.yml` because the available GitHub token still lacks `workflow` scope
- completed local Product full-flow UI/UX merge for `product.bookedai.au`: `Chat -> Search -> Preview -> Select -> Book -> Payment posture -> Thank You/Portal -> Calendar/CRM/email/messaging -> Telegram/customer-care handoff`
- updated Product shell copy, assistant journey/status copy, confirmation messages/timeline, and regression tests; Product shortcuts now show Chess, Future Swim, AI Event WSTI, and AI Mentor 1-1; frontend typecheck passed and Product Playwright regression/UAT passed (`6 passed`), including mobile `390x844` and desktop web app preview-to-book flow
- live deploy/smoke for this Product UI pass completed later on `2026-04-27`
- completed QA follow-up for Product flow: removed duplicate post-booking QR, added one-QR regression assertions, restored live-read result provenance/next-step chips, and added a chat-stream soft fallback so V1 search still renders if `/chat/send` fails
- verification passed: Product regression (`6 passed`), live-read Product popup UAT (`4 passed`), frontend build, release gate with frontend/tenant/backend/search all passing, and backend booking route tests (`7 passed`)
- tightened Product slow-search and booking-to-care flow: instant local matches now appear while live-read verifies ranking/booking actions, duplicate shadow search is avoided after live-read grounding, Thank You uses compact portal/Telegram decision cards, email previews show `info@bookedai.au`, and linked Telegram follow-up via `@BookedAI_Manager_Bot` is queued/sent only when a customer Telegram chat is actually linked
- promoted the Product full-flow pass live with `python3 scripts/telegram_workspace_ops.py deploy-live`; stack health passed at `2026-04-27T02:45:45Z`, `product.bookedai.au` returned `HTTP/2 200`, the served Product bundle contains the slow-search status/Telegram/email/shortcut updates, and live mobile smoke at `390x844` confirmed all four shortcuts plus no horizontal overflow
- operator sync archived the live-promotion notes and posted Discord summaries; Notion sync returned external `HTTP 403` on two attempts
- placed uploaded product-proof screenshot `https://upload.bookedai.au/images/df6e/iarJydFRgp1aWGk5UF0d7g.png` after the chess proof on `bookedai.au` and inside the `Product proof` section on `pitch.bookedai.au`; docs now track the placement as a public/pitch evidence-surface change
- verification passed with frontend typecheck, frontend build, and `git diff --check`; Notion/Discord sync succeeded with archive `docs/development/telegram-sync/2026-04-27/030128-public-and-pitch-product-proof-image-placement.md`; live deploy was not run
- refreshed the embedded public pitch video on `bookedai.au` and `pitch.bookedai.au` to `https://upload.bookedai.au/videos/9eb8/BhVuOlB2QXlBo-_nyOFCcA.mp4`; frontend build, deploy-live, stack health, direct byte-range probe, and live bundle checks passed
- reviewed Telegram customer booking flow end-to-end against CRM/email handoff paths and found a gap: Telegram `Book n` capture created `booking_intents` but did not trigger booking follow-up CRM records or confirmation-email lifecycle tracking like the v1 web booking flow
- patched Telegram messaging flow so captured booking intents now carry customer/contact fields in metadata and trigger CRM deal/task sync plus lifecycle email recording/send attempts when customer email is present; syntax check passed with `python3 -m py_compile`
- QA environment note: repo runtime here currently lacks installed FastAPI/pytest deps in the available Python envs, so full automated route tests could not be executed in-session; static review plus Python syntax validation completed instead
- added an admin integrations reliability view for customer-booking Telegram care, exposing customer-agent health, webhook backlog, callback ack posture, identity-resolution watchlist, and tenant-scoped Telegram/CRM/email/outbox spotlight cards inside the admin workspace
- added Telegram deep-link entry points on booking and post-booking surfaces in both the homepage booking flow and landing assistant dialog; post-booking links now open `@BookedAI_Manager_Bot` with a booking-context start payload, and pre-booking links open Telegram care with selected-service context
- Telegram webhook intake now rewrites deep-link `/start` payloads like `bk.<booking_reference>` and `svc.<service>` into useful booking-help text so the new chat session lands with the right booking or product context; frontend typecheck and backend `py_compile` both passed
- synced the Phase 19 Telegram/customer-care follow-up across `project.md`, implementation progress, next-phase plan, master roadmap, current sprint execution plan, and phase operating system so docs now reflect Telegram handoff, admin reliability spotlight, webhook metadata preservation, and the remaining verification gates
- QA follow-up added stronger Telegram route-test expectations for `Book n` capture: lifecycle metadata must now include CRM deal/task and email-confirmation evidence, with recipient verification for `long@example.com`; direct pytest remains blocked until a clean backend test env is rebuilt
- backend test-env investigation found the current `.venv` and `.venv-backend` are broken mixed-version environments; recommended next lane is a fresh local Python 3.11 `.venv-test` from `backend/requirements.txt` plus `pytest`, with Docker-image execution as the parity fallback
- prepared a concrete live UAT checklist for tenant `co-mai-hung-chess-class` covering Telegram search/book, confirmation email, portal reopen, existing-order continuity, and CRM sync evidence, using one booking reference as the end-to-end proof artifact
- operator clarified tenant-chess UAT credentials/context: use `chess@bookedai.au` instead of `long@example.com`; this mailbox is also the tenant login user, default password is `FirstHundredM$`, and the tenant chess Telegram id is `@Dvl_Aus`
- added a Telegram regression test for the merged-side-effects edge case so one webhook turn can carry both booking-intent lifecycle evidence and queued-request lifecycle evidence without overwriting `metadata.lifecycle_updates`; `python3 -m py_compile backend/tests/test_telegram_webhook_routes.py backend/api/route_handlers.py` passed
- attempted to rebuild a clean backend test env, but this runtime initially lacked `python3.11-venv`, `pip`, `ensurepip`, and Docker; worked around it by bootstrapping user-local `pip` with `get-pip.py --break-system-packages`, installing `virtualenv`, and creating a clean repo-local `.venv-test` on Python 3.11
- direct pytest from the workspace then hit a repo `.env` permission block, so the backend tree was copied to `/tmp/bookedai-test` for a clean read without touching protected secrets; from there `/.venv-test/bin/python -m pytest backend/tests/test_telegram_webhook_routes.py -q` passed (`12 passed`) and `/.venv-test/bin/python -m pytest backend/tests/test_whatsapp_webhook_routes.py -q` passed (`13 passed`)
- created `docs/development/chess-telegram-live-uat-quick-run-2026-04-27.md`, `docs/development/chess-telegram-live-uat-evidence-log-2026-04-27.md`, and `docs/development/chess-telegram-live-uat-closeout-template-2026-04-27.md` so the chess Telegram live UAT now has operator run, evidence, and closeout templates
- checked OpenClaw runtime config: Telegram interaction for trusted operator Aus is already active in `/home/node/.openclaw/openclaw.json` with `channels.telegram.enabled=true`, `dmPolicy=allowlist`, `allowFrom=[8426853622]`, and elevated Telegram allow-from set to the same actor; syncing the separate BookedAI customer-agent manifest is currently blocked by permission denied on `/home/node/.openclaw/agents/bookedai-booking-customer-agent.json`
- verified the public customer-bot handle `@BookedAI_Manager_Bot` resolves on Telegram with a live `Start Bot` page; because Telegram customer bots cannot initiate a first DM, sent Aus a proactive Telegram message through the operator bot with deep links for tenant-chess (`https://t.me/BookedAI_Manager_Bot?start=svc.co-mai-hung-chess-class`), generic customer chat, and the booking-reference continuity pattern `?start=bk.<booking_reference>`
- redeployed the live stack on `2026-04-27` through `python3 scripts/telegram_workspace_ops.py deploy-live`; backend/beta-backend/web/beta-web rebuilt and restarted, proxy restarted, n8n workflow activated, stack health passed at `2026-04-27T05:48:02Z`, and live API health returned `200`
- fixed the deploy-blocking frontend type error by replacing the undefined `MessageIcon` in `BookingAssistantDialog.tsx` with `lucide-react` `MessageCircle`; frontend TypeScript then passed
- verified trusted Telegram actor `8426853622` has deploy/build/test/workspace and allowlisted `host_command` authority; unrestricted `host_shell` remains intentionally disabled unless `BOOKEDAI_ENABLE_HOST_SHELL=1` is set for audited break-glass work
- operator then requested full root/full-host access as the repo default for OpenClaw and `@Bookedairevenuebot`; repo defaults now grant trusted Telegram actions `host_shell`, `openclaw_runtime_admin`, and `full_project`, set `BOOKEDAI_ENABLE_HOST_SHELL=1`, and run `openclaw-cli` as root with privileged host PID, `/hostfs`, and Docker socket access
- live OpenClaw full-access policy was applied to the v3 runtime config and the stack was recreated; verification confirmed CLI `user=0:0`, `privileged=true`, `pid=host`, `/hostfs`, Docker socket, gateway health live, and CLI health healthy
- `enable-openclaw-full-access` was corrected for OpenClaw v2026.4.15 by removing schema-invalid `tools.exec.askFallback` from `openclaw.json` while keeping `askFallback=full` in `exec-approvals.json`
- Telegram remains trusted-actor allowlist scoped by default; the change does not open the operator bot to every Telegram sender
- fixed the remaining Telegram/OpenClaw deploy-live Docker host-context failure: `telegram_workspace_ops.py` now resolves `nsenter` through `/hostfs` and falls back to full-host `host-shell` when Docker is absent in the current runtime; direct OpenClaw CLI probe confirmed `docker=None` but `should_nsenter=True`
- ran the requested VPS host deploy with `cd /home/dovanlong/BookedAI && bash scripts/deploy_live_host.sh`; deploy completed, stack health passed at `2026-04-27T06:05:59Z`, and live API health returned `200`
- restored customer Telegram bot token settings into both `supabase/.env` and root `.env`, then redeployed from the VPS host; backend container now has both customer Telegram env vars present, webhook was reactivated to `/api/webhooks/bookedai-telegram`, pending updates are `0`, and stack health passed at `2026-04-27T06:40:32Z`
- redeployed backend and homepage live through `python3 scripts/telegram_workspace_ops.py --telegram-user-id 8426853622 deploy-live`; preflight backend py_compile and frontend TypeScript passed, stack health passed at `2026-04-27T08:20:21Z`, API health returned `200`, and `https://bookedai.au/` returned `200` with the expected BookedAI title
- redeployed homepage live again through the fixed Telegram/OpenClaw deploy path; frontend TypeScript passed, stack health passed at `2026-04-27T08:51:01Z`, homepage returned `200` with expected BookedAI title, and API health returned `200`
- redeployed homepage live from the VPS host entrypoint `bash scripts/deploy_live_host.sh`; stack health passed at `2026-04-27T08:57:44Z`, `bookedai.au` returned `200`, and `bookedai-web-1`/proxy were freshly up
- rebuilt and redeployed the full live BookedAI stack with `python3 scripts/telegram_workspace_ops.py deploy-live`; backend, beta-backend, web, and beta-web images rebuilt, containers recreated, proxy restarted, and n8n workflow activated
- post-deploy verification passed with `bash scripts/healthcheck_stack.sh` at `2026-04-27T00:09:13Z`
- live HTTP smoke returned `200` for API health, public, product, portal, tenant, admin, pitch, beta, and bot health surfaces
- host Docker status showed BookedAI production containers newly up; one unrelated non-BookedAI container named `optimistic_greider` remained unhealthy and was not part of the deploy closeout
- removed stray unhealthy container `optimistic_greider` after confirming it had no mounts, ports, compose ownership, or restart policy; it was a leftover OpenClaw debug/grep container and BookedAI/OpenClaw/Supabase services remained up
- post-cleanup stack health passed again at `2026-04-27T00:11:53Z`; GitHub sync excluded `.github/workflows/release-gate.yml` because the available GitHub token still lacks `workflow` scope
- completed local Product full-flow UI/UX merge for `product.bookedai.au`: `Chat -> Search -> Preview -> Select -> Book -> Payment posture -> Thank You/Portal -> Calendar/CRM/email/messaging -> Telegram/customer-care handoff`
- updated Product shell copy, assistant journey/status copy, confirmation messages/timeline, and regression tests; Product shortcuts now show Chess, Future Swim, AI Event WSTI, and AI Mentor 1-1; frontend typecheck passed and Product Playwright regression/UAT passed (`6 passed`), including mobile `390x844` and desktop web app preview-to-book flow
- live deploy/smoke for this Product UI pass completed later on `2026-04-27`
- completed QA follow-up for Product flow: removed duplicate post-booking QR, added one-QR regression assertions, restored live-read result provenance/next-step chips, and added a chat-stream soft fallback so V1 search still renders if `/chat/send` fails
- verification passed: Product regression (`6 passed`), live-read Product popup UAT (`4 passed`), frontend build, release gate with frontend/tenant/backend/search all passing, and backend booking route tests (`7 passed`)
- tightened Product slow-search and booking-to-care flow: instant local matches now appear while live-read verifies ranking/booking actions, duplicate shadow search is avoided after live-read grounding, Thank You uses compact portal/Telegram decision cards, email previews show `info@bookedai.au`, and linked Telegram follow-up via `@BookedAI_Manager_Bot` is queued/sent only when a customer Telegram chat is actually linked
- promoted the Product full-flow pass live with `python3 scripts/telegram_workspace_ops.py deploy-live`; stack health passed at `2026-04-27T02:45:45Z`, `product.bookedai.au` returned `HTTP/2 200`, the served Product bundle contains the slow-search status/Telegram/email/shortcut updates, and live mobile smoke at `390x844` confirmed all four shortcuts plus no horizontal overflow
- operator sync archived the live-promotion notes and posted Discord summaries; Notion sync returned external `HTTP 403` on two attempts
- placed uploaded product-proof screenshot `https://upload.bookedai.au/images/df6e/iarJydFRgp1aWGk5UF0d7g.png` after the chess proof on `bookedai.au` and inside the `Product proof` section on `pitch.bookedai.au`; docs now track the placement as a public/pitch evidence-surface change
- verification passed with frontend typecheck, frontend build, and `git diff --check`; Notion/Discord sync succeeded with archive `docs/development/telegram-sync/2026-04-27/030128-public-and-pitch-product-proof-image-placement.md`; live deploy was not run
- refreshed the embedded public pitch video on `bookedai.au` and `pitch.bookedai.au` to `https://upload.bookedai.au/videos/9eb8/BhVuOlB2QXlBo-_nyOFCcA.mp4`; frontend build, deploy-live, stack health, direct byte-range probe, and live bundle checks passed
- reviewed Telegram customer booking flow end-to-end against CRM/email handoff paths and found a gap: Telegram `Book n` capture created `booking_intents` but did not trigger booking follow-up CRM records or confirmation-email lifecycle tracking like the v1 web booking flow
- patched Telegram messaging flow so captured booking intents now carry customer/contact fields in metadata and trigger CRM deal/task sync plus lifecycle email recording/send attempts when customer email is present; syntax check passed with `python3 -m py_compile`
- QA environment note: repo runtime here currently lacks installed FastAPI/pytest deps in the available Python envs, so full automated route tests could not be executed in-session; static review plus Python syntax validation completed instead
- added an admin integrations reliability view for customer-booking Telegram care, exposing customer-agent health, webhook backlog, callback ack posture, identity-resolution watchlist, and tenant-scoped Telegram/CRM/email/outbox spotlight cards inside the admin workspace
- added Telegram deep-link entry points on booking and post-booking surfaces in both the homepage booking flow and landing assistant dialog; post-booking links now open `@BookedAI_Manager_Bot` with a booking-context start payload, and pre-booking links open Telegram care with selected-service context
- Telegram webhook intake now rewrites deep-link `/start` payloads like `bk.<booking_reference>` and `svc.<service>` into useful booking-help text so the new chat session lands with the right booking or product context; frontend typecheck and backend `py_compile` both passed
- synced the Phase 19 Telegram/customer-care follow-up across `project.md`, implementation progress, next-phase plan, master roadmap, current sprint execution plan, and phase operating system so docs now reflect Telegram handoff, admin reliability spotlight, webhook metadata preservation, and the remaining verification gates
- QA follow-up added stronger Telegram route-test expectations for `Book n` capture: lifecycle metadata must now include CRM deal/task and email-confirmation evidence, with recipient verification for `long@example.com`; direct pytest remains blocked until a clean backend test env is rebuilt
- backend test-env investigation found the current `.venv` and `.venv-backend` are broken mixed-version environments; recommended next lane is a fresh local Python 3.11 `.venv-test` from `backend/requirements.txt` plus `pytest`, with Docker-image execution as the parity fallback
- prepared a concrete live UAT checklist for tenant `co-mai-hung-chess-class` covering Telegram search/book, confirmation email, portal reopen, existing-order continuity, and CRM sync evidence, using one booking reference as the end-to-end proof artifact
- operator clarified tenant-chess UAT credentials/context: use `chess@bookedai.au` instead of `long@example.com`; this mailbox is also the tenant login user, default password is `FirstHundredM$`, and the tenant chess Telegram id is `@Dvl_Aus`
- added a Telegram regression test for the merged-side-effects edge case so one webhook turn can carry both booking-intent lifecycle evidence and queued-request lifecycle evidence without overwriting `metadata.lifecycle_updates`; `python3 -m py_compile backend/tests/test_telegram_webhook_routes.py backend/api/route_handlers.py` passed
- attempted to rebuild a clean backend test env, but this runtime initially lacked `python3.11-venv`, `pip`, `ensurepip`, and Docker; worked around it by bootstrapping user-local `pip` with `get-pip.py --break-system-packages`, installing `virtualenv`, and creating a clean repo-local `.venv-test` on Python 3.11
- direct pytest from the workspace then hit a repo `.env` permission block, so the backend tree was copied to `/tmp/bookedai-test` for a clean read without touching protected secrets; from there `/.venv-test/bin/python -m pytest backend/tests/test_telegram_webhook_routes.py -q` passed (`12 passed`) and `/.venv-test/bin/python -m pytest backend/tests/test_whatsapp_webhook_routes.py -q` passed (`13 passed`)
- created `docs/development/chess-telegram-live-uat-quick-run-2026-04-27.md`, `docs/development/chess-telegram-live-uat-evidence-log-2026-04-27.md`, and `docs/development/chess-telegram-live-uat-closeout-template-2026-04-27.md` so the chess Telegram live UAT now has operator run, evidence, and closeout templates
- checked OpenClaw runtime config: Telegram interaction for trusted operator Aus is already active in `/home/node/.openclaw/openclaw.json` with `channels.telegram.enabled=true`, `dmPolicy=allowlist`, `allowFrom=[8426853622]`, and elevated Telegram allow-from set to the same actor; syncing the separate BookedAI customer-agent manifest is currently blocked by permission denied on `/home/node/.openclaw/agents/bookedai-booking-customer-agent.json`
- verified the public customer-bot handle `@BookedAI_Manager_Bot` resolves on Telegram with a live `Start Bot` page; because Telegram customer bots cannot initiate a first DM, sent Aus a proactive Telegram message through the operator bot with deep links for tenant-chess (`https://t.me/BookedAI_Manager_Bot?start=svc.co-mai-hung-chess-class`), generic customer chat, and the booking-reference continuity pattern `?start=bk.<booking_reference>`
- redeployed the live stack on `2026-04-27` through `python3 scripts/telegram_workspace_ops.py deploy-live`; backend/beta-backend/web/beta-web rebuilt and restarted, proxy restarted, n8n workflow activated, stack health passed at `2026-04-27T05:48:02Z`, and live API health returned `200`
- fixed the deploy-blocking frontend type error by replacing the undefined `MessageIcon` in `BookingAssistantDialog.tsx` with `lucide-react` `MessageCircle`; frontend TypeScript then passed
- verified trusted Telegram actor `8426853622` has deploy/build/test/workspace and allowlisted `host_command` authority; unrestricted `host_shell` remains intentionally disabled unless `BOOKEDAI_ENABLE_HOST_SHELL=1` is set for audited break-glass work
- operator then requested full root/full-host access as the repo default for OpenClaw and `@Bookedairevenuebot`; repo defaults now grant trusted Telegram actions `host_shell`, `openclaw_runtime_admin`, and `full_project`, set `BOOKEDAI_ENABLE_HOST_SHELL=1`, and run `openclaw-cli` as root with privileged host PID, `/hostfs`, and Docker socket access
- live OpenClaw full-access policy was applied to the v3 runtime config and the stack was recreated; verification confirmed CLI `user=0:0`, `privileged=true`, `pid=host`, `/hostfs`, Docker socket, gateway health live, and CLI health healthy
- `enable-openclaw-full-access` was corrected for OpenClaw v2026.4.15 by removing schema-invalid `tools.exec.askFallback` from `openclaw.json` while keeping `askFallback=full` in `exec-approvals.json`
- Telegram remains trusted-actor allowlist scoped by default; the change does not open the operator bot to every Telegram sender
- fixed the remaining Telegram/OpenClaw deploy-live Docker host-context failure: `telegram_workspace_ops.py` now resolves `nsenter` through `/hostfs` and falls back to full-host `host-shell` when Docker is absent in the current runtime; direct OpenClaw CLI probe confirmed `docker=None` but `should_nsenter=True`
- ran the requested VPS host deploy with `cd /home/dovanlong/BookedAI && bash scripts/deploy_live_host.sh`; deploy completed, stack health passed at `2026-04-27T06:05:59Z`, and live API health returned `200`
- restored customer Telegram bot token settings into both `supabase/.env` and root `.env`, then redeployed from the VPS host; backend container now has both customer Telegram env vars present, webhook was reactivated to `/api/webhooks/bookedai-telegram`, pending updates are `0`, and stack health passed at `2026-04-27T06:40:32Z`
- homepage audit via parallel QA, UX/UI, and copy reviews converged on the same P0 issues: positioning is unclear between local-discovery marketplace vs B2B AI revenue engine, hero/CTA promise a search-led flow without real result behavior, and public trust/proof is too weak for conversion
- homepage QA flagged release-risk items to verify before pushing more homepage changes: runtime `/` had a recent 500 in one environment, `Search` currently scrolls instead of producing true results, `Book a Demo` behaves like an anchor rather than a real demo/contact flow, and existing homepage tests may no longer match the shipped experience
- recommended next homepage lane is B2B conversion-first: rewrite above-the-fold around revenue/booking outcomes, remove internal strategy copy, add trust metrics/logos/testimonials near hero, tighten CTA hierarchy, and either implement a real search/results path or stop positioning the homepage as search-led
- deployed the requested new homepage live via `python3 scripts/telegram_workspace_ops.py --telegram-user-id 8426853622 deploy-live`; backend, beta-backend, web, and beta-web containers were recreated, proxy restarted, and n8n booking workflow stayed active
- post-deploy stack health passed at `2026-04-27T08:31:41Z`; `https://bookedai.au/` returned `HTTP/2 200` with the expected `BookedAI | The AI Revenue Engine for Service Businesses` title, and API health returned `200`
- verified the live web container bundle includes the new homepage conversational/revenue-engine copy, including `The homepage is now the primary conversational landing surface` and `AI revenue engine for service businesses`
- granted full host root posture to Linux users `openclaw` and `telegram`: created `telegram`, added both users to `sudo` and `docker`, installed `/etc/sudoers.d/99-bookedai-full-root-users` with passwordless `ALL`, and validated sudoers syntax
- verification passed for both users: `sudo -n id` returned root and `sudo -n docker ps` listed the live BookedAI/OpenClaw/Supabase containers
- re-applied full deploy permissions after ACL mask drift: `openclaw`, `telegram`, `ubuntu`, and host uid `1000` now have recursive/default repo ACL write access, `/home/dovanlong` traverse access, and executable deploy/operator scripts; the previously unreadable Telegram access memory file is readable again
- verified as both `openclaw` and `telegram`: passwordless root sudo works, Docker host access works, trusted actor `8426853622` has `deploy_live`, `host_shell`, `openclaw_runtime_admin`, and `full_project`, and host shell is enabled
- proved the full-deploy lane by running `python3 scripts/telegram_workspace_ops.py --telegram-user-id 8426853622 deploy-live` as `openclaw`; production rebuild/recreate completed, n8n workflow activated, stack health passed at `2026-04-27T08:53:07Z`, and API/public/OpenClaw hosts returned `200`
- operator sync archived this permission/deploy closeout at `docs/development/telegram-sync/2026-04-27/085402-openclaw-full-deploy-host-permissions-repaired.md`; Notion page creation and Discord summary both succeeded
- changed the deploy execution context so `deploy-live` treats host users `openclaw` and `telegram` as trusted full deploy users and routes them through the VPS host-shell path; `deploy_live_host.sh` also self-enters the host namespace through `nsenter` when called directly from a privileged OpenClaw runtime with `/hostfs`
- verified syntax plus permissions for both users, then ran `deploy-live` as `telegram`; production rebuild/recreate and n8n activation completed, stack health passed at `2026-04-27T08:56:59Z`, and API/public/OpenClaw hosts returned `200`
- operator sync archived this execution-context closeout at `docs/development/telegram-sync/2026-04-27/085730-openclaw-and-telegram-deploy-live-host-execution-context-enabled.md`; Notion and Discord both succeeded
- fixed the remaining sudo-less runtime deploy blocker: `telegram_workspace_ops.py` now runs `nsenter` host-context commands directly instead of prepending `sudo`, and only requires `sudo` when there is no host namespace prefix and the process is not root
- verified the no-sudo/nsenter path with a simulation, ran `bash scripts/deploy_live_host.sh` on the VPS host, stack health passed at `2026-04-27T08:59:56Z`, and API/public/product/OpenClaw hosts returned `200`
- operator sync archived the sudo-less host-shell repair at `docs/development/telegram-sync/2026-04-27/090051-openclaw-sudo-less-host-shell-deploy-repair.md`; Notion and Discord both succeeded
- reactivated Telegram bot webhooks for both surfaces: operator `@Bookedairevenuebot` -> `https://api.bookedai.au/telegram-webhook` with pending `0` and no last error, and customer `@BookedAI_Manager_Bot` -> `https://api.bookedai.au/api/webhooks/bookedai-telegram` with pending `0`
- fixed live customer Telegram webhook processing by casting `tenant_ref` to text in repository tenant lookup SQL; after deploy, synthetic customer Telegram webhook returned `200` / `messages_processed=1` and backend logs no longer showed the idempotency `AmbiguousParameterError`
- configured tenant `co-mai-hung-chess-class` notifications to Telegram chat id `8426853622`; customer bot test send returned message id `79`, and operator bot test send returned message id `550`
- operator sync archived the Telegram bot activation closeout at `docs/development/telegram-sync/2026-04-27/091207-operator-and-customer-telegram-bots-activated-for-tenant.md`; Notion and Discord both succeeded
- rechecked Telegram channels after a transient operator webhook `401`: re-set `@Bookedairevenuebot` webhook with the live OpenClaw secret, pending updates drained from `4` to `0`, and `last_error` returned to `null`; customer `@BookedAI_Manager_Bot` remained pending `0` on `/api/webhooks/bookedai-telegram`
- hardened `host-shell` sudo behavior for OpenClaw/Telegram: non-root runtimes with `nsenter` now try direct host namespace execution and fall back to `sudo -n nsenter ...` if direct `nsenter` is denied; commands inside the host namespace can use host `sudo`
- verified `host-shell --command "sudo -n id && sudo -n docker ps"` as both `openclaw` and `telegram`, then ran `deploy-live`; stack health passed at `2026-04-27T09:46:28Z`, and API/public/product/OpenClaw probes returned `200`
- operator sync archived the host-shell sudo/deploy closeout at `docs/development/telegram-sync/2026-04-27/094719-openclaw-telegram-host-shell-sudo-support-and-live-deploy.md`; Notion and Discord both succeeded
- added `host-exec` as an explicit alias for full host-shell execution, gated by `host_shell`, for OpenClaw requests that ask for host-exec; verified it can run `sudo -n id` and Docker under both `openclaw` and `telegram`
- deployed live through `host-exec --cwd /home/dovanlong/BookedAI --command "bash scripts/deploy_live_host.sh"`; stack health passed at `2026-04-27T09:54:52Z`, and API/public/product/OpenClaw probes returned `200`
- operator sync archived the host-exec live deploy closeout at `docs/development/telegram-sync/2026-04-27/095553-openclaw-host-exec-enabled-and-host-live-deploy-completed.md`; Notion and Discord both succeeded
- aligned outgoing BookedAI email configuration on `info@bookedai.au`: root `.env` now uses it for booking business email, SMTP username/from, and IMAP username; Supabase email admin/certbot settings, backend blank-env fallbacks, and Docker dev/prod compose defaults also resolve to the same mailbox
- updated remaining webapp mailto/login placeholders from older BookedAI mailboxes to `info@bookedai.au`; focused config/booking-assistant pytest passed (`18 passed`) plus direct settings probes for root `.env` and omitted-env fallback
- deployed the email identity change live with `bash scripts/deploy_live_host.sh`; stack health passed at `2026-04-27T09:28:05Z`, API/public HTTP smoke passed, and backend/beta-backend container probes confirmed booking business email, SMTP username/from, and IMAP username all resolve to `info@bookedai.au`
- SMTP auth check without sending mail returned `SMTPAuthenticationError`; real outbound delivery now requires replacing `EMAIL_SMTP_PASSWORD` and `EMAIL_IMAP_PASSWORD` with valid Zoho app passwords for `info@bookedai.au`
- operator sync archived the email identity closeout at `docs/development/telegram-sync/2026-04-27/092854-email-sender-identity-aligned-on-info-bookedai-au.md`; Notion page creation and Discord summary both succeeded
- follow-up operator sync corrected the closeout status to credential-action-required at `docs/development/telegram-sync/2026-04-27/093042-email-sender-identity-deployed-info-bookedai-au-smtp-secret-required.md`; Notion and Discord both succeeded
- WhatsApp/Twilio live readiness check: API and provider-status routes are live, runtime reports primary `whatsapp_meta` with `whatsapp_twilio` fallback, outbound test to `+61455301335` fell back from Meta to Twilio and returned Twilio provider id `SM38f700466354b9a66c1c38f2777e7252` with `queued` status, and `backend/tests/test_whatsapp_webhook_routes.py` passed (`13 passed`); inbound/reply code path is healthy, but true customer reply still needs a real inbound WhatsApp message from the paired number to prove end-to-end delivery.
- Telegram pending-booking UX tightened locally: booking-captured/payment-pending replies and existing-booking care replies now expose rich HTML Telegram menus for keep booking, view portal/QR, change time, cancel, new booking search, and open BookedAI; Telegram webhook regression passed (`15 passed`), with live deploy/UAT still pending.
- operator sync archived the Telegram pending-booking menu closeout at `docs/development/telegram-sync/2026-04-27/093226-telegram-pending-booking-rich-menu.md`; Notion and Discord both succeeded.
- Live WhatsApp/SMS send test to `+61455301335` and `+61481993178`: BookedAI WhatsApp fell back from Meta to Twilio and created Twilio SIDs `SM58abc35d34de689556393dd339d94014` and `SM22b3c430d03592b4b8690ecaa0e46c9d`, but provider status was `undelivered`/error `63016` for `+61455301335` and `failed`/error `63015` for `+61481993178`; BookedAI SMS/Twilio queued both messages for manual review, and direct Twilio SMS with configured API-key mode returned `401 Authenticate`/`20003` while account SID/auth-token mode was missing an SMS auth token.
- Portal QR/payment/channel handoff tightened locally: `PortalApp` now shows booking QR, payment QR/posture, canonical booking-reference URL, copy/save/download controls, Telegram/WhatsApp continuation links with the ID attached, and action-effect guidance for pay/change/cancel/help/add paths; frontend TypeScript passed and focused portal Playwright passed (`6 passed`), with live deploy/UAT still pending.
- operator sync archived the portal QR handoff closeout at `docs/development/telegram-sync/2026-04-27/094528-portal-qr-payment-channel-handoff.md`; Notion and Discord both succeeded.
- merged the only open PR into `main` (PR #3: Telegram customer webhook secret enforcement plus booking-assistant fallback stabilization), rebuilt backend/web/beta images from `origin/main`, redeployed the live `bookedai` stack, repaired the recreated Supabase app/n8n database roles/databases, and stack health passed at `2026-04-27T10:10:20Z`; API/public/product/bot/n8n probes all returned `200`.
- post-deploy Telegram channel check: customer `@BookedAI_Manager_Bot` webhook remains on `/api/webhooks/bookedai-telegram` with pending `0`; operator webhook remains on `/telegram-webhook` with no last error and one pending update at the check moment; trusted actor `8426853622` still has deploy/live host-shell permissions.
- committed and merged the accumulated live workspace source/docs/deploy changes through PR #4 into `main` at `84c3f9b`, rebuilt production backend/beta/web/Hermes images from merged main, restarted Supabase/app stack, repaired app/n8n DB roles/databases, and stack health passed at `2026-04-27T10:22:03Z`; API/public/product/OpenClaw/n8n probes all returned `200`.
- `.github/workflows/release-gate.yml` remains local only because GitHub rejected workflow-file push under the current OAuth token without `workflow` scope; no real `.env` or private key files were staged.
