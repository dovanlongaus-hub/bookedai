services:
  openclaw-gateway:
    image: ${OPENCLAW_IMAGE:-ghcr.io/openclaw/openclaw:2026.3.12}
    container_name: openclaw-bookedai-gateway
    environment:
      HOME: /home/node
      TERM: xterm-256color
      TZ: ${OPENCLAW_TZ:-UTC}
      CODEX_HOME: /home/node/.codex
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      BOOKEDAI_TELEGRAM_TRUSTED_USER_IDS: ${BOOKEDAI_TELEGRAM_TRUSTED_USER_IDS:-8426853622}
      BOOKEDAI_TELEGRAM_ALLOWED_ACTIONS: ${BOOKEDAI_TELEGRAM_ALLOWED_ACTIONS:-sync_doc,sync_repo_docs,build_frontend,deploy_live,test,workspace_write,repo_structure,host_command,host_shell,full_project}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      ANTHROPIC_BASE_URL: ${ANTHROPIC_BASE_URL:-}
    volumes:
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace
      - ${BOOKEDAI_REPO_DIR}:/workspace/bookedai.au
      - ${CODEX_HOME_DIR}:/home/node/.codex
    ports:
      - "${OPENCLAW_GATEWAY_PORT:-18789}:18789"
      - "${OPENCLAW_BRIDGE_PORT:-18790}:18790"
    networks:
      - default
      - bookedai_internal
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${OPENCLAW_GATEWAY_BIND:-lan}",
        "--port",
        "18789"
      ]
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:18789/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 720s

  openclaw-cli:
    image: ${OPENCLAW_IMAGE:-ghcr.io/openclaw/openclaw:2026.3.12}
    container_name: openclaw-bookedai-cli
    network_mode: "service:openclaw-gateway"
    user: "0:0"
    privileged: true
    pid: host
    environment:
      HOME: /home/node
      TERM: xterm-256color
      TZ: ${OPENCLAW_TZ:-UTC}
      BROWSER: echo
      CODEX_HOME: /home/node/.codex
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      BOOKEDAI_TELEGRAM_TRUSTED_USER_IDS: ${BOOKEDAI_TELEGRAM_TRUSTED_USER_IDS:-8426853622}
      BOOKEDAI_TELEGRAM_ALLOWED_ACTIONS: ${BOOKEDAI_TELEGRAM_ALLOWED_ACTIONS:-sync_doc,sync_repo_docs,build_frontend,deploy_live,test,workspace_write,repo_structure,host_command,host_shell,full_project}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      ANTHROPIC_BASE_URL: ${ANTHROPIC_BASE_URL:-}
    volumes:
      - ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw
      - ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace
      - ${BOOKEDAI_REPO_DIR}:/workspace/bookedai.au
      - ${CODEX_HOME_DIR}:/home/node/.codex
      - /:/hostfs
      - /var/run/docker.sock:/var/run/docker.sock
    stdin_open: true
    tty: true
    init: true
    restart: unless-stopped
    entrypoint: ["node", "dist/index.js"]
    command:
      [
        "node",
        "run",
        "--host",
        "127.0.0.1",
        "--port",
        "18789",
        "--display-name",
        "bookedai-host-cli"
      ]
    depends_on:
      - openclaw-gateway

networks:
  default:
    name: openclaw_default
  bookedai_internal:
    external: true
